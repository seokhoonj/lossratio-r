% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/link-vis.R
\name{plot.Link}
\alias{plot.Link}
\title{Plot link-factor diagnostics}
\usage{
\method{plot}{Link}(x, model = NULL, ...)
}
\arguments{
\item{x}{An object of class \code{"Link"}.}

\item{model}{Either \code{"ata"} or \code{"ed"}. Default depends on
\code{attr(x, "premium_var")}.}

\item{...}{Arguments forwarded to the underlying plotting helper. See
the per-model parameter list in Details.}
}
\value{
A \code{ggplot} object.
}
\description{
Visualise diagnostic summaries from a \code{"Link"} object. Dispatches to
the multiplicative ATA branch (\code{model = "ata"}) or the additive
exposure-driven branch (\code{model = "ed"}).

The default \code{model} is chosen from \code{attr(x, "premium_var")}: \code{NULL}
(single-variable link) selects \code{"ata"}, a non-\code{NULL} exposure variable
(dual-variable link) selects \code{"ed"}.
}
\details{
For \code{model = "ata"}, accepted arguments include \code{type}
(\code{"cv" | "rse" | "summary" | "box" | "point"}), \code{alpha}, \code{show_maturity},
\code{max_cv}, \code{max_rse}, \code{min_valid_ratio}, \code{min_n_valid},
\code{min_run}, \code{scales}, \code{nrow}, \code{ncol}, \code{theme}, and \code{x.angle}.

For \code{model = "ed"}, accepted arguments include \code{type}
(\code{"summary" | "box" | "point"}), \code{alpha}, \code{scales}, \code{nrow}, \code{ncol},
\code{theme}, and \code{x.angle}.
}
