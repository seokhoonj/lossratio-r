% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/triangle.R
\name{mask_triangle}
\alias{mask_triangle}
\title{Mask the last N calendar diagonals from a Triangle}
\usage{
mask_triangle(x, holdout = 0L)
}
\arguments{
\item{x}{A \code{Triangle} object.}

\item{holdout}{Non-negative integer. Number of latest calendar
diagonals to mask. \code{0L} (default) returns a copy of \code{x}
unchanged.}
}
\value{
A \code{Triangle} with the held-out cells removed.
}
\description{
Drops the most recent \code{holdout} calendar diagonals (per group) from
a \code{Triangle}, returning a new \code{Triangle} of the same class with all
attributes preserved. Useful for simulating a historical analyst's
view -- the same masking \code{\link[=backtest]{backtest()}} and \code{detect_regime(holdout=)}
apply internally.

The calendar diagonal index is built as \code{rank(cohort) + dev - 1},
with \code{rank()} computed within group. The \code{holdout} most recent
calendar indices are dropped.
}
\examples{
\dontrun{
data(experience)
tri <- as_triangle(experience, groups = "coverage",
                      cohort = "uy_m", calendar = "cy_m",
                      loss = "incr_loss", exposure = "incr_exposure")

# Inspect what the analyst at a 6-month historical cutoff would see
tri_masked <- mask_triangle(tri, holdout = 6L)
plot_triangle(tri_masked)

# Use same masked tri to detect regime + fit
r   <- detect_regime(tri_masked)
fit <- fit_ratio(tri_masked, loss_regime = r)
}

}
