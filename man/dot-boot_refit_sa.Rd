% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{.boot_refit_sa}
\alias{.boot_refit_sa}
\title{Refit stage-adaptive (SA) hybrid per bootstrap replicate}
\usage{
.boot_refit_sa(triangle, boots, alpha = 1, maturity)
}
\arguments{
\item{triangle}{The original \code{Triangle} (carries the observed region
mask).}

\item{boots}{A \code{BootstrapTriangle} from \code{bootstrap()}.}

\item{alpha}{Mack variance exponent (currently \code{1} only).}

\item{maturity}{Resolved \code{Maturity} object.}
}
\value{
Same shape as \code{.boot_refit_cl()}.
}
\description{
ED projection for dev \verb{< maturity$change}, CL projection from
\code{maturity$change} onward. Maturity is per-group when supplied with
group columns. The per-cell process variance switches between
\verb{sigma^2*_g_k * P_\{i,k-1\}} (ED region) and
\verb{sigma^2*_f_k * cell_mean[i, k-1]} (CL region) accordingly.
}
\keyword{internal}
