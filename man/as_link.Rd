% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/link.R
\name{as_link}
\alias{as_link}
\title{Coerce a Triangle to a Link object}
\usage{
as_link(
  x,
  loss = "loss",
  exposure = NULL,
  weight = NULL,
  min_denom = 0,
  drop_invalid = FALSE
)
}
\arguments{
\item{x}{A \code{Triangle} object.}

\item{loss}{A single cumulative metric used as the link
numerator. Must be one of \code{"loss"}, \code{"premium"}, or \code{"ratio"}. Default
\code{"loss"}. For loss-side ATA this is the cumulative loss column, but
any cumulative metric on the Triangle may be supplied.}

\item{exposure}{Optional second cumulative metric, treated as the
exposure base (denominator anchor) for the ED workflow. Must be
one of \code{"loss"}, \code{"premium"}, \code{"ratio"}, and must differ from
\code{loss}. When \code{NULL} (default), only the single-variable columns
are produced.}

\item{weight}{Optional cumulative metric used as WLS weight in
downstream \code{summary} / \code{fit_ata} calls. Must differ from
\code{loss}. Cannot be combined with \code{exposure} (the dual
workflow has its own anchor).}

\item{min_denom}{Minimum denominator required to compute \code{ata}
and \code{intensity}. If \code{loss_from <= min_denom}, \code{ata} becomes \code{NA};
if \code{premium_from <= min_denom}, \code{intensity} becomes \code{NA}. Default
\code{0}.}

\item{drop_invalid}{Logical; if \code{TRUE}, rows with non-finite \code{ata}
(single-var) or non-finite \code{intensity} (dual-var) are dropped.
Default \code{FALSE} so the full link grid is preserved for
diagnostics.}
}
\value{
A \code{data.table} of class \code{"Link"} with columns:
\itemize{
\item Always: \verb{[group]}, \code{cohort}, \code{ata_from}, \code{ata_to}, \code{ata_link},
\code{loss_from}, \code{loss_to}, \code{loss_delta}, \code{ata}.
\item If \code{exposure} is set: also \code{premium_from}, \code{premium_to},
\code{premium_delta}, \code{intensity}.
\item If \code{weight} is set: also \code{weight}.
}

The returned object carries attributes \code{groups}, \code{cohort},
\code{dev}, \code{loss}, \code{premium} (or \code{NULL}), \code{weight}
(or \code{NULL}).
}
\description{
Derive the development-link table from a \code{Triangle} and assign the
\code{Link} S3 class so the associated \code{summary.Link()}, \code{plot.Link()},
\code{fit_ata()}, \code{fit_intensity()}, etc. methods dispatch on the result.

Unlike \code{\link[=as_triangle]{as_triangle()}} / \code{\link[=as_calendar]{as_calendar()}} / \code{\link[=as_total]{as_total()}} (which take
raw experience data and validate/aggregate it), \code{as_link()} operates
\emph{on a Triangle} (already validated upstream) and reshapes it into
link-pair rows. Each row corresponds to one development link
\verb{(cohort, ata_from -> ata_to)}, the long-format intermediate
underlying both the chain ladder (CL) and exposure-driven (ED)
workflows.

Two modes are produced depending on \code{exposure}:

\describe{
\item{Single-variable mode (\code{exposure = NULL})}{The age-to-age
factor is \eqn{ata = value_{to} / value_{from}}, where
\eqn{value} is the column named by \code{loss}.}
\item{Dual-variable mode (\code{exposure} supplied)}{In addition to
the loss-side ATA, the exposure-driven intensity
\eqn{g = \Delta loss / premium_{from}} is computed and stored in
the \code{intensity} column. The exposure measure is the denominator
for loss ratio calculations; for long-term health insurance
applications, risk premium is commonly used.}
}
}
\examples{
\dontrun{
tri <- as_triangle(
  df,
  groups   = "coverage",
  cohort   = "uy_m",
  calendar = "cy_m",
  loss     = "incr_loss",
  premium  = "incr_premium"
)

# Single-variable: cumulative-loss link factors (ATA workflow)
link_loss <- as_link(tri, loss = "loss")

# Dual-variable: ED-ready link table (loss + premium)
link_ed <- as_link(tri, loss = "loss", exposure = "premium")
head(link_ed)
}

}
\seealso{
\code{\link[=as_triangle]{as_triangle()}}, \code{\link[=summary.Link]{summary.Link()}}, \code{\link[=plot.Link]{plot.Link()}},
\code{\link[=fit_ata]{fit_ata()}}, \code{\link[=fit_ed]{fit_ed()}}
}
