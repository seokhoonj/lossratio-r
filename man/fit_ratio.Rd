% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ratio.R
\name{fit_ratio}
\alias{fit_ratio}
\title{Fit loss ratio projection model}
\usage{
fit_ratio(
  x,
  method = c("ed", "cl", "sa", "bf", "cc"),
  loss_alpha = 1,
  loss_regime = NULL,
  exposure_method = c("ed", "cl"),
  exposure_alpha = 1,
  exposure_regime = NULL,
  sigma_method = c("locf", "min_last2", "loglinear", "mack", "none"),
  recent = NULL,
  maturity = "auto",
  tail = FALSE,
  se_method = c("fixed", "delta"),
  rho = 0.95,
  conf_level = 0.95,
  bootstrap = NULL,
  B = 999L,
  seed = NULL,
  ...
)
}
\arguments{
\item{x}{An object of class \code{"Triangle"}. The standardized \code{"loss"}
and \code{"exposure"} columns are used (\code{as_triangle()} produces these).}

\item{method}{One of \code{"ed"} (default), \code{"cl"}, \code{"sa"}, \code{"bf"}, or
\code{"cc"} -- the loss-side projection method, forwarded to
\code{\link[=fit_loss]{fit_loss()}}. \code{"bf"} and \code{"cc"} require their prior arguments
(\code{prior}, etc.) supplied through \code{...}.}

\item{loss_alpha}{Numeric scalar controlling the variance structure for
loss estimation. Default is \code{1}.}

\item{loss_regime}{Optional regime specification for the loss-side
filter. Accepts four input types:
\describe{
\item{\code{NULL} (default)}{No regime filter.}
\item{\code{Regime} object}{Use as-is. Typically built via
\code{\link[=detect_regime]{detect_regime()}} or \code{\link[=regime_at]{regime_at()}}.}
\item{\code{"auto"}}{Detect regime internally via \code{detect_regime(x)} on
the input triangle.}
\item{Function / closure}{A user-supplied function taking the
triangle and returning a \code{Regime} object (or \code{NULL}).}
}
Behavior depends on \code{method}:
\describe{
\item{\code{"sa"}}{Hybrid filter. Pre-change cohorts are dropped only for
development periods at or before the maturity point (ED phase);
post-maturity (CL) cells use the \code{recent}-diagonal window across
all cohorts. This preserves CL stability while protecting the ED
intensities from a regime change.}
\item{\code{"ed"}, \code{"cl"}}{Simple cohort cut: all cohorts strictly before
the change date are excluded from estimation.}
}}

\item{exposure_method}{One of \code{"ed"} (default) or \code{"cl"}. Forwarded to
\code{\link[=fit_exposure]{fit_exposure()}} when constructing the exposure projection.}

\item{exposure_alpha}{Numeric scalar for exposure chain ladder. Default
is \code{1}.}

\item{exposure_regime}{Exposure-side regime specification. Same four
input types as \code{loss_regime} (\code{NULL} / \code{Regime} / \code{"auto"} / function).
Default \code{NULL} -- exposure is fit on the full triangle independently
of \code{loss_regime} (no lazy default). Set explicitly when the regime
shift affects exposure accrual too.}

\item{sigma_method}{Method used to extrapolate \code{sigma} for links where it
cannot be estimated. One of \code{"locf"} (default), \code{"min_last2"},
\code{"loglinear"}, \code{"mack"}, or \code{"none"}. \code{"mack"} applies the Mack
(1993, Appendix B) tail estimator to the last unestimated link only,
falling back to LOCF for any earlier ones with a warning. \code{"none"}
performs no extrapolation; \code{sigma} stays \code{NA} and downstream variance
terms drop those links via finite-value guards. Passed to
\code{\link[=.extrapolate_sigma_ata]{.extrapolate_sigma_ata()}}.}

\item{recent}{Optional positive integer for estimation window.
Default is \code{NULL}.}

\item{maturity}{Optional maturity specification. Accepts four input
types:
\describe{
\item{\code{NULL}}{No maturity filter. Disables SA-mode switch detection.}
\item{\code{Maturity} object}{Use as-is. Typically built via
\code{\link[=detect_maturity]{detect_maturity()}} or \code{\link[=maturity_at]{maturity_at()}}.}
\item{\code{"auto"} (default)}{Detect maturity internally via
\code{detect_maturity(x)} on the input triangle.}
\item{Function / closure}{A user-supplied function taking the
triangle and returning a \code{Maturity} object (e.g. from
\code{\link[=maturity_spec]{maturity_spec()}}) for deferred custom-config detection.}
}
When \code{method = "sa"}, this also determines the switch point between
ED and CL phases.}

\item{tail}{Tail factor for the loss-side projection, forwarded to
\code{\link[=fit_loss]{fit_loss()}}. \code{FALSE} (default) applies none; \code{TRUE} estimates a
log-linear tail factor; a numeric value is used directly.}

\item{se_method}{Method for computing \code{ratio_se = SE(L/P)}. One of:
\describe{
\item{\code{"fixed"} (default)}{Premium treated as fixed (non-random).
\eqn{\mathrm{SE}(L/P) = \mathrm{SE}(L) / P}. Strictly, this is
the delta method with \code{Var(P) = 0} and \code{Cov(L,P) = 0}, i.e., a
degenerate case under the assumption that exposure is known.}
\item{\code{"delta"}}{Full delta method including exposure uncertainty
and the loss-exposure correlation \code{rho}:
\deqn{\mathrm{Var}(L/P) \approx \frac{\mathrm{Var}(L)}{P^2}
        + \frac{L^2 \mathrm{Var}(P)}{P^4}
        - \frac{2 \rho L \mathrm{SE}(L) \mathrm{SE}(P)}{P^3}}
}
}}

\item{rho}{Numeric scalar in \verb{(-1, 1)}; assumed correlation between
ultimate loss and ultimate exposure. Only used when
\code{se_method = "delta"}. Default is \code{0.95}, matching the strong
positive correlation typically observed between cumulative loss
and cumulative exposure in long-tail health portfolios (analogous
to the paid/incurred correlation used in Munich chain ladder).}

\item{conf_level}{Confidence level used for \code{ratio_ci_lo}/\code{ratio_ci_hi}
in the cohort summary. Default is \code{0.95}.}

\item{bootstrap}{Bootstrap configuration. Five forms accepted:
\describe{
\item{\code{NULL} (default)}{Auto-resolved by \code{method}: bootstrap for
\code{"sa"}/\code{"ed"}, analytical for \code{"cl"}/\code{"bf"}/\code{"cc"}.}
\item{\code{TRUE} / \code{FALSE}}{Back-compat with the legacy logical arg.
\code{TRUE} triggers \code{"auto"}; \code{FALSE} disables.}
\item{\code{"auto"}}{Internal \code{bootstrap()} call on the loss triangle
with defaults \verb{(type = "analytical", process = "normal", target = "loss")}.}
\item{\code{BootstrapTriangle}}{Pre-built object from \code{bootstrap()}.
Must have \code{meta$target == "loss"}.}
\item{Function \verb{function(tri) -> BootstrapTriangle}}{Lazy spec
invoked on the input Triangle (leakage-safe for \code{backtest()}).}
}
Premium is held at observed values during the bootstrap (loss-only
convention). \code{ratio_se} is recomputed from the bootstrap-derived
\code{loss_total_se} via \code{.compute_ratio_se()}, combined with the
exposure-side SE per \code{se_method} (\code{"fixed"} ignores exposure SE;
\code{"delta"} uses \code{exposure_total_se} from the inner \code{fit_exposure()}
plus \code{rho} correlation).}

\item{B}{Integer number of bootstrap replications. Used only when
\code{bootstrap} resolves to \code{"auto"}. Default is \code{999}.}

\item{seed}{Optional integer seed for reproducible bootstrap.
Default is \code{NULL}.}

\item{...}{Additional arguments forwarded to \code{\link[=fit_loss]{fit_loss()}} and its
worker -- notably \code{prior} (and related arguments) when
\code{method = "bf"} or \code{"cc"}.}
}
\value{
An object of class \code{"RatioFit"}.
}
\description{
Unified interface for loss ratio projection from a \code{"Triangle"} object.
Five projection methods are available:

\describe{
\item{\code{"ed"} (default)}{Exposure-driven for all development periods.
All future increments are \eqn{g_k \cdot C^P_k}. Unconditional
safe baseline -- no maturity dependency, robust under early-dev
ATA volatility.}
\item{\code{"cl"}}{Chain ladder for all development periods.
Equivalent to the classical Mack (1993) recursion.}
\item{\code{"sa"}}{Stage-adaptive composition: ED before maturity, CL
after maturity. Requires maturity detection (2-pass), uses age-to-
age factors only once they have stabilised.
\itemize{
\item Before maturity: age-to-age factors are volatile, so
exposure-driven projection
\eqn{\Delta C^L = g_k \cdot C^P_k} anchors the estimate
to exposure volume.
\item After maturity: age-to-age factors are stable, so
chain ladder projection
\eqn{C^L_{k+1} = f_k \cdot C^L_k} preserves the cohort's
observed level.
}}
\item{\code{"bf"}}{Bornhuetter-Ferguson: blends the chain-ladder
ultimate with an external prior loss ratio by the emergence
fraction. Requires a \code{prior} argument, passed through \code{...}.}
\item{\code{"cc"}}{Cape Cod: like \code{"bf"}, but the prior loss ratio is
estimated by pooling the data rather than supplied externally.}
}

In all cases, exposure is projected forward to ultimate; the point
projection is identical under \code{exposure_method = "ed"} or \code{"cl"}
(they differ only in the variance recursion).

This function is the \emph{composition} layer over \code{\link[=fit_loss]{fit_loss()}} and
\code{\link[=fit_exposure]{fit_exposure()}}: it delegates loss projection to \code{fit_loss()},
retrieves the embedded \code{ExposureFit}, and composes the loss-ratio
point + variance via the delta method (\code{se_method = "fixed"} or
\code{"delta"}). See \code{ARCHITECTURE.md} for the layered design.
}
\examples{
\dontrun{
data(experience)
tri <- as_triangle(
  experience[coverage == "surgery"],
  groups   = "coverage",
  cohort   = "uy_m",
  calendar = "cy_m",
  loss     = "incr_loss",
  exposure = "incr_exposure"
)

# Stage-adaptive (default): ED before maturity, CL after
ratio_sa <- fit_ratio(tri, method = "sa")
summary(ratio_sa)
plot(ratio_sa)

# Pure exposure-driven for all development periods
ratio_ed <- fit_ratio(tri, method = "ed")

# Pure chain ladder (Mack-style) for all development periods
ratio_cl <- fit_ratio(tri, method = "cl")
}

}
\seealso{
\code{\link[=fit_loss]{fit_loss()}}, \code{\link[=fit_exposure]{fit_exposure()}}, \code{\link[=as_triangle]{as_triangle()}},
\code{\link[=as_link]{as_link()}}, \code{\link[=fit_ata]{fit_ata()}}, \code{\link[=fit_ed]{fit_ed()}}, \code{\link[=detect_maturity]{detect_maturity()}}
}
