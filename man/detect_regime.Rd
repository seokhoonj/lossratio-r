% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regime.R
\name{detect_regime}
\alias{detect_regime}
\alias{print.Regime}
\alias{summary.Regime}
\alias{print.summary.Regime}
\title{Detect structural regime shifts across underwriting cohorts}
\usage{
detect_regime(
  x,
  target = "lr",
  by = NULL,
  window = "auto",
  method = c("e_divisive", "pelt", "hclust"),
  n_regimes = NULL,
  sig_level = 0.05,
  min_size = 3L,
  treatment = c("latest_only", "segment_wise"),
  ...
)

\method{print}{Regime}(x, ...)

\method{summary}{Regime}(object, ...)

\method{print}{summary.Regime}(x, ...)
}
\arguments{
\item{x}{An object of class \code{"Triangle"}. May contain one or more
groups (per-group detection runs independently).
Also used by S3 \code{print()} method on \code{Regime} objects.}

\item{target}{Trajectory variable. Default is \code{"lr"} (cumulative loss
ratio). Accepts any column on the \code{Triangle} (e.g. \code{"lr"},
\code{"loss"}, \code{"premium"}, \code{"loss_incr"}, \code{"premium_incr"}), plus three
\emph{diagnostic} derived targets computed inline per (group, cohort):
\describe{
\item{\code{"loss_ata"}}{Loss age-to-age factor
\code{loss[k+1] / loss[k]} — multiplicative loss development speed
(CL $f_k$).}
\item{\code{"premium_ata"}}{Premium age-to-age factor — same form on
premium.}
\item{\code{"loss_ed"}}{Loss intensity
\code{(loss[k] - loss[k-1]) / premium[k-1]} — additive,
exposure-anchored (ED model's $g_k$).}
\item{\code{"premium_ed"}}{Alias of \code{"premium_ata"} — the two differ
only by a constant \code{(premium_ata - 1)}, and the PCA
standardization in detection removes that shift, so they yield
identical regime changes. Provided for API symmetry with the
\code{loss_ata} / \code{loss_ed} pair.}
}
Derived targets drop the first dev row per cohort (no predecessor),
then re-index \code{dev} so detection sees a contiguous sequence. See the
\code{vignette("regime")} "Choice of target" section for guidance on
which target matches which suspected event.}

\item{by}{Grouping column(s) for per-combination detection. \code{NULL}
(default) reuses the Triangle's \code{attr(x, "groups")} when non-empty —
so \code{detect_regime(tri)} dispatches per group automatically — and
otherwise falls back to pooled detection. Pass \code{by = character(0)}
to force pooled detection on a multi-group Triangle, or a character
vector (subset of \code{names(x)}) to dispatch on an explicit combo,
e.g. \code{by = "coverage"} or \code{by = c("channel", "coverage")}.}

\item{window}{Trajectory window. Integer (e.g., \code{12L}) for a fixed window, or
the string \code{"auto"} (default) — resolves to each group's maturity
via \code{\link[=detect_maturity]{detect_maturity()}}, falling back to \code{6L} when maturity is
unavailable (NA, pooled mode, or \code{by} mismatching the Triangle's
\code{attr("groups")}). Cohorts with fewer than the resolved \code{window}
observed periods are dropped.}

\item{method}{One of \code{"e_divisive"}, \code{"pelt"}, \code{"hclust"}.}

\item{n_regimes}{Integer. Number of regimes to force. \code{NULL} means
auto-detect for \code{"e_divisive"} and \code{"pelt"}; ignored (required to equal
the requested value) for \code{"hclust"}, where the default is \code{2}.}

\item{sig_level}{Significance level for \code{"e_divisive"}. Default \code{0.05}.}

\item{min_size}{Minimum segment size for \code{"e_divisive"}. Default \code{3}.}

\item{treatment}{How downstream fits should apply this Regime when
\verb{$changes} contains multiple change points. One of:
\describe{
\item{\code{"latest_only"}}{(default) Collapse to the most recent change
date and drop all pre-latest-change cohorts. Single pooled factor
estimate over the surviving (post-latest-change) cohorts.}
\item{\code{"segment_wise"}}{Preserve all change points. Each segment
(consecutive cohorts between adjacent changes) gets its own factor
estimate, and each cohort is projected using its own segment's
factor. Recommended for multi-regime + long-tail data where opt
\code{"latest_only"} would lose self-regime responsiveness on older
cohorts.}
}}

\item{...}{Reserved for future use.}

\item{object}{An object of class \code{"Regime"}. Used by the S3
\code{summary()} method.}
}
\value{
An object of class \code{"Regime"}. For single-group input:
\describe{
\item{\code{call}}{Matched call.}
\item{\code{method}}{Detection method used.}
\item{\code{target}}{Trajectory variable used for detection.}
\item{\code{window}}{Trajectory window per combo. Scalar integer when a
single combo was analysed; integer vector (one per surviving
combo, in the order of \verb{$labels} / \verb{$changes} group rows)
otherwise.}
\item{\code{window_mode}}{Either \code{"auto"} (resolved per group via
\code{\link[=detect_maturity]{detect_maturity()}}) or \code{"manual"} (user-supplied integer).}
\item{\code{cohort}}{Period variable from \code{x}.}
\item{\code{labels}}{\code{data.table} of one row per analysed cohort:
\verb{[by..., cohort, regime, regime_id]}. Group columns are prepended
when \code{by} resolves to a non-empty vector.}
\item{\code{changes}}{\code{data.table} of detected regime changes with
columns \verb{[by..., change, regime_id, pre_value, post_value, magnitude]}. \code{regime_id} = id of the regime that STARTS at this
change (the pre-change regime is \code{regime_id - 1}); matches
\verb{$labels$regime_id}. \code{pre_value} / \code{post_value} are the mean
\code{target} over the cohort × dev trajectory windows in the pre- /
post-change regimes; \verb{magnitude = |post_value - pre_value|}.
Empty (zero rows) when no change is detected.}
\item{\code{n_regimes}}{Number of regimes detected. Scalar integer for
single-combo detection; named integer vector (keyed by combo) for
multi-combo.}
\item{\code{trajectory}}{Cohort × dev feature matrix used for detection.
Single matrix when single combo; named list of matrices for
multi-combo.}
\item{\code{pca}}{\code{prcomp} object (single combo) or named list of
\code{prcomp} objects (multi-combo).}
\item{\code{dropped}}{Cohorts excluded due to the \code{window} window
constraint. Vector (single) / named list (multi).}
\item{\code{multi_group}}{Logical flag; \code{TRUE} when detection ran over
multiple group combos.}
\item{\code{treatment}}{Either \code{"latest_only"} or \code{"segment_wise"} — the
value supplied via the \code{treatment} argument. Read by downstream
fits (\code{fit_ata()}, \code{fit_intensity()}, \code{fit_cl()}, \code{fit_ed()}) to
decide whether to collapse to the latest change (drop pre-change
cohorts, single pooled factor) or estimate per-segment factors.}
}
}
\description{
Detect structural change points in the sequence of cohort-level
development trajectories. Each underwriting cohort (indexed by the
\code{cohort} of a \code{"Triangle"} object) is treated as a feature vector
whose entries are the selected \code{target} metric observed at development
periods \verb{1, ..., window}. Cohorts are then ordered by underwriting
period and tested for structural shifts in the multivariate
sequence.

Multi-group \code{Triangle} inputs are supported: detection runs
independently per group, and results are combined into a single
\code{Regime} object whose \verb{$changes}, \verb{$labels}, etc. carry the
group column. Single-group input retains the original scalar /
Date-vector / matrix layout for backward compatibility.

Three detection strategies are supported:
\describe{
\item{\code{"e_divisive"}}{Multivariate non-parametric divisive change-point
detection via \code{\link[ecp:e.divisive]{ecp::e.divisive()}}. The number of regimes is
determined by the data; only significant changes at
\code{sig_level} are retained. Preferred when the number of regimes
is not known in advance.}
\item{\code{"pelt"}}{Univariate mean change-point detection via
\code{\link[changepoint:cpt.mean]{changepoint::cpt.mean()}} with the PELT algorithm applied to the
first principal component of the cohort feature matrix. Fast
and may return multiple changes.}
\item{\code{"hclust"}}{Ward hierarchical clustering on the scaled
cohort feature matrix, cut to \code{n_regimes} clusters. Ignores
time ordering -- useful as a sanity check since non-adjacent
cohorts may cluster together if the trajectory pattern is not
strictly chronological.}
}
}
\examples{
\dontrun{
data(experience)
tri_sur <- build_triangle(
  experience[coverage == "SUR"],
  groups   = "coverage",
  cohort   = "uy_m",
  calendar = "cy_m",
  loss     = "loss_incr",
  premium  = "premium_incr"
)

# Hierarchical clustering (no extra package dependency)
r <- detect_regime(tri_sur, method = "hclust",
                          n_regimes = 2L)
print(r)
summary(r)
plot(r)

# ecp divisive change-point detection (requires the ecp package)
r_ecp <- detect_regime(tri_sur, method = "e_divisive")

# Multi-group: detection per coverage
tri_all <- build_triangle(
  experience,
  groups   = "coverage",
  cohort   = "uy_m",
  calendar = "cy_m",
  loss     = "loss_incr",
  premium  = "premium_incr"
)
r_all <- detect_regime(tri_all, by = "coverage", method = "e_divisive")
print(r_all$changes)
}

}
\seealso{
\code{\link[=plot.Regime]{plot.Regime()}}, \code{\link[=build_triangle]{build_triangle()}}
}
