% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regime.R
\name{.resolve_maturity}
\alias{.resolve_maturity}
\title{Resolve a maturity input to a Maturity object (or NULL)}
\usage{
.resolve_maturity(arg, tri, masked_tri = NULL)
}
\arguments{
\item{arg}{The maturity input (NULL / Maturity / \code{"auto"} /
function).}

\item{tri}{A \code{"Triangle"} object — used as the detection input when
\code{masked_tri} is \code{NULL}.}

\item{masked_tri}{Optional masked \code{"Triangle"} (e.g. backtest's
training-only triangle). When supplied, \code{"auto"} and function
inputs operate on this triangle instead of \code{tri}.}
}
\value{
\code{NULL} or a \code{"Maturity"} object.
}
\description{
Internal 4-type dispatcher used by \code{fit_ratio()}, \code{fit_loss()}, and
\code{\link[=backtest]{backtest()}} to normalize the \code{maturity} input into a single
representation: either \code{NULL} (no maturity override) or a
\code{"Maturity"} object.

The four accepted input types are:
\describe{
\item{\code{NULL}}{Returns \code{NULL} — caller falls back to its default
maturity behavior.}
\item{\code{"Maturity"} object}{Returned as-is.}
\item{\code{"auto"}}{Runs \code{\link[=detect_maturity]{detect_maturity()}} on \code{masked_tri} if
supplied, otherwise on \code{tri}. The \code{masked_tri} fallback is the
leakage-safe path used by \code{\link[=backtest]{backtest()}} — fit functions pass
only \code{tri}, while \code{\link[=backtest]{backtest()}} passes both so detection sees
only the masked (training) data.}
\item{\verb{function(tri) -> Maturity}}{Closure invoked with
\code{masked_tri} (if non-NULL) or \code{tri}. Its return value must
inherit \code{"Maturity"}; an error is raised otherwise.}
}
}
\keyword{internal}
