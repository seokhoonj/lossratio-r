% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maturity.R
\name{detect_maturity}
\alias{detect_maturity}
\title{Find ata maturity by group}
\usage{
detect_maturity(
  x,
  loss_var = "loss",
  weight_var = NULL,
  alpha = 1,
  max_cv = 0.15,
  max_rse = 0.05,
  min_valid_ratio = 0.5,
  min_n_valid = 3L,
  min_run = 2L
)
}
\arguments{
\item{x}{A \code{Triangle} object.}

\item{loss_var}{Cumulative metric for the link factor. Default
\code{"loss"} (chain-ladder convention; see Description). Forwarded to
\code{\link[=build_link]{build_link()}}.}

\item{weight_var}{Optional WLS weight variable. Forwarded to
\code{\link[=build_link]{build_link()}}.}

\item{alpha}{Numeric scalar controlling the variance structure in
the underlying WLS fit. Default \code{1}. Forwarded to \code{\link[=summary.Link]{summary.Link()}}.}

\item{max_cv}{Maximum allowed coefficient of variation.
Default is \code{0.15}.}

\item{max_rse}{Maximum allowed relative standard error.
Default is \code{0.05}.}

\item{min_valid_ratio}{Minimum proportion of finite ata values required.
Default is \code{0.5}.}

\item{min_n_valid}{Minimum number of finite ata factors required.
Default is \code{3L}.}

\item{min_run}{Minimum number of consecutive ata links satisfying the
maturity criteria. Default is \code{2L}.}
}
\value{
A \code{data.table} with class \code{"Maturity"} containing one row
per group. If no mature link is found, all values for that group are
\code{NA}.
}
\description{
Identify the first mature age-to-age (ata) link from a \code{Triangle}.
Internally builds a single-variable \code{Link} table, computes the
per-link diagnostic via \code{\link[=summary.Link]{summary.Link()}} with \code{model = "ata"}, and
then locates the first link whose statistics satisfy all maturity
criteria.

Maturity is determined using a combination of:
\itemize{
\item \code{cv < max_cv}
\item \code{rse < max_rse}
\item \code{valid_ratio >= min_valid_ratio}
\item \code{n_valid >= min_n_valid}
\item optional consecutive maturity over \code{min_run} ata links
}

Both \code{cv} and \code{rse} must be satisfied simultaneously. \code{cv} captures
the raw variability of observed ata factors across cohorts, while \code{rse}
reflects the precision of the WLS-estimated factor. Using both criteria
together provides a more robust maturity assessment than either alone.

Default \code{loss_var = "loss"} (cumulative loss). Maturity in chain
ladder is methodologically a property of \emph{loss} development:
the ATA factors of cumulative loss stabilize when chain ladder
becomes reliable, which in turn makes downstream LR projection
reliable. ATA factors of \code{lr} itself (a ratio of two cumulative
quantities) carry additional noise and tend to give less precise
maturity decisions. Override \code{loss_var} only when you specifically
want maturity of premium development or another cumulative metric.
}
