% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/capecod.R
\name{fit_capecod}
\alias{fit_capecod}
\title{Cape Cod projection (Stanard 1985)}
\usage{
fit_capecod(x, loss = "loss", exposure = "exposure", ...)
}
\arguments{
\item{x}{A \code{Triangle} object.}

\item{loss}{A single cumulative loss variable. Default \code{"loss"}.}

\item{exposure}{A single cumulative exposure variable. Default
\code{"exposure"}.}

\item{...}{Reserved for future extension (currently unused).}
}
\value{
An object of class \code{"CapeCodFit"} containing:
\describe{
\item{\code{call}}{The matched call.}
\item{\code{data}}{The input \code{Triangle}.}
\item{\code{method}}{\code{"capecod"}.}
\item{\code{groups}, \code{cohort}, \code{dev}, \code{loss}, \code{exposure}}{Metadata.}
\item{\code{full}, \code{proj}, \code{summary}}{Same shape as \code{BFFit}.}
\item{\code{elr_cc}}{\code{data.table(group..., elr_cc)} -- the pooled ELR
per group (or scalar if no group).}
\item{\code{q}}{Per-cohort emerged fraction.}
\item{\code{cl_fit}, \code{exposure_fit}}{Inner CL / Exposure fits.}
}
}
\description{
Fit a Cape Cod projection from a \code{"Triangle"} object. Cape Cod is
the \emph{prior-free} Bornhuetter-Ferguson variant introduced by Stanard
(1985): the a priori expected loss ratio is \emph{estimated from the
data itself} as a portfolio-pooled quantity, then plugged into the
BF formula.

\deqn{\widehat{\mathrm{ELR}}^{CC} =
  \frac{\sum_i L_{obs, i}}{\sum_i E_i^{ult} \cdot q_i}}

where
\itemize{
\item \eqn{L_{obs, i}}: cohort \eqn{i}'s observed cumulative loss
at its latest observed development period.
\item \eqn{q_i = L_{obs, i} / \hat L_{ult, i}^{CL}}: the expected
emerged fraction (inverse of cumulative LDF).
\item \eqn{E_i^{ult}}: cohort \eqn{i}'s ultimate exposure
(projected via chain ladder on exposure).
}

Given \eqn{\widehat{\mathrm{ELR}}^{CC}}, the per-cohort ultimate is
obtained from the BF formula with this single pooled ELR:

\deqn{\hat L_{ult, i}^{CC} = L_{obs, i} +
  (1 - q_i) \cdot \widehat{\mathrm{ELR}}^{CC} \cdot E_i^{ult}}

When multiple groups are present, \eqn{\widehat{\mathrm{ELR}}^{CC}}
is computed \emph{within group} (not pooled across groups) so each
group retains its own portfolio-level ELR estimate.

This is a \emph{standalone} worker -- it does not currently integrate
with \code{\link[=fit_loss]{fit_loss()}} / \code{\link[=fit_ratio]{fit_ratio()}}. Point projection only.
}
\examples{
\dontrun{
data(experience)
tri <- as_triangle(
  experience[coverage == "surgery"],
  groups   = "coverage",
  cohort   = "uy_m",
  calendar = "cy_m",
  loss     = "incr_loss",
  exposure = "incr_exposure"
)
cc <- fit_capecod(tri)
summary(cc)
cc$elr_cc   # pooled ELR per group
}

}
\references{
Stanard, J. N. (1985). A simulation test of prediction errors of
loss reserve estimation techniques. \emph{Proceedings of the Casualty
Actuarial Society}, 72, 124-148.

Bornhuetter, R. L. and Ferguson, R. E. (1972). The actuary and IBNR.
\emph{Proceedings of the Casualty Actuarial Society}, 59, 181-195.
}
\seealso{
\code{\link[=fit_bf]{fit_bf()}} (Bornhuetter-Ferguson with user-supplied prior),
\code{\link[=fit_cl]{fit_cl()}}, \code{\link[=fit_exposure]{fit_exposure()}}
}
