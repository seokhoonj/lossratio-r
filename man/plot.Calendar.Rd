% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/triangle-vis.R
\name{plot.Calendar}
\alias{plot.Calendar}
\title{Plot calendar-based development statistics}
\usage{
\method{plot}{Calendar}(
  x,
  metric = "ratio",
  amount_divisor = "auto",
  show_label = FALSE,
  label_size = 2.8,
  theme = c("view", "save", "shiny"),
  ...
)
}
\arguments{
\item{x}{An object of class \code{Calendar}.}

\item{metric}{A single metric to plot. Must be one of:
\code{"ratio"}, \code{"incr_ratio"},
\code{"loss"}, \code{"incr_loss"}, \code{"premium"}, \code{"incr_premium"}, \code{"margin"}, \code{"incr_margin"},
\code{"loss_share"}, \code{"incr_loss_share"}, \code{"premium_share"}, or \code{"incr_premium_share"}.}

\item{amount_divisor}{Numeric scaling factor used only for y-axis labels of
amount variables. Default \code{"auto"} (picks the divisor that produces
the shortest formatted label; pass an explicit numeric to fix it).}

\item{show_label}{Logical; if \code{TRUE}, overlay the metric value as a
text label at each (calendar, group) point. Ratio metrics
(\code{"ratio"}, \code{"incr_ratio"}, share variants) are formatted as percent
(one decimal). Amount metrics are scaled by \code{amount_divisor} and
formatted with one decimal. Default \code{FALSE}.}

\item{label_size}{Numeric text size passed to \code{geom_text} when
\code{show_label = TRUE}. Default \code{2.8}.}

\item{theme}{A string passed to \code{\link[=.switch_theme]{.switch_theme()}}
(\code{"view"}, \code{"save"}, \code{"shiny"}).}

\item{...}{Additional arguments passed to \code{\link[=.switch_theme]{.switch_theme()}}.}
}
\value{
A \code{ggplot} object.
}
\description{
Visualise an object of class \code{Calendar} as a time-series plot.
The selected metric is plotted over the calendar-style \code{calendar},
or over the calendar development variable stored in \code{attr(x, "dev")}.

Ratio metrics (\code{ratio}, \code{incr_ratio}) and proportion metrics
(\code{loss_share}, \code{incr_loss_share}, \code{premium_share}, \code{incr_premium_share}) are
plotted on the original scale and displayed as percentages via y-axis labels.
Amount metrics (\code{loss}, \code{incr_loss}, \code{premium}, \code{incr_premium}, \code{margin},
\code{incr_margin}) are plotted on the original scale and displayed using y-axis
labels scaled by \code{amount_divisor}.

If grouping variables are present, lines are drawn separately by group.
}
\details{
The x-axis is the calendar variable stored in \code{attr(x, "calendar")}
(a Date, formatted per the triangle's \code{grain}).

The loss ratio is defined as:
\deqn{ratio = loss / premium}

where \code{premium} denotes risk premium rather than written premium.
}
\examples{
\dontrun{
x <- as_calendar(
  df,
  groups   = "coverage",
  calendar = "cy_m",
  loss     = "incr_loss",
  premium = "incr_premium"
)

plot(x)
plot(x, metric = "ratio")
}

}
