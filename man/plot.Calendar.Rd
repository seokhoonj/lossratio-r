% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/triangle-vis.R
\name{plot.Calendar}
\alias{plot.Calendar}
\title{Plot calendar-based development statistics}
\usage{
\method{plot}{Calendar}(
  x,
  value_var = "lr",
  x_by = c("period", "dev"),
  amount_divisor = 1e+08,
  theme = c("view", "save", "shiny"),
  ...
)
}
\arguments{
\item{x}{An object of class \code{Calendar}.}

\item{value_var}{A single metric to plot. Must be one of:
\code{"lr"}, \code{"lr"},
\code{"loss"}, \code{"rp"}, \code{"margin"}, \code{"loss"}, \code{"premium"}, \code{"margin"},
\code{"loss_prop"}, \code{"rp_prop"}, \code{"loss_prop"}, or \code{"premium_prop"}.}

\item{x_by}{X-axis basis. One of:
\describe{
\item{"period"}{Use the calendar variable stored in \code{attr(x, "calendar_var")}.}
\item{"dev"}{Use the sequential \code{dev} column.}
}}

\item{amount_divisor}{Numeric scaling factor used only for y-axis labels of
amount variables. Default is \code{1e8}.}

\item{theme}{A string passed to \code{\link[=.switch_theme]{.switch_theme()}}
(\code{"view"}, \code{"save"}, \code{"shiny"}).}

\item{...}{Additional arguments passed to \code{\link[=.switch_theme]{.switch_theme()}}.}
}
\value{
A \code{ggplot} object.
}
\description{
Visualise an object of class \code{Calendar} as a time-series plot.
The selected metric is plotted over the calendar-style \code{calendar_var},
or over the calendar development variable stored in \code{attr(x, "dev_var")}.

Ratio metrics (\code{lr}, \code{lr}) and proportion metrics
(\code{loss_prop}, \code{rp_prop}, \code{loss_prop}, \code{premium_prop}) are plotted on the
original scale and displayed as percentages via y-axis labels.
Amount metrics (\code{loss}, \code{rp}, \code{margin}, \code{loss}, \code{premium}, \code{margin}) are
plotted on the original scale and displayed using y-axis labels scaled by
\code{amount_divisor}.

If grouping variables are present, lines are drawn separately by group.
}
\details{
The x-axis uses either the calendar variable stored in \code{attr(x, "calendar_var")}
or the sequential \code{dev} column, depending on \code{x_by}.

The loss ratio is defined as:
\deqn{lr = loss / rp}

where \code{rp} denotes risk premium rather than written premium.
}
\examples{
\dontrun{
x <- build_calendar(df, cv_nm, cym)

plot(x)
plot(x, value_var = "lr")
plot(x, x_by = "dev")
}

}
