% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{.boot_add_process_noise}
\alias{.boot_add_process_noise}
\title{Add Stage 2 process noise to cell means (method-independent)}
\usage{
.boot_add_process_noise(refit_dt, process_dist = c("normal", "gamma", "odp"))
}
\arguments{
\item{refit_dt}{A \code{data.table} from \code{.boot_refit_cl()} /
\code{.boot_refit_ed()} / \code{.boot_refit_sa()} with columns \code{cell_mean} and
\code{cell_proc_var}.}

\item{process_dist}{One of \code{"normal"}, \code{"gamma"}, \code{"odp"}. Typically
passed via \code{boots$meta$process}.}
}
\value{
The input \code{data.table} with an added \code{cell_real} column.
}
\description{
Draws per-cell noise from the configured process distribution. Pure
\verb{(mean, variance, distribution)} -> realized value. The method-specific
variance structure has already been baked into \code{cell_proc_var} by the
refit step.
}
\details{
Cells with \code{cell_proc_var <= 0} (observed region) pass through to
\code{cell_real = cell_mean} unchanged. Negative realized values are clipped
to 0 (cumulative loss is non-negative).
}
\keyword{internal}
