% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regime.R
\name{.e_divisive}
\alias{.e_divisive}
\title{E-Divisive change-point detection (Matteson & James 2014)}
\usage{
.e_divisive(X, k = NULL, sig_level = 0.05, min_size = 3L, R = 199L, alpha = 1)
}
\arguments{
\item{X}{Numeric matrix of observations; rows are ordered
observations.}

\item{k}{Optional integer count of change points to force. \code{NULL}
(default) lets the permutation test decide how many to keep.}

\item{sig_level}{Permutation-test significance threshold; used only
when \code{k} is \code{NULL}.}

\item{min_size}{Minimum size of either side of any split.}

\item{R}{Number of permutations per significance test.}

\item{alpha}{Distance exponent; \code{1} gives plain Euclidean distance.}
}
\value{
A list: \code{breakpoints} (sorted regime-start row indices) and
\code{p_values} (permutation p-value per breakpoint; \code{NA} for forced
breaks).
}
\description{
Nonparametric multivariate change-point detection via the energy
statistic. A greedy divisive procedure: within every open segment
find the split that maximises the energy distance, accept the
globally largest candidate when a permutation test clears
\code{sig_level} (or, when \code{k} is supplied, accept the \code{k} largest
unconditionally), and recurse on the two new segments.
}
\details{
Written from the paper: Matteson, D. S. & James, N. A. (2014),
"A nonparametric approach for multiple change point analysis of
multivariate data", JASA 109(505), 334-345. The greedy recursion
and its per-break permutation test run in the native kernel
\code{C_e_divisive} (\code{src/e_divisive.c}) -- this is a thin wrapper.
Permutations draw from R's RNG, so \code{set.seed()} controls
reproducibility.
}
\keyword{internal}
