% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ata.R
\name{.summarize_link_ata}
\alias{.summarize_link_ata}
\title{Internal: summarise age-to-age factor statistics from a \code{Link} table}
\usage{
.summarize_link_ata(object, alpha = 1, digits = 3, ...)
}
\arguments{
\item{object}{An object of class \code{"Link"}, typically produced by
\code{\link[=build_link]{build_link()}}.}

\item{alpha}{Numeric scalar controlling the variance structure in the
WLS fit. Default is \code{1}.}

\item{digits}{Number of decimal places to round numeric columns.
Default is \code{3}. Pass \code{NULL} to skip rounding.}

\item{...}{Additional arguments passed to the internal WLS estimation.}
}
\value{
A \code{data.table} with class \code{"ATASummary"} containing one row
per ata link:
\describe{
\item{\code{ata_from}, \code{ata_to}, \code{ata_link}}{Link identifiers.}
\item{\code{mean}}{Arithmetic mean of observed ata factors.}
\item{\code{median}}{Median of observed ata factors.}
\item{\code{wt}}{Volume-weighted mean:
\eqn{\sum C_{i,k+1} / \sum C_{i,k}}, independent of \code{alpha}.}
\item{\code{cv}}{Coefficient of variation of observed ata factors
(\eqn{SD / mean}). Used by \code{\link[=detect_maturity]{detect_maturity()}} to assess
stability.}
\item{\code{f}}{WLS-estimated factor. Equals \code{wt} when \code{alpha = 2}
and no zero \code{loss_from} rows are present.}
\item{\code{f_se}}{Standard error of the WLS-estimated factor.}
\item{\code{rse}}{Relative standard error of the WLS-estimated factor
(\eqn{f\_se / f}).}
\item{\code{sigma}}{Mack sigma (residual standard deviation from the
WLS fit). Used in Mack variance estimation.}
\item{\code{n_obs}}{Total number of observations for the link.}
\item{\code{n_valid}}{Number of finite ata values.}
\item{\code{n_inf}}{Number of infinite ata values.}
\item{\code{n_nan}}{Number of NaN ata values.}
\item{\code{valid_ratio}}{Proportion of finite ata values
(\eqn{n\_valid / n\_obs}).}
}
}
\description{
Compute group-wise summary statistics for age-to-age factors from an
object of class \code{"Link"}. This helper backs the \code{summary.Link()}
dispatcher when \code{model = "ata"}. It serves two purposes:

\enumerate{
\item \strong{Diagnostics}: provides descriptive statistics
(\code{mean}, \code{median}, \code{wt}, \code{cv}) that help the user assess the
stability and consistency of observed ata factors across cohorts.
\item \strong{Estimation}: fits a no-intercept weighted least squares
model per ata link to produce the WLS-estimated factor (\code{f}), its
standard error (\code{f_se}), relative standard error (\code{rse}), and Mack
sigma (\code{sigma}). These are used downstream by
\code{\link[=detect_maturity]{detect_maturity()}} and \code{\link[=fit_ata]{fit_ata()}}.
}
}
\section{Relationship between \code{wt} and \code{f}}{

Both \code{wt} and \code{f} are weighted averages of the observed ata factors,
but they differ in how weights are assigned and which observations
are included:

\describe{
\item{\code{wt}}{Volume-weighted mean:
\eqn{wt = \sum C_{i,k+1} / \sum C_{i,k}}.
Computed from all rows where \code{loss_from} and \code{loss_to} are
finite, including rows where either value is zero.
Independent of \code{alpha}.}
\item{\code{f}}{WLS-estimated factor. Only rows where \code{loss_from > 0}
are used, since \code{loss_from = 0} causes numerical issues in the
WLS weights (\eqn{w = value\_from^{\alpha}}). When \code{alpha = 2},
\code{f} and \code{wt} are numerically equivalent (assuming no zero
\code{loss_from} rows). When \verb{alpha \\ne 2}, they diverge.}
}

Therefore \code{wt} and \code{f} can differ for two reasons:
\enumerate{
\item \strong{Zero exclusion}: rows with \code{loss_from = 0} are
included in \code{wt} but excluded from \code{f}. This typically affects
early development periods where some cohorts have not yet accumulated
any claims.
\item \strong{Alpha effect}: when \verb{alpha \\ne 2}, the WLS weights
differ from the volume weights used in \code{wt}, leading to different
estimates. Comparing \code{wt} and \code{f} can help diagnose whether the
choice of \code{alpha} materially affects the estimated factor.
}
}

\section{Weights}{

When the input \code{"Link"} object contains a \code{weight} column (added by
\code{\link[=build_link]{build_link()}} when \code{weight_var} is supplied), that column is
automatically used as the WLS weight in place of \code{loss_from}. This
is useful when \code{loss_var = "lr"}, where \code{loss_from} carries no
exposure information and an external exposure variable such as \code{premium}
should be used instead.
}

\section{Coefficient of variation (\code{cv})}{

The coefficient of variation is defined as:
\deqn{cv = \frac{SD(f_k)}{\bar{f}_k}}
where \eqn{f_k} are the individual observed ata values for link
\eqn{k} and \eqn{\bar{f}_k} is their arithmetic mean. The \code{cv}
reflects the relative spread of observed factors across cohorts,
regardless of the exposure scale. It is used by
\code{\link[=detect_maturity]{detect_maturity()}} as one of the criteria for determining the
maturity point.
}

\section{Relative standard error (\code{rse})}{

The relative standard error is defined as:
\deqn{rse = \frac{SE(\hat{f}_k)}{\hat{f}_k}}
where \eqn{SE(\hat{f}_k)} is the standard error of the
WLS-estimated factor. Unlike \code{cv}, which treats all cohorts equally,
\code{rse} gives more weight to cohorts with larger exposures (via the
WLS weights). A small \code{rse} indicates that the WLS estimate is
precise, which tends to occur when: (1) there are many cohorts,
(2) exposures are large, and (3) the observed ata values are
consistent across cohorts.
}

\seealso{
\code{\link[=build_link]{build_link()}}, \code{\link[=summary.Link]{summary.Link()}}, \code{\link[=detect_maturity]{detect_maturity()}},
\code{\link[=fit_ata]{fit_ata()}}
}
\keyword{internal}
