% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/premium.R
\name{.build_internal_premium_fit}
\alias{.build_internal_premium_fit}
\title{Build the internal premium-side \code{PremiumFit} for a within-role composer}
\usage{
.build_internal_premium_fit(
  x,
  alpha,
  sigma_method,
  recent = NULL,
  regime = NULL,
  groups = NULL,
  conf_level = 0.95
)
}
\arguments{
\item{x}{A \code{Triangle}.}

\item{alpha, sigma_method, recent, regime}{Forwarded to \code{\link[=fit_cl]{fit_cl()}}.}

\item{groups}{Group columns, for the incremental / CI derivation in
\code{\link[=.premium_rename_full]{.premium_rename_full()}}.}

\item{conf_level}{Confidence level for the analytical CI columns.}
}
\value{
A \code{CLFit} with \code{PremiumFit} prepended to its class and an
\verb{premium_*}-schema \verb{$full}.
}
\description{
Within-role composers (\code{fit_bf()}, \code{fit_cc()}) need an ultimate
premium projection. They obtain it by calling the worker \code{fit_cl()}
directly on the standardized \code{premium} column -- a downward
worker-layer dispatch, avoiding the upward dependency on the
\code{fit_premium()} dispatcher -- then translating the worker's \verb{loss_*}
schema to \verb{premium_*} and tagging the result as an \code{PremiumFit}.
This helper packages that shared three-step block.
}
\keyword{internal}
