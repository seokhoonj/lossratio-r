% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{.boot_refit}
\alias{.boot_refit}
\title{Refit chain-ladder per bootstrap replicate (unified entry point)}
\usage{
.boot_refit(
  triangle,
  boots,
  method = c("cl", "ed", "sa"),
  alpha = 1,
  maturity = NULL
)
}
\arguments{
\item{triangle}{The original \code{Triangle} (carries the observed region
mask).}

\item{boots}{A \code{BootstrapTriangle} from \code{bootstrap()}.}

\item{method}{One of \code{"cl"}, \code{"ed"}, \code{"sa"}.}

\item{alpha}{Mack variance exponent (currently \code{1} only).}

\item{maturity}{Required when \code{method = "sa"}. A resolved \code{Maturity}
object (use \code{.resolve_maturity()} upstream).}
}
\value{
A \code{data.table} keyed by \verb{[groups..], cohort, dev, rep} with
columns \code{cell_mean}, \code{cell_proc_var}.
}
\description{
For each replicate \code{b} in \code{boots$alt_triangles}, refit factors and
compute the per-cell mean projection and process variance. Method
dispatch (\code{"cl"} / \code{"ed"} / \code{"sa"}) determines the recursion and the
variance structure. \code{cell_proc_var} carries the method-specific
variance baked in so the downstream Stage 2 helper can stay
method-blind.
}
\details{
Recursions:
\itemize{
\item \code{"cl"} — Mack multiplicative: \verb{C_\{k+1\} = f_k * C_k},
\verb{proc_var = sigma^2_f_k * C_k}.
\item \code{"ed"} — additive exposure-driven: \verb{C_\{k+1\} = C_k + g_k * P_k},
\verb{proc_var = sigma^2_g_k * P_k}. Uses the triangle's \code{prem} column
as the exposure anchor.
\item \code{"sa"} — stage-adaptive hybrid: ED for \code{dev < maturity$change},
CL from \code{maturity$change} onward.
}

The CL path reads the target column from \code{boots$meta$target} (\code{"loss"}
by default, but \code{fit_premium()} passes \code{"prem"}). ED and SA paths are
loss-specific (they need an external exposure column) and only support
\code{target = "loss"}.
}
\keyword{internal}
