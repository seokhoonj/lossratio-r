% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regime.R
\name{.derive_regime_target}
\alias{.derive_regime_target}
\title{Derive a non-native regime detection metric}
\usage{
.derive_regime_target(d, loss, groups = character(0))
}
\description{
Computes diagnostic / experimental detection metrics that are not stored
directly on the Triangle:
\describe{
\item{\code{loss_ata}}{Loss age-to-age factor -- \code{loss[k+1] / loss[k]} per
(group, cohort). Captures \emph{multiplicative} development speed.}
\item{\code{premium_ata}}{Premium age-to-age factor -- same form on premium.
Captures premium \emph{recognition speed}.}
\item{\code{loss_ed}}{Loss intensity (ED model's $g_k$) --
\code{(loss[k] - loss[k-1]) / premium[k-1]} per (group, cohort).
\emph{Additive}, exposure-anchored.}
}

The first dev row per cohort is NA (no predecessor). Downstream
\code{.detect_regime_single} handles NA-tolerant aggregation.
}
\keyword{internal}
