% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lr.R
\name{.bootstrap_cohort}
\alias{.bootstrap_cohort}
\title{Parametric bootstrap for one cohort's loss and loss-ratio CI}
\usage{
.bootstrap_cohort(
  loss_obs,
  loss_proj,
  prem_proj,
  g_selected,
  f_selected,
  g_sigma2,
  f_sigma2,
  g_var,
  f_var,
  last_obs,
  maturity_from,
  B,
  loss_alpha,
  method,
  probs
)
}
\value{
A list with four vectors of length \code{length(prem_proj)}:
\code{lr_ci_lo}, \code{lr_ci_hi} (for LR), and \code{loss_ci_lo},
\code{loss_ci_hi} (for cumulative loss).
}
\description{
Internal helper for bootstrap CI calculation used by \code{\link[=fit_lr]{fit_lr()}} when
\code{bootstrap = TRUE}. For a single cohort, simulates \code{B} replicates of the
projected loss path using Mack-style variance estimates as sampling
inputs, and returns percentile-based CI bounds aligned with the input
rows.

Observed rows (index <= \code{last_obs}) are returned with CI equal to the
observed value (no uncertainty). Projected rows get bootstrap
percentiles.
}
\keyword{internal}
