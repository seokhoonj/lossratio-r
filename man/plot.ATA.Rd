% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ata-vis.R
\name{plot.ATA}
\alias{plot.ATA}
\title{Plot age-to-age factor diagnostics}
\usage{
\method{plot}{ATA}(
  x,
  type = c("cv", "rse", "summary", "box", "point"),
  alpha = 1,
  show_maturity = TRUE,
  cv_threshold = 0.1,
  rse_threshold = 0.05,
  min_valid_ratio = 0.5,
  min_n_valid = 3L,
  min_run = 1L,
  scales = c("fixed", "free", "free_x", "free_y"),
  nrow = NULL,
  ncol = NULL,
  theme = c("view", "save", "shiny"),
  x.angle = 90,
  ...
)
}
\arguments{
\item{x}{An object of class \code{"ATA"}.}

\item{type}{One of \code{"cv"}, \code{"rse"}, \code{"summary"}, \code{"box"}, or \code{"point"}.}

\item{alpha}{Numeric scalar controlling the variance structure in the
WLS fit. Default is \code{1}. Passed to \code{\link[=summary.ATA]{summary.ATA()}}.}

\item{show_maturity}{Logical; if \code{TRUE}, draw a vertical reference line
and shade the mature region. Default is \code{TRUE}.}

\item{cv_threshold}{Numeric threshold for \code{cv}. Used when
\code{show_maturity = TRUE}. Default is \code{0.10}.}

\item{rse_threshold}{Numeric threshold for \code{rse}. Used when
\code{show_maturity = TRUE}. Default is \code{0.05}.}

\item{min_valid_ratio}{Minimum valid ratio. Default is \code{0.5}.}

\item{min_n_valid}{Minimum number of valid observations. Default is \code{3L}.}

\item{min_run}{Minimum consecutive mature links. Default is \code{1L}.}

\item{scales}{Facet scale argument passed to \code{\link[ggplot2:facet_wrap]{ggplot2::facet_wrap()}}.
One of \code{"fixed"}, \code{"free"}, \code{"free_x"}, or \code{"free_y"}.}

\item{nrow, ncol}{Number of rows and columns for \code{\link[ggplot2:facet_wrap]{ggplot2::facet_wrap()}}.}

\item{theme}{A string passed to \code{\link[=.switch_theme]{.switch_theme()}}.}

\item{x.angle}{Numeric angle for x-axis tick labels. Default is \code{90}
to prevent overlap of the \code{from-to} link labels.}

\item{...}{Additional arguments passed to \code{\link[=.switch_theme]{.switch_theme()}}.}
}
\value{
A \code{ggplot} object.
}
\description{
Visualise diagnostic summaries from an \code{"ATA"} object. Internally calls
the \code{summary()} method on an \code{ATA} object to compute descriptive
statistics and WLS estimates, and optionally \code{\link[=find_ata_maturity]{find_ata_maturity()}} to
identify the maturity point.
}
