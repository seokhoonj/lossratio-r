% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maturity.R
\name{maturity_at}
\alias{maturity_at}
\title{Construct a Maturity object from manually specified maturity points}
\usage{
maturity_at(...)
}
\arguments{
\item{...}{Named vectors of equal length. Must include \code{change}
(coercible to integer; the maturity point, i.e. the \code{to}-index of
the first mature ata link). Any other named arguments are
interpreted as group column values (e.g. \code{coverage}, \code{channel}).
With no group columns the result is a pooled (single-row)
Maturity.}
}
\value{
A \code{data.table} with class \code{"Maturity"} carrying the same
columns as \code{\link[=detect_maturity]{detect_maturity()}} output: group columns (if any),
\code{ata_from = change - 1L}, \code{change}, \code{ata_link = "<from>-<to>"},
and the diagnostic stat columns (\code{mean}, \code{median}, \code{wt}, \code{cv},
\code{f}, \code{f_se}, \code{rse}, \code{sigma}, \code{n_cohorts}, \code{n_valid}, \code{n_inf}, \code{n_nan},
\code{valid_ratio}) set to \code{NA_real_}. \code{attr(., "groups")} holds the
group column names (possibly \code{character(0)}).
}
\description{
User-facing helper for hand-specifying a maturity point (or a set of
per-group maturity points) without running \code{\link[=detect_maturity]{detect_maturity()}}. The
returned \code{"Maturity"} object plugs into any function that consumes a
Maturity result -- \code{fit_lr()}, \code{fit_loss()}, \code{\link[=backtest]{backtest()}}, and the
maturity input dispatcher -- by carrying the same row schema as
\code{\link[=detect_maturity]{detect_maturity()}} output (group columns plus \code{ata_from}, \code{change},
\code{ata_link}).

Use this when company-standard or domain-knowledge maturity points
are known a priori and you want to override the data-driven
detection. Stat columns (\code{mean}, \code{cv}, \code{f}, \code{rse}, ...) are set to
\code{NA_real_} because manual entry carries no estimates.

Argument syntax mirrors \code{data.frame()} / \code{data.table()}: named
vectors of equal length, one of which \strong{must} be \code{change} (the
maturity point, an integer dev index). Any other named arguments are
treated as group columns.
}
\examples{
\dontrun{
# Single-group manual override
maturity_at(coverage = "surgery", change = 4)

# Multi-group manual override (e.g. company-standard k*)
maturity_at(coverage = c("cancer", "ci", "inpatient", "surgery"),
            change   = c(   9,   10,     7,     4))

# Pooled (no group columns)
maturity_at(change = 5)
}

}
\seealso{
\code{\link[=detect_maturity]{detect_maturity()}}, \code{\link[=regime_at]{regime_at()}}
}
