% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-plot.R
\name{.regime_pca_plot}
\alias{.regime_pca_plot}
\title{Draw a PCA scatter of cohort development trajectories}
\usage{
.regime_pca_plot(
  data,
  show_arrow = TRUE,
  show_label = TRUE,
  show_ellipse = TRUE,
  show_mean = TRUE,
  show_median = TRUE,
  alpha = 0.3,
  palette = "Set1",
  scale = 1,
  title = NULL,
  subtitle = NULL,
  caption = NULL,
  theme = "view",
  ...
)
}
\arguments{
\item{data}{A \code{data.frame} of numeric trajectory columns plus one
categorical \code{regime} column.}

\item{show_arrow, show_label}{Draw loading arrows / variable names.}

\item{show_ellipse}{Draw per-regime normal-theory 90\% ellipses.}

\item{show_mean, show_median}{Draw per-regime mean (open circle) and
median (cross) score points.}

\item{alpha}{Point transparency.}

\item{palette}{Discrete Brewer palette for the \code{regime} colour.}

\item{scale}{Biplot scaling exponent; \code{0} disables score scaling.}

\item{title, subtitle, caption}{Passed to \code{\link[ggplot2:labs]{ggplot2::labs()}}.}

\item{theme}{Theme key forwarded to \code{\link[=.switch_theme]{.switch_theme()}}.}

\item{...}{Forwarded to \code{\link[=.switch_theme]{.switch_theme()}}.}
}
\value{
A \code{ggplot} object.
}
\description{
A lean PCA biplot specialised for \code{\link[=detect_regime]{detect_regime()}} output: every
numeric column of \code{data} is treated as a trajectory feature, the
\code{regime} column colours the score cloud, and loading arrows show
how each development period contributes to the first two principal
components.

Scores on \code{PC1} / \code{PC2} are divided by \code{sdev * sqrt(n)} raised to
\code{scale} (biplot-style scaling); loading vectors are rescaled to fit
inside the score range.
}
\keyword{internal}
