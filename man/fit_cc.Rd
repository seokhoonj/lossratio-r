% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cc.R
\name{fit_cc}
\alias{fit_cc}
\title{Cape Cod projection (Stanard 1985)}
\usage{
fit_cc(
  x,
  loss = "loss",
  exposure = "exposure",
  bootstrap = NULL,
  B = 999L,
  seed = NULL,
  type = c("parametric", "nonparametric", "analytical"),
  residual = c("cell", "link"),
  process = c("gamma", "od_pois", "normal"),
  alpha = 1,
  sigma_method = c("locf", "min_last2", "loglinear", "mack", "none"),
  recent = NULL,
  regime = NULL,
  conf_level = 0.95,
  ...
)
}
\arguments{
\item{x}{A \code{Triangle} object.}

\item{loss}{A single cumulative loss variable. Default \code{"loss"}.}

\item{exposure}{A single cumulative exposure variable. Default
\code{"exposure"}.}

\item{bootstrap}{Bootstrap configuration. Same forms as
\code{\link[=fit_bf]{fit_bf()}}'s \code{bootstrap} arg -- see there for the full description.}

\item{B}{Integer number of bootstrap replicates. Used only when
\code{bootstrap} resolves to \code{"auto"}. Default \code{999}.}

\item{seed}{Optional integer seed for reproducible bootstrap. Default
\code{NULL}.}

\item{type}{One of \code{"parametric"} (default), \code{"nonparametric"}, or
\code{"analytical"}. The latter is reserved for Phase 3c.}

\item{residual}{Residual scope for \code{type = "nonparametric"}. One of
\code{"cell"} (default) or \code{"link"}.}

\item{process}{One of \code{"gamma"} (default), \code{"od_pois"}, \code{"normal"}.}

\item{alpha}{Numeric scalar passed through to the inner \code{\link[=fit_cl]{fit_cl()}} /
\code{\link[=fit_exposure]{fit_exposure()}} calls. Default \code{1}.}

\item{sigma_method}{Sigma extrapolation method forwarded to
\code{\link[=fit_cl]{fit_cl()}} / \code{\link[=fit_exposure]{fit_exposure()}}. Default \code{"locf"}.}

\item{recent}{Optional positive integer; calendar-diagonal filter
forwarded to the inner fits. Default \code{NULL}.}

\item{regime}{Optional regime specification forwarded to the inner
loss and exposure fits. See \code{\link[=fit_cl]{fit_cl()}} for the four-type dispatch.}

\item{conf_level}{Confidence level for bootstrap quantile CI. Default
\code{0.95}.}

\item{...}{Reserved for future extension (currently unused).}
}
\value{
An object of class \code{"CCFit"} containing:
\describe{
\item{\code{call}}{The matched call.}
\item{\code{data}}{The input \code{Triangle}.}
\item{\code{method}}{\code{"cc"}.}
\item{\code{groups}, \code{cohort}, \code{dev}, \code{loss}, \code{exposure}}{Metadata.}
\item{\code{full}, \code{proj}, \code{summary}}{Same shape as \code{BFFit}. With
bootstrap enabled, \verb{$full} carries
\code{loss_total_se}/\code{loss_total_cv}/\code{loss_ci_lo}/\code{loss_ci_hi} on
projected cells, and \verb{$summary} carries the same plus
\code{elr_cc_se}/\code{elr_cc_cv}/\code{elr_cc_ci_lo}/\code{elr_cc_ci_hi}
(uncertainty on the pooled ELR itself).}
\item{\code{elr_cc}}{\code{data.table(group..., elr_cc)} -- the pooled ELR
per group (or scalar if no group).}
\item{\code{q}}{Per-cohort emerged fraction.}
\item{\code{cl_fit}, \code{exposure_fit}}{Inner CL / Exposure fits.}
\item{\code{bootstrap}}{When \code{bootstrap} is enabled, a
\code{CCBootstrap} helper holding both Triangle-level
\code{BootstrapTriangle} objects, the per-replicate ultimate
replicates, and the per-replicate pooled ELR draws; \code{NULL}
otherwise.}
\item{\code{ci_type}}{\code{"bootstrap"} when \code{bootstrap} is enabled,
\code{"analytical"} (placeholder) otherwise.}
\item{\code{alpha}, \code{sigma_method}, \code{recent}, \code{regime}}{Inputs
forwarded to the inner \code{\link[=fit_cl]{fit_cl()}} / \code{\link[=fit_exposure]{fit_exposure()}} calls.}
}
}
\description{
Fit a Cape Cod projection from a \code{"Triangle"} object. Cape Cod is
the \emph{prior-free} Bornhuetter-Ferguson variant introduced by Stanard
(1985): the a priori expected loss ratio is \emph{estimated from the
data itself} as a portfolio-pooled quantity, then plugged into the
BF formula.

\deqn{\widehat{\mathrm{ELR}}^{CC} =
  \frac{\sum_i L_{obs, i}}{\sum_i E_i^{ult} \cdot q_i}}

where
\itemize{
\item \eqn{L_{obs, i}}: cohort \eqn{i}'s observed cumulative loss
at its latest observed development period.
\item \eqn{q_i = L_{obs, i} / \hat L_{ult, i}^{CL}}: the expected
emerged fraction (inverse of cumulative LDF).
\item \eqn{E_i^{ult}}: cohort \eqn{i}'s ultimate exposure
(projected via chain ladder on exposure).
}

Given \eqn{\widehat{\mathrm{ELR}}^{CC}}, the per-cohort ultimate is
obtained from the BF formula with this single pooled ELR:

\deqn{\hat L_{ult, i}^{CC} = L_{obs, i} +
  (1 - q_i) \cdot \widehat{\mathrm{ELR}}^{CC} \cdot E_i^{ult}}

When multiple groups are present, \eqn{\widehat{\mathrm{ELR}}^{CC}}
is computed \emph{within group} (not pooled across groups) so each
group retains its own portfolio-level ELR estimate.

This is a peer worker alongside \code{\link[=fit_bf]{fit_bf()}} / \code{\link[=fit_cl]{fit_cl()}} / \code{\link[=fit_ed]{fit_ed()}}.
Standalone for the Cape Cod recipe -- composition with \code{\link[=fit_ratio]{fit_ratio()}}
is not part of this worker. Point projection is always computed;
bootstrap SE / CI is opt-in via \code{bootstrap = TRUE} (Phase 3b). The
bootstrap path also produces per-replicate pooled ELR draws
(\code{elr_cc_se}, \code{elr_cc_cv}, \code{elr_cc_ci_lo}, \code{elr_cc_ci_hi}) since the
Cape Cod ELR itself is data-driven and thus uncertain.
}
\examples{
\dontrun{
data(experience)
tri <- as_triangle(
  experience[coverage == "surgery"],
  groups   = "coverage",
  cohort   = "uy_m",
  calendar = "cy_m",
  loss     = "incr_loss",
  exposure = "incr_exposure"
)
cc <- fit_cc(tri)
summary(cc)
cc$elr_cc   # pooled ELR per group
}

}
\references{
Stanard, J. N. (1985). A simulation test of prediction errors of
loss reserve estimation techniques. \emph{Proceedings of the Casualty
Actuarial Society}, 72, 124-148.

Bornhuetter, R. L. and Ferguson, R. E. (1972). The actuary and IBNR.
\emph{Proceedings of the Casualty Actuarial Society}, 59, 181-195.
}
\seealso{
\code{\link[=fit_bf]{fit_bf()}} (Bornhuetter-Ferguson with user-supplied prior),
\code{\link[=fit_cl]{fit_cl()}}, \code{\link[=fit_exposure]{fit_exposure()}}
}
