% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backtest-vis.R
\name{plot.Backtest}
\alias{plot.Backtest}
\title{Plot a backtest object}
\usage{
\method{plot}{Backtest}(
  x,
  type = c("col", "diag", "cell"),
  cell_type = c("cumulative", "incremental"),
  scales = c("fixed", "free_y", "free_x", "free"),
  theme = c("view", "save", "shiny"),
  ...
)
}
\arguments{
\item{x}{An object of class \code{"Backtest"}.}

\item{type}{Plot type. One of \code{"col"}, \code{"diag"}, \code{"cell"}.}

\item{cell_type}{Which projection view to display. One of
\code{"cumulative"} (default; uses \code{ae_err}) or \code{"incremental"} (uses
\code{ae_err_incr}). Both are stored on every \code{Backtest} object -- pick
the view at plot time.}

\item{scales}{Facet scale argument. One of \code{"fixed"}, \code{"free"},
\code{"free_x"}, \code{"free_y"}.}

\item{theme}{String passed to \code{\link[=.switch_theme]{.switch_theme()}}.}

\item{...}{Extra arguments passed to \code{\link[=.switch_theme]{.switch_theme()}}.}
}
\value{
A \code{ggplot} object.
}
\description{
Visualise the A/E Error (\code{ae_err}) of a \code{"Backtest"} object.

Three plot types:
\itemize{
\item \code{"col"}: A/E Error aggregated by development period (one line
per summary statistic).
\item \code{"diag"}: A/E Error aggregated by calendar diagonal.
\item \code{"cell"}: per-cell A/E Error as a scatter / line, faceted by
group.
}
}
