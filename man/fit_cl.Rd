% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cl.R
\name{fit_cl}
\alias{fit_cl}
\title{Fit chain ladder projection from a \code{Triangle} object}
\usage{
fit_cl(
  x,
  method = c("mack"),
  loss = "loss",
  weight = NULL,
  alpha = 1,
  sigma_method = c("locf", "min_last2", "loglinear", "mack", "none"),
  recent = NULL,
  regime = NULL,
  maturity = NULL,
  tail = FALSE
)
}
\arguments{
\item{x}{An object of class \code{"Triangle"}.}

\item{method}{One of \code{"mack"}. Default is \code{"mack"}. The argument is
retained for future extensibility.}

\item{loss}{A single cumulative loss variable (column to project).
Typical choices are \code{"loss"}, \code{"exposure"}, or \code{"ratio"}.}

\item{weight}{An optional column name passed to \code{\link[=as_link]{as_link()}} as
the WLS weight variable. Typically \code{"exposure"} when \code{loss = "ratio"}.
Default is \code{NULL}.}

\item{alpha}{Numeric scalar controlling the variance structure in
\code{\link[=fit_ata]{fit_ata()}}. Default is \code{1}.}

\item{sigma_method}{Method used to extrapolate \code{sigma} for links where it
cannot be estimated. One of \code{"locf"} (default), \code{"min_last2"},
\code{"loglinear"}, \code{"mack"}, or \code{"none"}. \code{"mack"} applies the Mack
(1993, Appendix B) tail estimator to the last unestimated link only,
falling back to LOCF for any earlier ones with a warning. \code{"none"}
performs no extrapolation; \code{sigma} stays \code{NA} and downstream variance
terms drop those links via finite-value guards. Passed to
\code{\link[=.extrapolate_sigma_ata]{.extrapolate_sigma_ata()}}.}

\item{recent}{Optional positive integer. When supplied, only the most
recent \code{recent} periods are used for factor estimation. Default is
\code{NULL} (use all periods).}

\item{regime}{Optional regime specification for cohort cutoff. Accepts:
\code{NULL} (default -- no filter), a \code{Regime} object (from \code{\link[=detect_regime]{detect_regime()}}
or \code{regime_at()}), the string \code{"auto"} (internal
\code{detect_regime(tri, loss = "ratio")} call), or a function
\verb{function(tri) -> Regime} for deferred custom-config detection. When
supplied, cohorts strictly before the resolved change date are excluded
from factor estimation.}

\item{maturity}{Maturity input forwarded to \code{\link[=fit_ata]{fit_ata()}}. Accepts four
forms:
\describe{
\item{\code{NULL} (default)}{No maturity filtering.}
\item{\code{Maturity} object}{Pre-built (e.g. from \code{\link[=detect_maturity]{detect_maturity()}}
or \code{\link[=maturity_at]{maturity_at()}}) -- used as-is.}
\item{\code{"auto"}}{Internal \code{\link[=detect_maturity]{detect_maturity()}} call with defaults
(loss inferred from \code{loss}).}
\item{function \verb{function(tri) -> Maturity}}{Lazy spec, typically
built with \code{\link[=maturity_spec]{maturity_spec()}}, invoked on the triangle at fit
time (leakage-safe for \code{\link[=backtest]{backtest()}}).}
}}

\item{tail}{Logical or numeric. If \code{FALSE}, no tail factor is applied.
If \code{TRUE}, a log-linear tail factor is estimated from selected factors.
If numeric, the supplied value is used as the tail factor.}
}
\value{
An object of class \code{"CLFit"} containing:
\describe{
\item{\code{call}}{The matched call.}
\item{\code{data}}{The input \code{"Triangle"} object.}
\item{\code{method}}{The method used (\code{"mack"}).}
\item{\code{groups}}{Character vector of grouping variable names.}
\item{\code{cohort}}{Character scalar of period variable name.}
\item{\code{dev}}{Character scalar of development variable name.}
\item{\code{loss}}{Character scalar of loss column name.}
\item{\code{full}}{\code{data.table} with observed and projected values,
including process/parameter SE and CV columns.}
\item{\code{proj}}{\code{data.table} identical to \code{full} with observed cells
set to \code{NA}.}
\item{\code{link}}{The \code{"Link"} object produced by \code{\link[=as_link]{as_link()}}.}
\item{\code{summary}}{Cohort-level summary with latest, ultimate,
reserve, and Mack standard errors.}
\item{\code{selected}}{\code{data.table} of selected factors used for
projection.}
\item{\code{factor}}{\code{data.table} of fitted factors from \code{\link[=fit_ata]{fit_ata()}}.}
\item{\code{maturity}}{Maturity diagnostics from \code{\link[=detect_maturity]{detect_maturity()}},
or \code{NULL} when maturity filtering was not applied.}
\item{\code{alpha}}{Value of \code{alpha} used.}
\item{\code{sigma_method}}{Sigma extrapolation method.}
\item{\code{weight}}{Weight variable name used, or \code{NULL}.}
\item{\code{recent}}{Number of recent periods used, or \code{NULL}.}
\item{\code{regime}}{Resolved \code{Regime} object, or \code{NULL}.}
\item{\code{use_maturity}}{Logical; whether maturity filtering was applied.}
\item{\code{tail}}{Tail factor argument supplied by the user.}
\item{\code{tail_factor}}{Numeric tail factor applied.}
}
}
\description{
Fit a Mack (1993) chain ladder projection from an object of class
\code{"Triangle"}. The function works on long-form cumulative data and does
not require a complete triangle. Age-to-age factors are estimated
through \code{\link[=as_link]{as_link()}} and \code{\link[=fit_ata]{fit_ata()}}, then applied recursively. The
point forecast follows the standard recursion, and prediction
uncertainty is decomposed into process variance and parameter variance.

When \code{weight} is supplied (e.g. \code{"exposure"}), age-to-age factors and
their variance are estimated using the supplied WLS weights.
}
\examples{
\dontrun{
data(experience)
tri <- as_triangle(
  experience[coverage == "surgery"],
  groups   = "coverage",
  cohort   = "uy_m",
  calendar = "cy_m",
  loss     = "incr_loss",
  exposure = "incr_exposure"
)

# Mack chain ladder with process / parameter standard errors
cl_mack <- fit_cl(tri, loss = "loss", method = "mack")
summary(cl_mack)
plot(cl_mack)

# WLS factors for ratio (loss ratio) using exposure as the weight
cl_ratio <- fit_cl(tri, loss = "ratio", weight = "exposure")
}

}
\seealso{
\code{\link[=fit_ata]{fit_ata()}}, \code{\link[=fit_ratio]{fit_ratio()}}
}
