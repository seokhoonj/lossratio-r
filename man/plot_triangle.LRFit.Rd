% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lr-vis.R
\name{plot_triangle.LRFit}
\alias{plot_triangle.LRFit}
\title{Plot loss ratio projection as a triangle heatmap}
\usage{
\method{plot_triangle}{LRFit}(
  x,
  region = c("pred", "full", "data"),
  view = c("value", "usage"),
  label_style = c("value", "detail"),
  label_size = NULL,
  show_maturity = TRUE,
  digits = 0,
  amount_divisor = 1e+08,
  theme = c("view", "save", "shiny"),
  nrow = NULL,
  ncol = NULL,
  ...
)
}
\arguments{
\item{x}{An object of class \code{"LRFit"}.}

\item{region}{Cell region to plot (only used when \code{view = "value"}).
One of \code{"pred"} (projected cells only, observed cells masked),
\code{"full"} (observed + projected), or \code{"data"} (observed cumulative
loss / premium / lr from \code{x$data} — the raw Triangle, no
projection). Default is \code{"pred"}.}

\item{view}{Plot mode. One of:
\describe{
\item{"value" (default)}{Per-cell \code{lr} heatmap with column-wise
relative fill. \code{region} selects which cells to display.}
\item{"usage"}{Cell-status heatmap (\code{fit_data} / \code{held_out} /
\code{excluded} / \code{future}) driven by the fit's own metadata
(\code{x$recent}, \code{x$regime_break}, \code{x$maturity}). \code{region} is
ignored.}
}}

\item{label_style}{One of \code{"value"} (lr only) or \code{"detail"}
(lr with loss/exposure amounts). Default is \code{"value"}.}

\item{label_size}{Numeric label text size forwarded to
\code{\link[ggshort:ggtable]{ggshort::ggtable()}}. Defaults to \code{3} for \code{label_style = "value"}
and \code{2.5} for \code{label_style = "detail"} (two-line labels).}

\item{show_maturity}{Logical; if \code{TRUE}, show maturity line.
Default is \code{TRUE}.}

\item{digits}{Number of decimal places for lr display.
Default is \code{0}.}

\item{amount_divisor}{Numeric divisor for amount display in
\code{"detail"} mode. Default is \code{1e8}.}

\item{theme}{Theme string.}

\item{nrow, ncol}{Facet dimensions.}

\item{...}{Additional arguments passed to \code{\link[=.switch_theme]{.switch_theme()}}.}
}
\value{
A \code{ggplot} object.
}
\description{
Visualise an \code{"LRFit"} object as a triangle-style heatmap of
cumulative loss ratios. Observed and projected cells are
distinguished by border style.
}
