% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.assert_column_arg}
\alias{.assert_column_arg}
\title{Validate a column-name argument}
\usage{
.assert_column_arg(arg, arg_name, df, length_one = FALSE)
}
\arguments{
\item{arg}{The argument value (already extracted from the call).}

\item{arg_name}{The argument name as a string, used in error
messages (e.g., \code{"loss"}, \code{"cohort"}).}

\item{df}{The data.frame/data.table the columns must be present in.}

\item{length_one}{If \code{TRUE}, the argument must have length exactly 1.}
}
\value{
Invisibly returns \code{arg} on success; aborts otherwise.
}
\description{
Internal helper used by entry-point functions (\code{as_triangle},
\code{as_link}, \code{fit_cl}, ...) that take column names as plain
character arguments (no NSE). Performs:
\itemize{
\item type check — must be a non-empty character vector
\item optional length-one check — for arguments expected to resolve to
a single column (e.g., \code{cohort}, \code{loss})
\item presence check — every name must exist in \code{df}'s columns
}

Produces clear, argument-named error messages.
}
\keyword{internal}
