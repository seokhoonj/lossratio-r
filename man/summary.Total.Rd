% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/triangle.R
\name{summary.Total}
\alias{summary.Total}
\title{Summarise a \code{Total} object}
\usage{
\method{summary}{Total}(object, digits = 4L, ...)
}
\arguments{
\item{object}{An object of class \code{Total}.}

\item{digits}{Integer; number of digits passed to \code{\link[=round]{round()}} for
numeric columns. Default \code{4L}. Pass \code{NULL} to skip rounding.}

\item{...}{Unused; included for S3 compatibility.}
}
\value{
A \code{data.table} of class \code{"TotalSummary"} with the same rows
as the input \code{Total} (one per group), ordered by descending \code{lr}.
Preserves the \code{group_var} attribute.
}
\description{
S3 method for \code{summary()} on \code{Total} objects. \code{Total} already carries
one row per group (no time dimension), so this method produces a
compact view that orders rows by descending loss ratio and rounds
numeric columns for display.
}
\examples{
\dontrun{
tot <- build_total(df, groups = coverage)
summary(tot)
}

}
