% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regime.R
\name{.resolve_regime}
\alias{.resolve_regime}
\title{Resolve a regime input to a Regime object (or NULL)}
\usage{
.resolve_regime(arg, tri, masked_tri = NULL)
}
\arguments{
\item{arg}{The regime-change input (NULL / Regime / \code{"auto"} /
function).}

\item{tri}{A \code{"Triangle"} object — used as the detection input when
\code{masked_tri} is \code{NULL}.}

\item{masked_tri}{Optional masked \code{"Triangle"} (e.g. backtest's
training-only triangle). When supplied, \code{"auto"} and function
inputs operate on this triangle instead of \code{tri}.}
}
\value{
\code{NULL} or a \code{"Regime"} object.
}
\description{
Internal 4-type dispatcher used by \code{fit_lr()}, \code{fit_loss()},
\code{fit_premium()}, and \code{\link[=backtest]{backtest()}} to normalize the \code{regime}
input (or split-axis variants such as \code{loss_regime}) into a
single representation: either \code{NULL} (no filter) or a \code{"Regime"}
object.

The four accepted input types are:
\describe{
\item{\code{NULL}}{Returns \code{NULL} — no filter is applied.}
\item{\code{"Regime"} object}{Returned as-is.}
\item{\code{"auto"}}{Runs \code{\link[=detect_regime]{detect_regime()}} on \code{masked_tri} if supplied,
otherwise on \code{tri}, with \code{target = "lr"}. The \code{masked_tri}
fallback is the leakage-safe path used by \code{\link[=backtest]{backtest()}} — fit
functions pass only \code{tri}, while \code{\link[=backtest]{backtest()}} passes both so
detection sees only the masked (training) data.}
\item{\verb{function(tri) -> Regime}}{Closure invoked with
\code{masked_tri} (if non-NULL) or \code{tri}. Its return value must
inherit \code{"Regime"}; an error is raised otherwise.}
}
}
\keyword{internal}
