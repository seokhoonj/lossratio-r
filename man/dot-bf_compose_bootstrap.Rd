% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bf.R
\name{.bf_compose_bootstrap}
\alias{.bf_compose_bootstrap}
\title{Per-replicate BF / Cape Cod composition from two BootstrapTriangle}
\usage{
.bf_compose_bootstrap(
  boots,
  prior_table,
  grp,
  by_cols,
  full,
  summary_table,
  conf_level,
  cohorts_present,
  cape_cod = FALSE
)
}
\arguments{
\item{boots}{A named list \code{list(loss = BT, exposure = BT)} from
\code{.resolve_bootstrap_bf()}.}

\item{prior_table}{Per-cohort ELR table (see \code{.resolve_bf_prior()}).
Pass \code{NULL} for the Cape Cod composition (ELR is data-pooled per
replicate).}

\item{grp}{Group column character vector.}

\item{by_cols}{\code{c(grp, "cohort")}.}

\item{full}{The point-estimate \verb{$full} data.table (used as the base
for join-on bootstrap SE / CI columns).}

\item{summary_table}{The point-estimate cohort-level summary.}

\item{conf_level}{Confidence level for CI bounds.}

\item{cohorts_present}{Unique \verb{[grp, cohort]} rows present in the
triangle.}
}
\value{
List \code{list(full, summary, bootstrap)} where \code{bootstrap} is the
\code{BFBootstrap} / \code{CCBootstrap} helper class.
}
\description{
Given paired loss-side and exposure-side \code{BootstrapTriangle} objects
(with \code{keep_pseudo = TRUE} so the per-replicate cohort-by-dev cum
loss / cum exposure means are available), compose the BF / Cape Cod
ultimate distribution per replicate:

\enumerate{
\item For each replicate \eqn{b}, derive \eqn{q_i^b =
    L_{obs,i} / L_{ult,i}^{CL,b}} from the loss-side Stage 1 mean
trajectory (last-dev cell).
\item Derive \eqn{E_i^{ult,b}} from the exposure-side Stage 1 mean
last-dev cell.
\item For BF: \eqn{L_{ult,i}^{b} = L_{obs,i} +
    (1 - q_i^b) \cdot \mathrm{ELR}_i \cdot E_i^{ult,b}}.
\item For Cape Cod: per group \eqn{\widehat{\mathrm{ELR}}^{CC,b} =
    \sum_i L_{obs,i} / \sum_i E_i^{ult,b} \cdot q_i^b}, then plug
into the BF formula.
\item Cell-level projection per replicate: scale the per-replicate
CL emergence pattern to land at \eqn{L_{ult,i}^{b}} at the last
dev.
}

Cell-level and cohort-level SE / CI are the SD / quantiles across
replicates.
}
\keyword{internal}
