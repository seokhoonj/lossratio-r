% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.compute_segment_mini_tri_bounds}
\alias{.compute_segment_mini_tri_bounds}
\title{Per-cell effective \code{dev_min} for the segment_wise mini-triangle}
\usage{
.compute_segment_mini_tri_bounds(coh_ranks, seg_ids, max_cal, bridge = FALSE)
}
\arguments{
\item{coh_ranks}{Integer vector. Per-cell cohort rank within the group.}

\item{seg_ids}{Integer vector. Per-cell segment id (1 = oldest).}

\item{max_cal}{Integer scalar. Maximum calendar index in the group.}

\item{bridge}{Logical. When \code{TRUE}, widen each older segment's
mini-triangle with the calendar-diagonal bridge anchored at the
next segment's first-cohort midpoint dev. When \code{FALSE} (default),
return the natural mini-triangle wall only -- the pure
\code{"segment_wise"} treatment. \code{TRUE} corresponds to
\code{"segment_wise_bridged"}.}
}
\value{
Integer vector (same length as \code{coh_ranks}) of per-cell
effective \code{dev_min} for the mini-triangle filter (bridged or pure).
}
\description{
For each cell, returns the minimum dev that keeps it inside its segment's
fit mask. The mask is the union of two regions:
\enumerate{
\item The segment's natural mini-triangle: \code{dev >= max_cal - seg_last + 1}.
\item A \emph{bridge} extension along the calendar diagonal anchored at the
\emph{next} (newer) segment's first-cohort midpoint dev. The bridge lets
each older segment connect to its successor, filling the late-dev
cells of its early cohorts that would otherwise be cut by the
natural mini-triangle wall and leave projection cells unreachable.
}

Bridge construction (segments ordered by \code{segment_id}, lower id = older
cohorts). For each segment \code{s} except the newest (no successor), find
segment \code{s+1}'s
\itemize{
\item \code{first_rank}  -- cohort rank of \code{s+1}'s first cohort,
\item \code{seg_dev_min} -- \code{max_cal - last_rank(s+1) + 1},
\item \code{first_cohort_dev_max} -- \code{max_cal - first_rank(s+1) + 1},
\item \code{mid_dev}     -- \code{floor((seg_dev_min + first_cohort_dev_max) / 2)}.
}

The bridge diagonal for segment \code{s} is at
\code{ext_cal_idx(s) = first_rank(s+1) + mid_dev(s+1) - 2} (the cell one
cohort earlier than \code{s+1}'s first cohort, at the same dev as that first
cohort's mini-triangle midpoint). Each cell in segment \code{s} then takes

\code{effective_dev_min = min(seg_dev_min(s), ext_cal_idx(s) - coh_rank + 1)}

with \code{pmin(..., na.rm = TRUE)} so the natural mini-tri wall stays put
when no bridge applies (the last segment, or cells whose bridge ray
lies above the wall).

Bridges do not cascade: segment \code{s} is bridged only from segment \code{s+1},
not from \code{s+2}. The bridge only ever \emph{widens} a segment's mini-triangle.
}
\keyword{internal}
