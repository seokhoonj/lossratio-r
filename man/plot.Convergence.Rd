% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convergence.R
\name{plot.Convergence}
\alias{plot.Convergence}
\title{Plot the Convergence diagnostic}
\usage{
\method{plot}{Convergence}(x, theme = c("view", "save", "shiny"), ...)
}
\arguments{
\item{x}{An object of class \code{Convergence}.}

\item{theme}{String passed to \code{\link[=.switch_theme]{.switch_theme()}}.}

\item{...}{Additional arguments passed to \code{\link[=.switch_theme]{.switch_theme()}}.}
}
\value{
A \code{ggplot} object.
}
\description{
Four-panel diagnostic showing the loss ratio backtest path and each
stability metric vs. its threshold:
\itemize{
\item Top: \code{ratio} (the portfolio LR projection at each valuation).
\item Then for each of \code{drift_window}, \code{drift_tail}, \verb{|slope|},
\code{dispersion}: the metric over \code{v} with a dashed horizontal
line at the threshold (\code{max_drift}, \code{max_slope}, or \code{max_dispersion}).
}
Vertical guides mark \code{mat_k} (dashed) and the detected \code{conv_k}
for the chosen \code{method} (solid). The chosen-method panel title is
annotated.
}
