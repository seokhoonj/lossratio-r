% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lr.R
\name{fit_lr}
\alias{fit_lr}
\title{Fit loss ratio projection model}
\usage{
fit_lr(
  x,
  method = c("sa", "ed", "cl"),
  loss_var = "closs",
  exposure_var = "crp",
  loss_alpha = 1,
  exposure_alpha = 1,
  delta_method = c("simple", "full"),
  rho = 0,
  conf_level = 0.95,
  sigma_method = c("min_last2", "locf", "loglinear"),
  recent = NULL,
  regime_break = NULL,
  maturity_args = NULL,
  bootstrap = FALSE,
  B = 1000,
  seed = NULL
)
}
\arguments{
\item{x}{An object of class \code{"Triangle"}.}

\item{method}{One of \code{"sa"}, \code{"ed"}, or \code{"cl"}.
Default is \code{"sa"}.}

\item{loss_var}{Cumulative loss variable. Default is \code{"closs"}.}

\item{exposure_var}{Cumulative exposure variable. Default is \code{"crp"}.}

\item{loss_alpha}{Numeric scalar controlling the variance structure for
loss estimation. Default is \code{1}.}

\item{exposure_alpha}{Numeric scalar for exposure chain ladder. Default
is \code{1}.}

\item{delta_method}{Method for computing \code{se_lr = SE(L/E)}. One of:
\describe{
\item{\code{"simple"} (default)}{\code{se_lr = se_proj / exposure_proj},
treats exposure as fixed.}
\item{\code{"full"}}{Full delta method with exposure uncertainty and
loss-exposure correlation:
\deqn{\mathrm{Var}(L/E) \approx \frac{\mathrm{Var}(L)}{E^2}
        + \frac{L^2 \mathrm{Var}(E)}{E^4}
        - \frac{2 \rho L \mathrm{SE}(L) \mathrm{SE}(E)}{E^3}}
}
}}

\item{rho}{Numeric scalar in \verb{(-1, 1)}; assumed correlation between
ultimate loss and ultimate exposure. Only used when
\code{delta_method = "full"}. Default is \code{0}.}

\item{conf_level}{Confidence level used for \code{ci_lower}/\code{ci_upper} in
the cohort summary. Default is \code{0.95}.}

\item{sigma_method}{Sigma extrapolation method. One of \code{"min_last2"}
(default), \code{"locf"}, or \code{"loglinear"}.}

\item{recent}{Optional positive integer for estimation window.
Default is \code{NULL}.}

\item{regime_break}{Optional cohort cutoff for the regime break. Accepts:
\code{NULL} (default, no filter), a single \code{Date}/character coercible to Date,
a vector of dates (uses the latest), or a \code{CohortRegime} object (extracts
the latest from \verb{$breakpoints}). Behavior depends on \code{method}:
\describe{
\item{\code{"sa"}}{Hybrid filter. Pre-break cohorts are dropped only for
development periods at or before the maturity point (ED phase);
post-maturity (CL) cells use the \code{recent}-diagonal window across
all cohorts. This preserves CL stability while protecting the ED
intensities from a regime shift.}
\item{\code{"ed"}, \code{"cl"}}{Simple cohort cut: all cohorts strictly before
the break date are excluded from estimation.}
}}

\item{maturity_args}{A named list forwarded to \code{\link[=find_ata_maturity]{find_ata_maturity()}},
or \code{NULL} (default) to skip maturity filtering. When
\code{method = "sa"}, this also determines the switch point between
ED and CL. Pass \code{list()} to use all defaults.}

\item{bootstrap}{Logical; if \code{TRUE}, parameter and process variance
are derived via residual bootstrap rather than the analytical
delta method. Default is \code{FALSE}.}

\item{B}{Integer number of bootstrap replications. Used only when
\code{bootstrap = TRUE}. Default is \code{1000}.}

\item{seed}{Optional integer seed for reproducible bootstrap.
Default is \code{NULL}.}
}
\value{
An object of class \code{"LRFit"}.
}
\description{
Unified interface for loss ratio projection from a \code{"Triangle"} object.
Three projection methods are available:

\describe{
\item{\code{"sa"} (default)}{Uses exposure-driven (ED) estimation
before maturity and chain ladder (CL) after maturity.
\itemize{
\item Before maturity: age-to-age factors are volatile, so
exposure-driven projection
\eqn{\Delta C^L = g_k \cdot C^P_k} anchors the estimate
to premium volume.
\item After maturity: age-to-age factors are stable, so
chain ladder projection
\eqn{C^L_{k+1} = f_k \cdot C^L_k} preserves the cohort's
observed level.
}}
\item{\code{"ed"}}{Exposure-driven for all development periods.
All future increments are \eqn{g_k \cdot C^P_k}.}
\item{\code{"cl"}}{Chain ladder for all development periods.
Equivalent to classical Mack model.}
}

In all cases, exposure is projected forward using chain ladder:
\deqn{\hat{C}^P_{i,k+1} = f^P_k \cdot \hat{C}^P_{i,k}}
}
\examples{
\dontrun{
data(experience)
exp <- as_experience(experience)
tri <- build_triangle(exp[cv_nm == "SUR"], group_var = cv_nm)

# Stage-adaptive (default): ED before maturity, CL after
lr_sa <- fit_lr(tri, method = "sa")
summary(lr_sa)
plot(lr_sa)

# Pure exposure-driven for all development periods
lr_ed <- fit_lr(tri, method = "ed")

# Pure chain ladder (Mack-style) for all development periods
lr_cl <- fit_lr(tri, method = "cl")
}

}
\seealso{
\code{\link[=build_triangle]{build_triangle()}}, \code{\link[=build_link]{build_link()}}, \code{\link[=fit_ata]{fit_ata()}},
\code{\link[=fit_ed]{fit_ed()}}, \code{\link[=find_ata_maturity]{find_ata_maturity()}}
}
