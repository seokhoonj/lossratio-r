% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-plot.R
\name{.cell_grid}
\alias{.cell_grid}
\title{Draw a cohort x development cell grid (heatmap or table)}
\usage{
.cell_grid(
  data,
  x,
  y,
  label = NULL,
  fill = NULL,
  fill_scale = c("none", "gradient", "threshold"),
  fill_args = list(),
  label_args = list(),
  border = c("tile", "panel", "none"),
  border_color = "black",
  border_width = 0.3
)
}
\arguments{
\item{data}{A \code{data.frame} / \code{data.table}.}

\item{x, y}{Column-name strings mapped to the grid axes.}

\item{label}{Optional column-name string drawn as in-cell text.}

\item{fill}{Optional column-name string (numeric) driving cell fill.}

\item{fill_scale}{One of \code{"gradient"}, \code{"threshold"}, \code{"none"}.}

\item{fill_args}{Named list of fill options. Gradient keys: \code{low},
\code{mid}, \code{high}, \code{midpoint}, \code{na}, \code{guide}. Threshold keys:
\code{threshold}, \code{when} (\code{">"}, \code{">="}, \code{"<"}, \code{"<="}), \code{high}, \code{low},
\code{na}.}

\item{label_args}{Named list passed to the label \code{geom_text()}.}

\item{border}{One of \code{"tile"} (per-cell border), \code{"panel"} (grid
lines on cell edges), or \code{"none"}.}

\item{border_color, border_width}{Border colour and line width.}
}
\value{
A \code{ggplot} object.
}
\description{
A general \code{ggplot2} cell-grid builder: the \code{x} and \code{y} columns are
coerced to factors and placed on a regular integer grid of unit
tiles, with optional in-cell text labels. One builder covers both
the continuous heatmap and the threshold-coloured table -- the only
difference is how the fill is mapped, selected by \code{fill_scale}:

\describe{
\item{\code{"gradient"}}{\code{fill} is numeric and mapped continuously
(\code{\link[ggplot2:scale_gradient]{ggplot2::scale_fill_gradient2()}} when \code{fill_args$midpoint} is
set, otherwise \code{\link[ggplot2:scale_gradient]{ggplot2::scale_fill_gradient()}}).}
\item{\code{"threshold"}}{\code{fill} is numeric and compared to
\code{fill_args$threshold} with the \code{fill_args$when} operator; cells
are coloured \code{high} / \code{low} / \code{na} and drawn with
\code{\link[ggplot2:scale_identity]{ggplot2::scale_fill_identity()}}.}
\item{\code{"none"}}{no fill.}
}

Columns are referenced by name (plain strings) -- no non-standard
evaluation. The result is a \code{ggplot} object the caller can extend.
}
\keyword{internal}
