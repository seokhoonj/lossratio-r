% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/triangle.R
\name{plot_triangle.TriangleValidation}
\alias{plot_triangle.TriangleValidation}
\title{Triangle-heatmap view of dev-sequence gaps}
\usage{
\method{plot_triangle}{TriangleValidation}(
  x,
  view = c("calendar", "dev"),
  show_label = FALSE,
  theme = c("view", "save", "shiny"),
  ...
)
}
\arguments{
\item{x}{A \code{TriangleValidation} object.}

\item{view}{Axis layout. One of \code{"calendar"} (cohort x calendar grid,
default) or \code{"dev"} (cohort x dev grid). \code{"calendar"} requires the
calendar column to have been supplied to \code{\link[=validate_triangle]{validate_triangle()}};
\code{"dev"} works when either calendar or dev was supplied.}

\item{show_label}{Logical; when \code{TRUE}, overlay each cell with the
input row count (\code{.N}). Default \code{FALSE}.}

\item{theme}{String passed to \code{\link[=.switch_theme]{.switch_theme()}}.}

\item{...}{Extra arguments passed to \code{\link[=.switch_theme]{.switch_theme()}}.}
}
\value{
A \code{ggplot} object, or \code{invisible(NULL)} when there is nothing
to visualise.
}
\description{
Visualise gap positions on a \verb{cohort x dev} grid: for every cohort
with gaps, expanded dev cells are coloured by status (\code{observed} /
\code{missing}). Complements \code{\link[=plot.TriangleValidation]{plot.TriangleValidation()}} (which shows
observed-vs-expected counts as bars) -- this heatmap shows \emph{where}
the gaps are.

When the validation found no gaps, prints a message and returns
\code{invisible(NULL)}.
}
