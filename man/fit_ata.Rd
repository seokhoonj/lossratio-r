% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ata.R
\name{fit_ata}
\alias{fit_ata}
\title{Fit age-to-age development factors}
\usage{
fit_ata(
  x,
  alpha = 1,
  na_method = c("locf", "none"),
  sigma_method = c("min_last2", "locf", "loglinear"),
  recent = NULL,
  regime_break = NULL,
  maturity_args = NULL,
  ...
)
}
\arguments{
\item{x}{An object of class \code{"ATA"}, typically produced by \code{\link[=build_ata]{build_ata()}}.}

\item{alpha}{Numeric scalar controlling the variance structure. Default
is \code{1}.}

\item{na_method}{Method used to fill \code{NA} values in \code{f_selected}. One of
\code{"locf"} (default) or \code{"none"}. Passed to \code{\link[=.filter_ata]{.filter_ata()}}.}

\item{sigma_method}{Method used to extrapolate \code{sigma} for links where it
cannot be estimated. One of \code{"min_last2"} (default), \code{"locf"}, or
\code{"loglinear"}. Passed to \code{\link[=.extrapolate_sigma_ata]{.extrapolate_sigma_ata()}}.}

\item{recent}{Optional positive integer. When supplied, only the most
recent \code{recent} periods in the \code{ata} triangle are used for factor
estimation. Applied before maturity filtering. Default is \code{NULL}
(use all periods).}

\item{regime_break}{Optional cohort cutoff for the regime break. Accepts:
\code{NULL} (default, no filter), a single \code{Date}/character coercible to Date,
a vector of dates (uses the latest), or a \code{CohortRegime} object (extracts
the latest from \verb{$breakpoints}). When supplied, cohorts with
\code{cohort < break_date} are excluded from estimation. Default is \code{NULL}.}

\item{maturity_args}{A named list of arguments forwarded to
\code{\link[=find_ata_maturity]{find_ata_maturity()}}, or \code{NULL} (default) to skip maturity filtering.
When a list is supplied, missing elements are filled with package
defaults via \code{\link[utils:modifyList]{utils::modifyList()}}:
\describe{
\item{\code{cv_threshold}}{Default \code{0.10}.}
\item{\code{rse_threshold}}{Default \code{0.05}.}
\item{\code{min_valid_ratio}}{Default \code{0.5}.}
\item{\code{min_n_valid}}{Default \code{3L}.}
\item{\code{min_run}}{Default \code{1L}.}
}
Pass \code{list()} to use all defaults with maturity filtering enabled.}

\item{...}{Additional arguments passed to \code{\link[=summary.ATA]{summary.ATA()}}.}
}
\value{
An object of class \code{"ATAFit"} (a named list) containing:
\describe{
\item{\code{call}}{The matched call.}
\item{\code{ata}}{The input \code{"ATA"} object.}
\item{\code{summary}}{\code{"ATASummary"} object from \code{\link[=summary.ATA]{summary.ATA()}}.}
\item{\code{selected}}{\code{data.table} of factors ready for projection,
including \code{f_selected} and \code{sigma2}.}
\item{\code{maturity}}{Maturity diagnostics from \code{\link[=find_ata_maturity]{find_ata_maturity()}},
or \code{NULL} when maturity filtering was not applied.}
\item{\code{alpha}}{Value of \code{alpha} used.}
\item{\code{na_method}}{NA fill method used.}
\item{\code{sigma_method}}{Sigma extrapolation method used.}
\item{\code{recent}}{Number of recent periods used, or \code{NULL}.}
\item{\code{regime_break}}{Resolved regime-break cutoff (\code{Date}), or \code{NULL}.}
\item{\code{use_maturity}}{Logical; whether maturity filtering was applied.}
\item{\code{maturity_args}}{Resolved maturity arguments, or \code{NULL}.}
}
}
\description{
Estimate age-to-age (ata) development factors from an object of class
\code{"ATA"} and return a unified \code{"ATAFit"} object that bundles:

\itemize{
\item Summary statistics and WLS estimates (\code{summary}) from
\code{\link[=summary.ATA]{summary.ATA()}}.
\item Selected factors (\code{selected}) ready for chain ladder projection,
after optional maturity filtering and LOCF fill.
\item Maturity diagnostics (\code{maturity}) from \code{\link[=find_ata_maturity]{find_ata_maturity()}}.
}
}
\seealso{
\code{\link[=build_ata]{build_ata()}}, \code{\link[=summary.ATA]{summary.ATA()}}, \code{\link[=find_ata_maturity]{find_ata_maturity()}},
\code{\link[=fit_cl]{fit_cl()}}
}
