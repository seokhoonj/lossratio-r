% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ata.R
\name{fit_ata}
\alias{fit_ata}
\title{Fit age-to-age development factors}
\usage{
fit_ata(
  x,
  target = "loss",
  weight = NULL,
  alpha = 1,
  na_method = c("locf", "none"),
  sigma_method = c("locf", "min_last2", "loglinear"),
  recent = NULL,
  regime = NULL,
  maturity = NULL,
  ...
)
}
\arguments{
\item{x}{An object of class \code{"Link"}, typically produced by
\code{\link[=as_link]{as_link()}}.}

\item{target}{Cumulative metric for the link factor. Default
\code{"loss"}. Forwarded to \code{\link[=as_link]{as_link()}}.}

\item{weight}{Optional WLS weight variable. Forwarded to
\code{\link[=as_link]{as_link()}}.}

\item{alpha}{Numeric scalar controlling the variance structure. Default
is \code{1}.}

\item{na_method}{Method used to fill \code{NA} values in \code{f_sel}. One of
\code{"locf"} (default) or \code{"none"}. Passed to \code{\link[=.filter_ata]{.filter_ata()}}.}

\item{sigma_method}{Method used to extrapolate \code{sigma} for links where it
cannot be estimated. One of \code{"locf"} (default), \code{"min_last2"}, or
\code{"loglinear"}. Passed to \code{\link[=.extrapolate_sigma_ata]{.extrapolate_sigma_ata()}}.}

\item{recent}{Optional positive integer. When supplied, only the most
recent \code{recent} periods in the \code{Link} triangle are used for factor
estimation. Applied before maturity filtering. Default is \code{NULL}
(use all periods).}

\item{regime}{Optional regime specification for cohort cutoff. Accepts:
\code{NULL} (default — no filter), a \code{Regime} object (from \code{\link[=detect_regime]{detect_regime()}}
or \code{regime_at()}), the string \code{"auto"} (internal
\code{detect_regime(tri, target = "lr")} call), or a function
\verb{function(tri) -> Regime} for deferred custom-config detection. When
supplied, cohorts strictly before the resolved change date are excluded
from estimation.}

\item{maturity}{Optional maturity specification for filtering ata
links. Accepts four input types:
\describe{
\item{\code{NULL} (default)}{No maturity filter.}
\item{\code{Maturity} object}{Use as-is. Typically built via
\code{\link[=detect_maturity]{detect_maturity()}} or \code{\link[=maturity_at]{maturity_at()}}.}
\item{\code{"auto"}}{Detect maturity internally via \code{detect_maturity(x)}
on the input triangle.}
\item{Function / closure}{A user-supplied function taking the
triangle and returning a \code{Maturity} object (e.g. from
\code{\link[=maturity_spec]{maturity_spec()}}) for deferred custom-config detection.}
}
When the supplied \code{Maturity} carries \code{attr(., "groups")} that differs
from the Triangle's grouping, the Triangle is rebucketed to the
maturity partition before link construction.}

\item{...}{Additional arguments passed to \code{\link[=summary.Link]{summary.Link()}}.}
}
\value{
An object of class \code{"ATAFit"} (a named list) containing:
\describe{
\item{\code{call}}{The matched call.}
\item{\code{link}}{The input \code{"Link"} object.}
\item{\code{summary}}{\code{"ATASummary"} object from \code{\link[=summary.Link]{summary.Link()}}.}
\item{\code{selected}}{\code{data.table} of factors ready for projection,
including \code{f_sel} and \code{sigma2}.}
\item{\code{maturity}}{Resolved \code{Maturity} object used for filtering,
or \code{NULL} when maturity filtering was not applied.}
\item{\code{alpha}}{Value of \code{alpha} used.}
\item{\code{na_method}}{NA fill method used.}
\item{\code{sigma_method}}{Sigma extrapolation method used.}
\item{\code{recent}}{Number of recent periods used, or \code{NULL}.}
\item{\code{regime}}{Resolved \code{Regime} object, or \code{NULL}.}
\item{\code{use_maturity}}{Logical; whether maturity filtering was applied.}
}
}
\description{
Estimate age-to-age (ata) development factors from an object of class
\code{"Link"} and return a unified \code{"ATAFit"} object that bundles:

\itemize{
\item Summary statistics and WLS estimates (\code{summary}) from
\code{\link[=summary.Link]{summary.Link()}} with \code{model = "ata"}.
\item Selected factors (\code{selected}) ready for chain ladder projection,
after optional maturity filtering and LOCF fill.
\item Maturity diagnostics (\code{maturity}) from \code{\link[=detect_maturity]{detect_maturity()}}.
}
}
\seealso{
\code{\link[=as_link]{as_link()}}, \code{\link[=summary.Link]{summary.Link()}}, \code{\link[=detect_maturity]{detect_maturity()}},
\code{\link[=fit_cl]{fit_cl()}}
}
