% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{.boot_refit_ed}
\alias{.boot_refit_ed}
\title{Refit exposure-driven model per bootstrap replicate}
\usage{
.boot_refit_ed(triangle, boots, alpha = 1)
}
\arguments{
\item{triangle}{The original \code{Triangle} (carries the observed region
mask).}

\item{boots}{A \code{BootstrapTriangle} from \code{bootstrap()}.}

\item{alpha}{Mack variance exponent (currently \code{1} only).}
}
\value{
Same shape as \code{.boot_refit_cl()}.
}
\description{
Like \code{.boot_refit_cl()} but additive: refit intensities
\verb{g*_k = sum(Delta_loss) / sum(P_\{k-1\})} per replicate (with original
premium kept fixed), then re-project missing cells via the ED additive
recursion. \verb{cell_proc_var = sigma^2*_g_k * P_\{i,k-1\}} for projected
cells, 0 for observed.
}
\keyword{internal}
