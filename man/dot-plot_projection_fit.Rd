% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cl-vis.R
\name{.plot_projection_fit}
\alias{.plot_projection_fit}
\title{Projection plot for a projection-level fit}
\usage{
.plot_projection_fit(
  x,
  conf_level = 0.95,
  show_interval = TRUE,
  amount_divisor = "auto",
  scales = c("fixed", "free_y", "free_x", "free"),
  theme = c("view", "save", "shiny"),
  nrow = NULL,
  ncol = NULL,
  ...
)
}
\arguments{
\item{x}{A projection-level fit (\code{CLFit}, \code{SAFit}, \code{BFFit}, \code{CCFit},
or \code{PremiumFit}) with a \verb{$full} grid and \verb{$loss} / \verb{$groups} /
\verb{$cohort} / \verb{$dev} slots.}

\item{conf_level}{Confidence level for interval display. Default is
\code{0.95}.}

\item{show_interval}{Logical; if \code{TRUE}, show normal-approximation
confidence intervals. Default is \code{TRUE}.}

\item{amount_divisor}{Numeric scaling factor for y-axis labels of amount
variables. Default is \code{1e8}.}

\item{scales}{Facet scale argument passed to \code{\link[ggplot2:facet_wrap]{ggplot2::facet_wrap()}}.
One of \code{"fixed"}, \code{"free"}, \code{"free_x"}, or \code{"free_y"}.}

\item{theme}{A string passed to \code{\link[=.switch_theme]{.switch_theme()}}.}

\item{nrow, ncol}{Number of rows and columns for \code{\link[ggplot2:facet_wrap]{ggplot2::facet_wrap()}}.}

\item{...}{Additional arguments passed to \code{\link[=.switch_theme]{.switch_theme()}}.}
}
\value{
A \code{ggplot} object.
}
\description{
Role-agnostic projection plot: observed and projected cumulative
values by cohort over development periods, with an optional
confidence band. The plotted column family is taken from \code{x$loss}
(the fit's standardized role -- \code{"loss"} for CL / SA / BF / CC,
\code{"premium"} for an \code{PremiumFit}), so every projection-level fit
shares one implementation. The interval is drawn whenever the
\verb{<role>_total_se} column is present and finite.
}
\keyword{internal}
