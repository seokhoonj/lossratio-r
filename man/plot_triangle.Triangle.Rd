% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/triangle-vis.R
\name{plot_triangle.Triangle}
\alias{plot_triangle.Triangle}
\title{Plot development values as a triangle table}
\usage{
\method{plot_triangle}{Triangle}(
  x,
  view = c("value", "usage"),
  metric = "ratio",
  label_style = c("value", "detail"),
  label_size = NULL,
  amount_divisor = "auto",
  nrow = NULL,
  ncol = NULL,
  theme = c("view", "save", "shiny"),
  ...
)
}
\arguments{
\item{x}{An object of class \code{Triangle}.}

\item{view}{Plot view. One of:
\describe{
\item{"value"}{(default) Per-cell metric heatmap controlled by
\code{metric}, \code{label_style}, \code{amount_divisor}, \code{nrow}, \code{ncol}.}
\item{"usage"}{Cell-status heatmap (used / holdout / unused /
future). Accepts \code{recent}, \code{regime}, \code{holdout}, \code{maturity}
via \code{...}. See \code{vignette("regime-change-filter")} for details.}
}}

\item{metric}{A single metric to plot. Must be one of:
\code{"ratio"}, \code{"incr_ratio"},
\code{"loss"}, \code{"incr_loss"}, \code{"premium"}, \code{"incr_premium"}, \code{"margin"}, \code{"incr_margin"},
\code{"loss_share"}, \code{"incr_loss_share"}, \code{"premium_share"}, or \code{"incr_premium_share"}.}

\item{label_style}{Label display style. One of:
\describe{
\item{"value"}{Show only the selected metric.}
\item{"detail"}{For \code{ratio} / \code{incr_ratio}, show the ratio in percent and, on the
next line, the associated loss / premium amounts. For amount and
proportion metrics, this falls back to \code{"value"}.}
}}

\item{label_size}{Numeric size of the in-cell text label. Defaults
to \code{3} for \code{label_style = "value"} and \code{2.5} for
\code{label_style = "detail"} (two-line labels need a smaller size to
fit). Other label appearance fields (family, color, hjust, ...)
fall back to the standard label defaults.}

\item{amount_divisor}{Numeric scaling factor applied to amount variables
(e.g., \code{loss}, \code{incr_loss}, \code{premium}, \code{incr_premium}, \code{margin}, \code{incr_margin}) before plotting.
Default \code{"auto"} picks the largest divisor in
\verb{\{1, 1e3, 1e6, 1e7, 1e8, 1e9\}} such that the median displayed
value is still at least \code{1}, minimising label digit count.}

\item{nrow, ncol}{Number of rows and columns passed to \code{\link[ggplot2:facet_wrap]{ggplot2::facet_wrap()}}.}

\item{theme}{A string passed to \code{\link[=.switch_theme]{.switch_theme()}}
(\code{"view"}, \code{"save"}, \code{"shiny"}).}

\item{...}{Additional arguments passed to \code{\link[=.switch_theme]{.switch_theme()}}.}
}
\value{
A ggplot object.
}
\description{
Visualise a \code{Triangle} object as a triangle-style table. Cells are arranged by
period and dev dimensions, and each cell displays the selected metric.

For ratio metrics (\code{ratio}, \code{incr_ratio}), labels can show either the ratio alone or
the ratio together with the associated loss / risk premium amounts.

For amount metrics (\code{loss}, \code{incr_loss}, \code{premium}, \code{incr_premium}, \code{margin}, \code{incr_margin}),
labels show the selected amount only.

For proportion metrics (\code{loss_share}, \code{incr_loss_share}, \code{premium_share}, \code{incr_premium_share}),
labels are displayed as percentages.

The loss ratio is defined as:
\deqn{ratio = loss / premium}

where \code{premium} denotes risk premium rather than written premium.
}
\details{
The x-axis uses the development variable stored in \code{attr(x, "dev")}, and
the y-axis uses the period variable stored in \code{attr(x, "cohort")}.
If either axis variable is a period-like variable such as \code{uy_m}, \code{cy_m},
\code{uy_q}, \code{cy_q}, \code{uy_h}, \code{cy_h}, \code{uy}, or \code{cy}, it is formatted using
\code{\link[=.format_period]{.format_period()}}.

Facets are created from \code{attr(x, "groups")}.

Ratio and proportion values are displayed in percent. Amount values are
displayed in units of 100 million KRW.
}
\examples{
\dontrun{
d <- as_triangle(
  df,
  groups   = "pd_cat_nm",
  cohort   = "uy_m",
  calendar = "cy_m",
  loss     = "incr_loss",
  premium  = "incr_premium"
)

plot_triangle(d)
plot_triangle(d, metric = "ratio")
plot_triangle(d, metric = "loss")
plot_triangle(d, metric = "premium")
plot_triangle(d, metric = "loss_share")
plot_triangle(d, metric = "premium_share")
plot_triangle(d, label_style = "value")
plot_triangle(d, label_style = "detail")
}

}
