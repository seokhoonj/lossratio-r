% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/triangle-vis.R
\name{plot_triangle.Triangle}
\alias{plot_triangle.Triangle}
\title{Plot development values as a triangle table}
\usage{
\method{plot_triangle}{Triangle}(
  x,
  type = c("value", "usage"),
  value_var = "lr",
  label_style = c("value", "detail"),
  label_size = NULL,
  amount_divisor = 1e+08,
  nrow = NULL,
  ncol = NULL,
  theme = c("view", "save", "shiny"),
  ...
)
}
\arguments{
\item{x}{An object of class \code{Triangle}.}

\item{type}{Plot type. One of:
\describe{
\item{"value"}{(default) Per-cell metric heatmap controlled by
\code{value_var}, \code{label_style}, \code{amount_divisor}, \code{nrow}, \code{ncol}.}
\item{"usage"}{Cell-status heatmap (fit_data / held_out / excluded /
future). Accepts \code{recent}, \code{regime_break}, \code{holdout}, \code{maturity_args}
via \code{...}. See \code{vignette("regime-break-filter")} for details.}
}}

\item{value_var}{A single metric to plot. Must be one of:
\code{"lr"}, \code{"lr_incr"},
\code{"loss"}, \code{"loss_incr"}, \code{"premium"}, \code{"premium_incr"}, \code{"margin"}, \code{"margin_incr"},
\code{"loss_prop"}, \code{"loss_incr_prop"}, \code{"premium_prop"}, or \code{"premium_incr_prop"}.}

\item{label_style}{Label display style. One of:
\describe{
\item{"value"}{Show only the selected metric.}
\item{"detail"}{For \code{lr} / \code{lr}, show the ratio in percent and, on the
next line, the associated loss / premium amounts. For amount and
proportion metrics, this falls back to \code{"value"}.}
}}

\item{label_size}{Numeric label text size forwarded to
\code{\link[ggshort:ggtable]{ggshort::ggtable()}}. Defaults to \code{3} for \code{label_style = "value"}
and \code{2.5} for \code{label_style = "detail"} (two-line labels need a
smaller size to fit). Other label appearance fields (family,
color, hjust, ...) fall back to ggshort defaults.}

\item{amount_divisor}{Numeric scaling factor applied to amount variables
(e.g., \code{loss}, \code{loss_incr}, \code{premium}, \code{premium_incr}, \code{margin}, \code{margin_incr}) before plotting.
Default is \code{1e8}}

\item{nrow, ncol}{Number of rows and columns passed to \code{\link[ggplot2:facet_wrap]{ggplot2::facet_wrap()}}.}

\item{theme}{A string passed to \code{\link[=.switch_theme]{.switch_theme()}}
(\code{"view"}, \code{"save"}, \code{"shiny"}).}

\item{...}{Additional arguments passed to \code{\link[=.switch_theme]{.switch_theme()}}.}
}
\value{
A ggplot object.
}
\description{
Visualise a \code{Triangle} object as a triangle-style table. Cells are arranged by
period and dev dimensions, and each cell displays the selected metric.

For ratio metrics (\code{lr}, \code{lr}), labels can show either the ratio alone or
the ratio together with the associated loss / risk premium amounts.

For amount metrics (\code{loss}, \code{loss_incr}, \code{premium}, \code{premium_incr}, \code{margin}, \code{margin_incr}),
labels show the selected amount only.

For proportion metrics (\code{loss_prop}, \code{loss_incr_prop}, \code{premium_prop}, \code{premium_incr_prop}),
labels are displayed as percentages.

The loss ratio is defined as:
\deqn{lr = loss / premium}

where \code{premium} denotes risk premium rather than written premium.
}
\details{
The x-axis uses the development variable stored in \code{attr(x, "dev_var")}, and
the y-axis uses the period variable stored in \code{attr(x, "cohort_var")}.
If either axis variable is a period-like variable such as \code{uy_m}, \code{cy_m},
\code{uy_q}, \code{cy_q}, \code{uy_s}, \code{cy_s}, \code{uy_a}, or \code{cy_a}, it is formatted using
\code{\link[=.format_period]{.format_period()}}.

Facets are created from \code{attr(x, "group_var")}.

Ratio and proportion values are displayed in percent. Amount values are
displayed in units of 100 million KRW.
}
\examples{
\dontrun{
d <- build_triangle(df, group_var = pd_cat_nm)

plot_triangle(d)
plot_triangle(d, value_var = "lr")
plot_triangle(d, value_var = "loss")
plot_triangle(d, value_var = "premium")
plot_triangle(d, value_var = "loss_prop")
plot_triangle(d, value_var = "premium_prop")
plot_triangle(d, label_style = "value")
plot_triangle(d, label_style = "detail")
}

}
