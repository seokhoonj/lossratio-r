% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ata.R
\name{.filter_ata}
\alias{.filter_ata}
\title{Filter and fill age-to-age factors for projection}
\usage{
.filter_ata(
  ata_summary,
  maturity = NULL,
  use_maturity = FALSE,
  grp = character(0),
  na_method = c("locf", "none")
)
}
\arguments{
\item{ata_summary}{A \code{data.table} of class \code{"ATASummary"} from
\code{\link[=summary.Link]{summary.Link()}} with \code{model = "ata"}.}

\item{maturity}{A \code{data.table} from \code{\link[=detect_maturity]{detect_maturity()}}, or \code{NULL}
when \code{use_maturity = FALSE}.}

\item{use_maturity}{Logical; if \code{TRUE}, apply the maturity filter.
When \code{FALSE}, \code{maturity} is ignored entirely.}

\item{grp}{Character vector of grouping variable names.}

\item{na_method}{One of \code{"locf"} or \code{"none"}.}
}
\value{
A \code{data.table} with \code{selected} and \code{f_selected} columns added.
}
\description{
Internal helper that produces a \code{f_selected} column by applying two steps:
\enumerate{
\item \strong{Filter} — when \code{use_maturity = TRUE}, development links that precede
the maturity point are excluded (\code{f_selected} set to \code{NA}).
\item \strong{Fill} — \code{NA} values in \code{f_selected} are forward-filled using LOCF,
so that every link used in projection has a finite factor.
}
}
\keyword{internal}
