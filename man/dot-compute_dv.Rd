% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lr-stability.R
\name{.compute_dv}
\alias{.compute_dv}
\title{Robust cross-cohort dispersion of incremental loss ratio}
\usage{
.compute_dv(triangle, min_n_cohorts = 5L)
}
\arguments{
\item{triangle}{A \code{Triangle} object.}

\item{min_n_cohorts}{Minimum number of cohorts required to compute
\code{D_v}; below this threshold the row is flagged \code{"sparse"} and \code{D_v}
is \code{NA}. Default \code{5L}.}
}
\value{
data.table with columns \code{dev}, \code{n_cohorts}, \code{median_lr},
\code{mad_lr}, \code{D_v}, \code{flag} (and grouping columns when present).
}
\description{
Internal helper. For each (group, dev) cell of a \code{Triangle}, computes a
robust scale-invariant dispersion of incremental loss ratio across
cohorts:

\deqn{\widehat{D}_v = \frac{1.4826 \cdot \mathrm{MAD}_i(lr_{i,v})}{|\mathrm{median}_i(lr_{i,v})|}}

Operating on \code{lr} (incremental) rather than \code{clr} keeps the metric
inertia-free.
}
\keyword{internal}
