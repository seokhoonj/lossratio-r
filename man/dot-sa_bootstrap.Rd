% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{.sa_bootstrap}
\alias{.sa_bootstrap}
\title{Per-cohort SA parametric bootstrap (ED before maturity, CL after)}
\usage{
.sa_bootstrap(
  target_obs,
  target_proj,
  exposure_proj,
  g_sel,
  f_sel,
  g_sigma2,
  f_sigma2,
  g_var,
  f_var,
  last_obs,
  maturity_from,
  B,
  alpha,
  process = c("normal")
)
}
\arguments{
\item{target_obs, target_proj}{See \link{.cl_bootstrap}.}

\item{exposure_proj}{Numeric vector of projected exposure (premium) by
dev; supplies the \code{P_k} multiplier for the ED intensity.}

\item{g_sel, g_sigma2, g_var}{Per-dev intensity / Mack-sigma^2 /
Var(g_hat) vectors.}

\item{f_sel, f_sigma2, f_var}{CL factor / Mack-sigma^2 / Var(f_hat).}

\item{last_obs, B, alpha, process}{See \link{.cl_bootstrap}.}

\item{maturity_from}{Source-dev index k\* where the SA boundary lies.
Cells with \code{k < maturity_from} are in the ED phase; cells with
\code{k >= maturity_from} are in the CL phase.}
}
\value{
A \verb{[length(target_proj) x B]} numeric matrix.
}
\description{
Simulates the stage-adaptive projection path: cells with dev source
\code{k < maturity_from} use the ED additive recursion; cells with
\code{k >= maturity_from} use the CL multiplicative recursion. When
\code{maturity_from} is non-finite, the function degenerates to pure ED.
}
\keyword{internal}
