% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ed.R
\name{fit_ed}
\alias{fit_ed}
\title{Fit ED intensity factors}
\usage{
fit_ed(
  x,
  value_var = "closs",
  exposure_var = "crp",
  method = c("basic", "mack"),
  alpha = 1,
  na_method = c("zero", "locf", "none"),
  sigma_method = c("min_last2", "locf", "loglinear"),
  recent = NULL,
  regime_break = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{Link} object built with \code{exposure_var} set, typically
produced by \code{\link[=build_link]{build_link()}}.}

\item{value_var}{Cumulative loss variable. Default \code{"closs"}.
Forwarded to \code{\link[=build_link]{build_link()}}.}

\item{exposure_var}{Cumulative exposure variable. Default \code{"crp"}.
Forwarded to \code{\link[=build_link]{build_link()}}.}

\item{method}{One of \code{"basic"} or \code{"mack"}. Default is \code{"basic"}.}

\item{alpha}{Numeric scalar controlling the variance structure. Default
is \code{1}.}

\item{na_method}{Method used to fill \code{NA} values in \code{g_selected}. One
of \code{"zero"} (default, set \code{NA} to 0 meaning no further development)
or \code{"locf"} or \code{"none"}.}

\item{sigma_method}{Method used to extrapolate \code{sigma}. One of
\code{"min_last2"} (default), \code{"locf"}, or \code{"loglinear"}.}

\item{recent}{Optional positive integer. When supplied, only the most
recent \code{recent} periods are used for estimation. Default is \code{NULL}.}

\item{regime_break}{Optional cohort cutoff for the regime break. Accepts:
\code{NULL} (default, no filter), a single \code{Date}/character coercible to Date,
a vector of dates (uses the latest), or a \code{Regime} object (extracts
the latest from \verb{$breakpoints}). When supplied, cohorts with
\code{cohort < break_date} are excluded from estimation. Default is \code{NULL}.}

\item{...}{Additional arguments passed to \code{\link[=summary.Link]{summary.Link()}}.}
}
\value{
An object of class \code{"EDFit"} (a named list).
}
\description{
Estimate incremental loss intensities \eqn{g_k} from a dual-variable
\code{Link} object (built with \code{exposure_var} set) and return an \code{"EDFit"}
object that bundles factor summaries, selected intensities, and
maturity diagnostics.

Two methods are supported via the \code{method} argument:
\describe{
\item{\code{"basic"} (default)}{Factor estimation only. Returns
\code{g_selected} and \code{sigma2} in \verb{$selected}.}
\item{\code{"mack"}}{Basic plus factor variance \eqn{\mathrm{Var}(\hat{g}_k)}
added as \code{g_var} column in \verb{$selected}.}
}
}
\seealso{
\code{\link[=build_link]{build_link()}}, \code{\link[=summary.Link]{summary.Link()}}, \code{\link[=fit_lr]{fit_lr()}}
}
