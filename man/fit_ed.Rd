% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ed.R
\name{fit_ed}
\alias{fit_ed}
\title{Fit ED intensity factors}
\usage{
fit_ed(
  x,
  loss = "loss",
  exposure = "exposure",
  method = c("mack"),
  alpha = 1,
  na_method = c("locf", "zero", "none"),
  sigma_method = c("locf", "min_last2", "loglinear", "mack", "none"),
  recent = NULL,
  regime = NULL,
  bootstrap = NULL,
  B = 999L,
  seed = NULL,
  conf_level = 0.95,
  ...
)
}
\arguments{
\item{x}{A \code{"Triangle"} object.}

\item{loss}{Cumulative loss variable. Default \code{"loss"}.
Forwarded to \code{\link[=as_link]{as_link()}} and to downstream workers.}

\item{exposure}{Cumulative exposure variable. Default \code{"exposure"}.
Forwarded to \code{\link[=as_link]{as_link()}} and to downstream workers.}

\item{method}{Estimation method. Currently only \code{"mack"} is supported.}

\item{alpha}{Numeric scalar controlling the variance structure. Default
is \code{1}.}

\item{na_method}{Method used to fill \code{NA} values in \code{g_sel}. One
of \code{"zero"} (default, set \code{NA} to 0 meaning no further development)
or \code{"locf"} or \code{"none"}.}

\item{sigma_method}{Method used to extrapolate \code{sigma} for links where it
cannot be estimated. One of \code{"locf"} (default), \code{"min_last2"},
\code{"loglinear"}, \code{"mack"}, or \code{"none"}. \code{"mack"} applies the Mack
(1993, Appendix B) tail estimator to the last unestimated link only,
falling back to LOCF for any earlier ones with a warning. \code{"none"}
performs no extrapolation; \code{sigma} stays \code{NA} and downstream variance
terms drop those links via finite-value guards. Passed to
\code{\link[=.extrapolate_sigma_ata]{.extrapolate_sigma_ata()}}.}

\item{recent}{Optional positive integer. When supplied, only the most
recent \code{recent} periods are used for estimation. Default is \code{NULL}.}

\item{regime}{Optional regime specification for cohort cutoff. Accepts:
\code{NULL} (default -- no filter), a \code{"Regime"} object (from
\code{\link[=detect_regime]{detect_regime()}}), the string \code{"auto"} (internal
\code{detect_regime(tri, loss = "ratio")} call), or a function
\verb{function(tri) -> Regime}. Resolved internally via
\code{\link[=.resolve_regime]{.resolve_regime()}}. When supplied, cohorts with
\code{cohort < change_date} are excluded from estimation. Default is \code{NULL}.}

\item{bootstrap}{Optional bootstrap specification. Accepts \code{NULL}
(default, analytical Mack SE only), a \code{BootstrapTriangle} object
produced by \code{\link[=bootstrap]{bootstrap()}} (replayed for SE / CI), or the string
\code{"auto"} to run an internal nonparametric bootstrap at fit time.}

\item{B}{Integer number of bootstrap replicates when \code{bootstrap = "auto"}.
Default \code{999L}.}

\item{seed}{Optional integer seed for reproducible bootstrap draws.
Default \code{NULL}.}

\item{conf_level}{Numeric in \verb{(0, 1)}. Confidence level used for
bootstrap-derived CI columns. Default \code{0.95}.}

\item{...}{Additional arguments passed to \code{\link[=summary.Link]{summary.Link()}}.}
}
\value{
An object of class \code{"EDFit"} (a named list) with components:
\describe{
\item{\code{factor}}{\code{EDSummary} of fitted intensities per development link.}
\item{\code{selected}}{\code{data.table} of selected \code{g_sel}, \code{sigma2},
and \code{g_var}.}
\item{\code{full}}{\code{data.table} of per-cell cumulative loss / exposure
projection plus role-prefixed SE / CV columns
(\code{loss_proj}, \code{incr_loss_proj}, \code{exposure_proj},
\code{incr_exposure_proj}, \code{loss_proc_se2}, \code{loss_param_se2},
\code{loss_total_se2}, \code{loss_proc_se}, \code{loss_param_se},
\code{loss_total_se}, \code{loss_total_cv}). Available cells include
both observed and projected; \code{is_observed} flags observed cells.}
\item{\code{link}}{\code{Link} object used for factor estimation.}
}
}
\description{
Estimate incremental loss intensities \eqn{g_k} from a \code{"Triangle"}
object and return an \code{"EDFit"} object that bundles factor summaries,
selected intensities, and a cell-level projection of cumulative loss
and exposure (\verb{$full}).

Returns \code{g_sel}, \code{sigma2}, and factor variance
\eqn{\mathrm{Var}(\hat{g}_k)} (column \code{g_var}) in \verb{$selected}.

The \verb{$full} projection table holds cumulative loss / exposure
projections and their standard errors, computed directly from the
Mack-style ED recursion (see \code{.ed_proj}, \code{.ed_proc_var},
\code{.ed_param_var}). To validate an ED projection via \code{\link[=backtest]{backtest()}},
call \code{backtest(tri, target = "ratio", loss_method = "ed")}.
}
\seealso{
\code{\link[=as_link]{as_link()}}, \code{\link[=summary.Link]{summary.Link()}}, \code{\link[=fit_ratio]{fit_ratio()}}, \code{\link[=backtest]{backtest()}}
}
