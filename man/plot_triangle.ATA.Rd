% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ata-vis.R
\name{plot_triangle.ATA}
\alias{plot_triangle.ATA}
\title{Plot ata factors as a triangle heatmap table}
\usage{
\method{plot_triangle}{ATA}(
  x,
  label_style = c("value", "detail"),
  label_args = list(),
  show_maturity = FALSE,
  alpha = 1,
  cv_threshold = 0.1,
  rse_threshold = 0.05,
  min_valid_ratio = 0.5,
  min_n_valid = 3L,
  min_run = 1L,
  amount_divisor = 1e+08,
  theme = c("view", "save", "shiny"),
  nrow = NULL,
  ncol = NULL,
  x.angle = 90,
  ...
)
}
\arguments{
\item{x}{An object of class \code{"ATA"}.}

\item{label_style}{Label display style. One of \code{"value"} or \code{"detail"}.}

\item{label_args}{A named list of arguments controlling cell label
appearance, passed to \code{\link[ggshort:ggheatmap]{ggshort::ggheatmap()}}. Recognised elements are
\code{family}, \code{size}, \code{angle}, \code{hjust}, \code{vjust}, and \code{color}. Any element
not supplied falls back to the \code{\link[ggshort:ggheatmap]{ggshort::ggheatmap()}} default.}

\item{show_maturity}{Logical; if \code{TRUE}, compute the maturity point and
draw a vertical reference line and label. Default is \code{FALSE}.}

\item{alpha}{Numeric scalar controlling the variance structure in the
WLS fit. Default is \code{1}. Passed to \code{\link[=summary.ATA]{summary.ATA()}}.}

\item{cv_threshold}{Maximum allowed coefficient of variation. Used when
\code{show_maturity = TRUE}. Default is \code{0.10}.}

\item{rse_threshold}{Maximum allowed relative standard error. Used when
\code{show_maturity = TRUE}. Default is \code{0.05}.}

\item{min_valid_ratio}{Minimum proportion of finite ata values required.
Default is \code{0.5}.}

\item{min_n_valid}{Minimum number of finite ata factors required.
Default is \code{3L}.}

\item{min_run}{Minimum number of consecutive mature ata links required.
Default is \code{1L}.}

\item{amount_divisor}{Numeric scaling divisor for amount display in
\code{label_style = "detail"}. Default is \code{1e8}.}

\item{theme}{A string passed to \code{\link[=.switch_theme]{.switch_theme()}}.}

\item{nrow, ncol}{Number of rows and columns for \code{\link[ggplot2:facet_wrap]{ggplot2::facet_wrap()}}.}

\item{x.angle}{Numeric angle for x-axis tick labels. Default is \code{90}
to prevent overlap of the \code{from-to} link labels.}

\item{...}{Additional arguments passed to \code{\link[=.switch_theme]{.switch_theme()}}.}
}
\value{
A ggplot object.
}
\description{
Visualise an \code{"ATA"} object as a triangle-style heatmap. Cells are
arranged by period and ata link, and fill colours are based on
\code{log(ata / median(ata))} within each link, highlighting relative
deviations column-wise.
}
\seealso{
\code{\link[=build_ata]{build_ata()}}, \code{\link[=summary.ATA]{summary.ATA()}}, \code{\link[=find_ata_maturity]{find_ata_maturity()}}
}
