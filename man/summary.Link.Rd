% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/link.R
\name{summary.Link}
\alias{summary.Link}
\title{Summarise a \code{Link} table}
\usage{
\method{summary}{Link}(object, model = NULL, alpha = 1, digits = NULL, ...)
}
\arguments{
\item{object}{A \code{Link} object from \code{\link[=build_link]{build_link()}}.}

\item{model}{Either \code{"ata"} (multiplicative chain-ladder factors) or
\code{"ed"} (additive exposure-driven intensities). When \code{model = "ed"},
the link table must have been built with \code{exposure_var} set. The
default uses \code{"ed"} if \code{attr(object, "exposure_var")} is non-\code{NULL},
otherwise \code{"ata"}.}

\item{alpha, digits, ...}{Forwarded to the underlying summary helper.}
}
\value{
Either an \code{ATASummary} (model = \code{"ata"}) or \code{EDSummary}
(model = \code{"ed"}) \code{data.table}.
}
\description{
Dispatch to the appropriate diagnostic table based on \code{model}.
}
\seealso{
\code{\link[=build_link]{build_link()}}, \code{\link[=find_maturity]{find_maturity()}}
}
