% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/triangle.R
\name{as_calendar}
\alias{as_calendar}
\title{Coerce experience data to a Calendar object}
\usage{
as_calendar(x)
}
\arguments{
\item{x}{A \code{Triangle} object (typically from \code{\link[=as_triangle]{as_triangle()}}).}
}
\value{
A data.frame with class \code{"Calendar"}, containing the following
derived columns:
\describe{
\item{cal_idx}{Sequential calendar-period index within each group
(\verb{1, 2, ..., N}). Time-series convention; intentionally NOT
\code{dev} -- in a Calendar the integer is just the rank of the date
within its group, not a true development period (\code{cym - uym}).
Useful for aligning groups with different starting periods on a
common index scale.}
\item{loss, incr_loss}{Cumulative and per-period loss}
\item{exposure, incr_exposure}{Cumulative and per-period exposure}
\item{ratio, incr_ratio}{Cumulative and per-period loss ratio}
\item{margin, incr_margin}{Cumulative and per-period margin}
\item{profit, incr_profit}{Profit indicator}
\item{loss_share, incr_loss_share, exposure_share, incr_exposure_share}{
Proportions within each \code{calendar} cell}
}

The returned object also has an attribute \code{"longer"} containing
a melted long-format version (\code{class = "CalendarLonger"}).
}
\description{
Validate raw experience data, aggregate it along a single
calendar-period axis, and assign the \code{Calendar} S3 class so the
associated \code{plot.Calendar()} / \code{summary.Calendar()} / longer-form
methods dispatch on the result.

Compared with \code{\link[=as_triangle]{as_triangle()}}, which builds a \emph{two-dimensional}
\verb{cohort x dev} structure, \code{as_calendar()} is \emph{one-dimensional}: a
single calendar-period time series (per group) showing how the
portfolio evolves through time, regardless of cohort membership.

The result is a long-format \code{data.table} with class
\code{c("Calendar", "data.table", "data.frame")} containing cumulative
loss / premium, incremental and cumulative LR, margin, profit, and
share columns within each \code{calendar} cell.

The cumulative loss ratio is defined as:
\deqn{ratio = loss / exposure}

For long-term health insurance applications, risk premium is commonly
used as the \code{exposure} measure.

Proportion variables are computed within each \code{calendar} cell:
\itemize{
\item \code{incr_loss_share     = incr_loss     / sum(incr_loss)}
\item \code{incr_exposure_share = incr_exposure / sum(incr_exposure)}
\item \code{loss_share          = loss          / sum(loss)}
\item \code{exposure_share      = exposure      / sum(exposure)}
}

Therefore, for a fixed \code{calendar} cell, the proportions
sum to 1 across groups. These are useful for examining the composition of
each calendar period across products or other grouping variables.

Calendar derives \code{calendar = cohort + (dev - 1)} using the
Triangle's \code{grain} attribute and aggregates the incremental
\code{loss} / \code{exposure} columns by \verb{(groups, calendar)}. This works for
Triangles built in either mode (with or without an original
\code{calendar} column in the raw experience), since \code{cohort + dev} is
always sufficient to reconstruct the calendar axis at the
Triangle's grain.
}
\examples{
\dontrun{
tri <- as_triangle(
  experience,
  groups   = "coverage",
  cohort   = "uy_m",
  calendar = "cy_m",
  loss     = "incr_loss",
  exposure = "incr_exposure"
)

cal <- as_calendar(tri)
head(cal)
attr(cal, "longer")
}

}
