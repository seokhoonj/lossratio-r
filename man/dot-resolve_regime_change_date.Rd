% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.resolve_regime_change_date}
\alias{.resolve_regime_change_date}
\title{Resolve a regime specifier to a single Date}
\usage{
.resolve_regime_change_date(regime, by = NULL)
}
\arguments{
\item{regime}{See \code{\link[=.apply_regime_filter]{.apply_regime_filter()}}.}

\item{by}{Optional character vector of group columns the caller wants
the change date dispatched on. When \code{NULL} (default) or empty, the
function always returns a scalar (the maximum change date),
preserving the historical single-value contract. When non-empty and
\code{regime} is a multi-group \code{Regime} whose \verb{$groups} intersect
\code{by}, returns a \code{data.table} with \verb{[intersect(by, regime$groups)..., change_date]} (one row per group combo, holding \code{max(change)}).
Otherwise falls back to scalar.}
}
\value{
One of:
\itemize{
\item \code{NULL} when no change date is specified.
\item A single Date (the latest change) -- the scalar path.
\item A \code{data.table} \verb{[join_cols..., change_date]} -- the per-group path.
}
}
\description{
Internal helper used by \code{\link[=.apply_regime_filter]{.apply_regime_filter()}} to coerce a
heterogeneous \code{regime} argument (NULL, Date scalar/vector,
character coercible to Date, or a \code{Regime} object) into either a
single Date scalar or a per-group \code{data.table} keyed by the
caller-supplied \code{by} columns.
}
\keyword{internal}
