% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ata.R
\name{find_ata_maturity}
\alias{find_ata_maturity}
\title{Find ata maturity by group}
\usage{
find_ata_maturity(
  x,
  cv_threshold = 0.1,
  rse_threshold = 0.05,
  min_valid_ratio = 0.5,
  min_n_valid = 3L,
  min_run = 1L
)
}
\arguments{
\item{x}{An object of class \code{"ATASummary"}, typically produced by
\code{\link[=summary.Link]{summary.Link()}} with \code{model = "ata"}.}

\item{cv_threshold}{Maximum allowed coefficient of variation.
Default is \code{0.10}.}

\item{rse_threshold}{Maximum allowed relative standard error.
Default is \code{0.05}.}

\item{min_valid_ratio}{Minimum proportion of finite ata values required.
Default is \code{0.5}.}

\item{min_n_valid}{Minimum number of finite ata factors required.
Default is \code{3L}.}

\item{min_run}{Minimum number of consecutive ata links satisfying the
maturity criteria. Default is \code{1L}.}
}
\value{
A \code{data.table} with class \code{"ATAMaturity"} containing one row
per group. If no mature link is found, all values for that group are
\code{NA}.
}
\description{
Identify the first mature age-to-age (ata) link from an object of class
\code{"ATASummary"}, typically produced by \code{\link[=summary.Link]{summary.Link()}} with
\code{model = "ata"}.

Maturity is determined using a combination of:
\itemize{
\item \code{cv < cv_threshold}
\item \code{rse < rse_threshold}
\item \code{valid_ratio >= min_valid_ratio}
\item \code{n_valid >= min_n_valid}
\item optional consecutive maturity over \code{min_run} ata links
}

Both \code{cv} and \code{rse} must be satisfied simultaneously. \code{cv} captures
the raw variability of observed ata factors across cohorts, while \code{rse}
reflects the precision of the WLS-estimated factor. Using both criteria
together provides a more robust maturity assessment than either alone.
}
