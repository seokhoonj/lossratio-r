% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maturity.R
\name{.rebucket_triangle_groups}
\alias{.rebucket_triangle_groups}
\title{Internal: rebucket a \code{Triangle} to a coarser \code{groups} partition}
\usage{
.rebucket_triangle_groups(x, groups)
}
\arguments{
\item{x}{A \code{Triangle} object.}

\item{groups}{\code{NULL}, \code{character(0)}, or a \code{character} subset of
\code{attr(x, "groups")}. \code{NULL} returns \code{x} unchanged.}
}
\value{
A \code{Triangle} with \code{attr(., "groups")} set to the requested
value and \code{loss} / \code{premium} / \code{ratio} aggregated to the requested
partition.
}
\description{
Re-aggregates \code{loss} / \code{premium} / \code{incr_loss} / \code{incr_premium} over
the dropped grouping columns and recomputes \code{ratio} / \code{incr_ratio} as
ratios of the aggregated totals. Other cell-level columns
(\code{margin}, \code{profit}, \code{loss_share}, ...) are not regenerated -- the
rebucketed object is intended for \code{as_link()} consumption only.
}
\keyword{internal}
