% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.apply_recent_filter}
\alias{.apply_recent_filter}
\title{Filter a long-format table to recent calendar diagonals}
\usage{
.apply_recent_filter(
  dt,
  recent,
  grp = character(0),
  coh,
  dev,
  dev_split = NULL
)
}
\arguments{
\item{dt}{A long-format development \code{data.table}.}

\item{recent}{Positive integer or \code{NULL}. When \code{NULL} or missing, \code{dt}
is returned unchanged.}

\item{grp}{Character vector of group columns (may be empty).}

\item{coh}{Single column name for the cohort variable (e.g. \code{cohort}).}

\item{dev}{Single column name for the development variable (e.g. \code{dev}
for \code{Triangle} objects, or \code{ata_from} for \code{ATA}/\code{ED} objects).}

\item{dev_split}{Optional numeric scalar — the maturity target dev
(= \code{ata_to}, equivalently the first CL-region dev). When supplied,
the recent filter is applied only to rows where \code{dev >= dev_split}
(CL region); rows with \code{dev < dev_split} (ED region) are kept
unconditionally.}
}
\value{
A filtered copy of \code{dt} (class preserved), keeping only rows
within the recent-diagonal window.
}
\description{
Internal long-format analogue of \code{\link[=get_recent_weights]{get_recent_weights()}}. Returns a
subset of the input \code{data.table} containing only rows whose calendar
position falls within the last \code{recent} calendar diagonals of its
group.

The matrix-form condition \code{row + col >= m - recent + 2} is translated
to the group-wise long-form condition
\code{rank(cohort) + dev - 1 > max(rank(cohort) + dev - 1) - recent}.
}
\keyword{internal}
