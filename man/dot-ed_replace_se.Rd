% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exposure.R
\name{.ed_replace_se}
\alias{.ed_replace_se}
\title{Replace CL multiplicative SE with ED additive SE on a CLFit's \verb{$full}}
\usage{
.ed_replace_se(full, selected, triangle)
}
\arguments{
\item{full}{The \verb{$full} data.table from a \code{CLFit} (must contain
\code{cohort}, \code{dev}, \code{loss_obs}, \code{loss_proj}).}

\item{selected}{The \verb{$selected} data.table (must contain \code{f_sel},
\code{sigma2}, \code{f_var}).}

\item{triangle}{The original \code{Triangle} (for \code{groups} attribute).}
}
\value{
Updated \code{full} data.table with \code{loss_proc_se2},
\code{loss_param_se2}, \code{loss_total_se2}, \code{loss_proc_se},
\code{loss_param_se}, \code{loss_total_se}, \code{loss_proc_cv},
\code{loss_param_cv}, \code{loss_total_cv} columns rebuilt under the ED
recursion (column names match the upstream \code{fit_cl} worker
convention; the dispatcher renames them to \verb{exposure_*} afterwards).
}
\description{
Point projection (\code{loss_proj}) is preserved -- it is identical under
both CL and self-weighted ED. Only the variance accumulation differs:

\itemize{
\item CL recursion: \verb{proc_\{k+1\} = f^2 * proc_k + sigma^2 * C_k}
(multiplicative scaling -- prior variance amplified by f^2 each
step).
\item ED recursion: \verb{proc_\{k+1\} = proc_k + sigma^2 * C_k} (additive
-- prior variance carried forward unchanged).
}

Both share the same per-link \code{sigma^2} and \code{f_var} (= \code{Var(f_hat_k)})
estimates. The recursion is per (group, cohort).
}
\keyword{internal}
