% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lr.R
\name{.lr_bootstrap_cohort}
\alias{.lr_bootstrap_cohort}
\title{LR-level bootstrap CI / SE composition for one cohort}
\usage{
.lr_bootstrap_cohort(
  loss_obs,
  loss_proj,
  prem_proj,
  g_sel,
  f_sel,
  g_sigma2,
  f_sigma2,
  g_var,
  f_var,
  last_obs,
  maturity_from,
  B,
  loss_alpha,
  method,
  probs,
  process = "normal"
)
}
\value{
A list of six numeric vectors aligned with the input cohort
rows: \code{lr_ci_lo}, \code{lr_ci_hi}, \code{lr_se}, \code{loss_ci_lo}, \code{loss_ci_hi},
\code{loss_total_se}.
}
\description{
Internal helper for \code{\link[=fit_lr]{fit_lr()}} bootstrap mode. Composes per-dev
bootstrap CI / SE for both \emph{cumulative loss} and \emph{loss ratio} from a
single simulation of the loss path. Premium is treated as known
(fixed \code{prem_proj}), so LR uncertainty inherits from loss uncertainty
divided by the fixed premium projection -- the \code{se_method = "fixed"}
analytical analogue.

Dispatches to one of the single-role per-cohort simulators based on
\code{method}:
\describe{
\item{\code{"sa"}}{\link{.sa_bootstrap} -- ED before maturity, CL after.}
\item{\code{"ed"}}{\link{.ed_bootstrap} -- additive ED throughout.}
\item{\code{"cl"}}{\link{.cl_bootstrap} -- multiplicative CL throughout.}
}
}
\keyword{internal}
