% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/triangle-vis.R
\name{.compute_triangle_usage}
\alias{.compute_triangle_usage}
\title{Compute cell-level data-usage status for a Triangle}
\usage{
.compute_triangle_usage(
  x,
  recent = NULL,
  regime_break = NULL,
  holdout = NULL,
  mat_k = NULL
)
}
\arguments{
\item{x}{A \code{Triangle} object.}

\item{recent}{Optional positive integer (calendar-diagonal cut), or
\code{NULL}.}

\item{regime_break}{Optional cohort cutoff. Accepts the same input
forms as \code{\link[=fit_lr]{fit_lr()}} (\code{NULL}, \code{Date}, character, vector, or
\code{CohortRegime}).}

\item{holdout}{Optional positive integer. When supplied, the last
\code{holdout} calendar diagonals are flagged \code{"held_out"}.}

\item{mat_k}{Optional integer. When both \code{recent} and \code{regime_break}
are provided, the hybrid mask uses \code{mat_k} as the maturity switch:
cells with \code{dev <= mat_k} apply the cohort cut, cells with
\code{dev > mat_k} apply the calendar-diagonal cut. When \code{NULL}, the
hybrid logic falls back to applying both filters jointly (cohort
cut AND recent cut).}
}
\value{
A \code{data.table} with one row per \verb{(group, cohort, dev)} cell
spanning the full triangle (observed plus future). Columns include
group columns (if any), \code{cohort}, \code{dev}, \code{.coh_rank}, \code{.cal_idx},
\code{.max_cal}, \code{is_observed}, \code{is_held_out}, \code{is_fit_data},
\code{is_excluded}, and \code{status} (factor).
}
\description{
Internal helper that classifies every \verb{(group, cohort, dev)} cell of a
\code{Triangle} into one of four buckets given a fit-data filter
configuration: \code{"fit_data"}, \code{"held_out"}, \code{"excluded"}, or \code{"future"}.

Mask precedence: \code{held_out} > \code{fit_data} > \code{excluded} > \code{future}.
}
\keyword{internal}
