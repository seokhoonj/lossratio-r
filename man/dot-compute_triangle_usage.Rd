% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/triangle-vis.R
\name{.compute_triangle_usage}
\alias{.compute_triangle_usage}
\title{Compute cell-level data-usage status for a Triangle}
\usage{
.compute_triangle_usage(
  x,
  recent = NULL,
  regime = NULL,
  holdout = NULL,
  m_k = NULL,
  m_k_dt = NULL
)
}
\arguments{
\item{x}{A \code{Triangle} object.}

\item{recent}{Optional positive integer (calendar-diagonal cut), or
\code{NULL}.}

\item{regime}{Optional cohort cutoff. Accepts the same input
forms handled by \code{\link[=.resolve_regime_change_date]{.resolve_regime_change_date()}} (\code{NULL}, \code{Date}, character,
vector, or \code{Regime}).}

\item{holdout}{Optional positive integer. When supplied, the last
\code{holdout} calendar diagonals are flagged \code{"holdout"}. The \code{recent}
filter is then evaluated against the post-holdout boundary so the
recent wedge sits \emph{before} the holdout wedge (no overlap), matching
\code{backtest()} semantics -- the internal fitter operates on the masked
triangle whose own max_cal is \code{original - holdout}.}

\item{m_k}{Optional integer. The maturity switch as a \emph{target}
development index (= \code{change} of the first stable link). When
both \code{recent} and \code{regime} are provided, the hybrid mask uses
\code{m_k} as the boundary: cells with \code{dev < m_k} apply the cohort
cut, cells with \code{dev >= m_k} apply the calendar-diagonal cut.
When \code{NULL}, the hybrid logic falls back to applying both filters
jointly (cohort cut AND recent cut).}
}
\value{
A \code{data.table} with one row per \verb{(group, cohort, dev)} cell
spanning the full triangle (observed plus future). Columns include
group columns (if any), \code{cohort}, \code{dev}, \code{.coh_rank}, \code{.cal_idx},
\code{.max_cal}, \code{is_observed}, \code{is_held_out}, \code{is_fit_data},
\code{is_excluded}, and \code{status} (factor).
}
\description{
Internal helper that classifies every \verb{(group, cohort, dev)} cell of a
\code{Triangle} into one of four buckets given a fit-data filter
configuration: \code{"used"}, \code{"holdout"}, \code{"unused"}, or \code{"future"}.

Mask precedence: \code{holdout} > \code{used} > \code{unused} > \code{future}.
}
\keyword{internal}
