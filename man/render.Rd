% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render.R
\name{render}
\alias{render}
\alias{render.default}
\alias{render.data.frame}
\alias{render.data.table}
\alias{render.tbl_df}
\alias{render.Triangle}
\alias{print.Triangle}
\title{Render a tabular object as a compact console table}
\usage{
render(x, ...)

\method{render}{default}(x, ...)

\method{render}{data.frame}(
  x,
  n = 10,
  width = getOption("width", 100),
  max_col_width = 14,
  verbose = TRUE,
  ...
)

\method{render}{data.table}(
  x,
  n = 10,
  width = getOption("width", 100),
  max_col_width = 14,
  verbose = TRUE,
  ...
)

\method{render}{tbl_df}(
  x,
  n = 10,
  width = getOption("width", 100),
  max_col_width = 14,
  verbose = TRUE,
  ...
)

\method{render}{Triangle}(
  x,
  n = 10,
  width = getOption("width", 100),
  max_col_width = 14,
  verbose = TRUE,
  ...
)

\method{print}{Triangle}(x, ...)
}
\arguments{
\item{x}{An object to render. Methods are provided for data frames,
data tables, tibbles, and Triangle objects; the default method
falls back to \code{\link[=print]{print()}}.}

\item{...}{Additional arguments passed to methods.}

\item{n}{Integer; the maximum number of data rows to show before
head / tail sampling applies. Default \code{10}.}

\item{width}{Integer; the target console width in characters.
Default \code{getOption("width", 100)}.}

\item{max_col_width}{Integer; the maximum width of a single column --
longer values are truncated with an ellipsis. Default \code{14}.}

\item{verbose}{Logical; whether to print the "more variables" footer
for columns dropped to fit \code{width}. Default \code{TRUE}.}
}
\value{
The input object \code{x}, invisibly.
}
\description{
\code{render()} prints a data frame, data table, or Triangle object as a
compact, fixed-width console table.

The rendered table has four parts:
\itemize{
\item a \verb{shape: (rows, cols)} header;
\item a box-drawn grid carrying a column-name row and a
column-type row (\verb{<int>}, \verb{<dbl>}, \verb{<date>}, \verb{<chr>}, ...);
\item a head / tail sample of rows -- when the object has more than
\code{n} rows the middle is collapsed to a single ellipsis row;
\item a tibble-style "\code{N} more variables" footer listing the
columns dropped when the table is too wide for the console.
}

The grid, \verb{shape:} header, and per-column type row follow the
dataframe console layout of the package's Python sibling,
\code{lossratio-py}; the truncated-columns footer follows the \code{tibble}
print convention. Both are adopted so that \code{lossratio} previews stay
visually consistent with the wider R and Python tabular ecosystems.

Columns are selected from both ends inward until they fill \code{width};
the dropped middle columns are summarised in the footer.
\code{print.Triangle()} delegates to \code{render()}, so Triangle objects
print in this style by default.
}
\examples{
data(experience)
render(head(experience, 20))

}
