% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.borrow_segment_factors}
\alias{.borrow_segment_factors}
\title{Borrow missing per-segment factors across segments (segment_bridged_borrowed)}
\usage{
.borrow_segment_factors(sel, groups, dev_col, factor_cols)
}
\arguments{
\item{sel}{A \code{data.table} of per-segment factors with columns
\code{groups}, \code{dev_col}, \code{segment_id}, and \code{factor_cols} (plus optional
\code{ata_to} / \code{ata_link} carried along).}

\item{groups}{Character vector of group columns (may be empty).}

\item{dev_col}{Single column name holding the development index
(\code{"dev"} after the projection-time rename).}

\item{factor_cols}{Character vector of factor columns to borrow. The
first element is the \emph{primary} factor (\code{"f_sel"} / \code{"g_sel"}) whose
non-\code{NA} presence defines whether a segment owns a dev.}
}
\value{
\code{sel} augmented with borrowed rows so every \verb{(group, segment, dev)} present in the donor space is covered.
}
\description{
Augments a per-segment factor table so every segment carries a factor
for the full development range, borrowing the entries a segment cannot
estimate from a donor segment that can. Used only by the
\code{"segment_bridged_borrowed"} treatment: early-development factors stay
regime-specific (each segment's own estimate), while late-development
factors a segment never reaches are filled from another segment.

Donor rule (\dQuote{recent}): for each \verb{(group, dev)} the donor is the
segment with the \emph{largest} \code{segment_id} that has a non-\code{NA} primary
factor at that dev -- i.e. the most recent regime whose own cohorts
developed that far. The bridged band guarantees a donor exists at every
dev that any segment needs (the boundary factor gaps are closed by the
bridge), so no dev is left unfilled.

The borrow only \emph{adds} rows for \verb{(segment, dev)} combinations the
segment lacks; rows a segment owns are never overwritten.
}
\keyword{internal}
