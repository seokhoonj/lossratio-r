% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convergence.R
\name{.compute_dispersion}
\alias{.compute_dispersion}
\title{Robust cross-cohort dispersion of incremental loss ratio}
\usage{
.compute_dispersion(triangle, min_n_cohorts = 5L)
}
\arguments{
\item{triangle}{A \code{Triangle} object.}

\item{min_n_cohorts}{Minimum number of cohorts required to compute the
dispersion; below this threshold the row is flagged \code{"sparse"} and
\code{dispersion} is \code{NA}. Default \code{5L}.}
}
\value{
data.table with columns \code{dev}, \code{n_cohorts}, \code{lr_median},
\code{lr_mad}, \code{dispersion}, \code{flag} (and grouping columns when present).
}
\description{
Internal helper. For each (group, dev) cell of a \code{Triangle}, computes a
robust scale-invariant dispersion of incremental loss ratio across
cohorts:

\deqn{\mathrm{dispersion} = \frac{1.4826 \cdot \mathrm{MAD}_i(lr_{i,v})}{|\mathrm{median}_i(lr_{i,v})|}}

Operating on incremental LR keeps the metric inertia-free.
}
\keyword{internal}
