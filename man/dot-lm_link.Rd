% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/link.R
\name{.lm_link}
\alias{.lm_link}
\title{Estimate age-to-age factors via weighted least squares}
\usage{
.lm_link(x, weights = 1, alpha = 1, na_rm = TRUE, tol = 1e-12)
}
\arguments{
\item{x}{An object of class \code{"Link"}.}

\item{weights}{Either a length-one numeric scalar (default \code{1}) or a
single column name present in the \code{Link} data that provides per-row
weights.}

\item{alpha}{Numeric scalar controlling the variance structure. Default
is \code{1}.}

\item{na_rm}{Logical; if \code{TRUE} (default), rows with non-finite or
non-positive \code{value_from} are dropped before fitting. Note that
\code{value_to = 0} is permitted, as zero cumulative values are valid
observations (e.g. no claims yet developed in early development periods).}

\item{tol}{Non-negative numeric scalar. Values below \code{tol} are set to
zero. Default is \code{1e-12}.}
}
\value{
A \code{data.table} with one row per ata link containing \code{f},
\code{f_se}, \code{sigma}, \code{rse}, and \code{n_obs}. \code{rse} is defined as
\eqn{f\_se / f} and represents the relative standard error of the
WLS-estimated factor. \code{rse} is \code{NA} when \code{f_se} is \code{NA} (single
observation links) or when \code{f} is zero.
}
\description{
Internal helper that fits one no-intercept weighted linear model per
age-to-age link:

\deqn{C_{i,k+1} = f_k \cdot C_{i,k} + \varepsilon_{i,k}}

Weights are proportional to \eqn{w_{i,k} / C_{i,k}^{2 - \alpha}}, where
\eqn{w_{i,k}} is either a constant or a column supplied via \code{weights}.
This corresponds to Mack's variance assumption
\eqn{\mathrm{Var}(C_{i,k+1} \mid C_{i,k}) \propto C_{i,k}^{\alpha}}.

When only one observation is available for a link, the factor is computed
directly as \code{value_to / value_from} and standard errors are set to \code{NA}.

Near-zero values of \code{f_se} and \code{sigma} (below \code{tol}) are set to zero to
avoid numerical noise from essentially perfect fits.
}
\keyword{internal}
