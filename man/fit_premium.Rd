% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/premium.R
\name{fit_premium}
\alias{fit_premium}
\title{Fit a chain ladder projection on the premium triangle}
\usage{
fit_premium(
  x,
  method = c("ed", "cl"),
  alpha = 1,
  regime = NULL,
  sigma_method = c("locf", "min_last2", "loglinear", "mack", "none"),
  recent = NULL,
  tail = FALSE,
  conf_level = 0.95,
  bootstrap = NULL,
  B = 999L,
  seed = NULL
)
}
\arguments{
\item{x}{A \code{"Triangle"} object. The standardized \code{"premium"} column
is used as the projection metric.}

\item{method}{One of \code{"ed"} (default) or \code{"cl"}.}

\item{alpha}{Numeric scalar controlling the variance structure passed
through to \code{\link[=fit_ata]{fit_ata()}}. Default \code{1}.}

\item{regime}{Optional regime specification (premium side). Accepts
four input types:
\describe{
\item{\code{NULL} (default)}{No regime filter.}
\item{\code{Regime} object}{Use as-is. Typically built via
\code{\link[=detect_regime]{detect_regime()}} or \code{\link[=regime_at]{regime_at()}}.}
\item{\code{"auto"}}{Detect regime internally via
\code{detect_regime(x, loss = "ratio")} on the input triangle.}
\item{Function / closure}{A user-supplied
\verb{function(tri) -> Regime} for deferred custom-config detection.}
}
Pre-change cohorts (cohorts before the resolved \code{Regime}'s change
date) are excluded from premium factor estimation.}

\item{sigma_method}{Method used to extrapolate \code{sigma} for links where it
cannot be estimated. One of \code{"locf"} (default), \code{"min_last2"},
\code{"loglinear"}, \code{"mack"}, or \code{"none"}. \code{"mack"} applies the Mack
(1993, Appendix B) tail estimator to the last unestimated link only,
falling back to LOCF for any earlier ones with a warning. \code{"none"}
performs no extrapolation; \code{sigma} stays \code{NA} and downstream variance
terms drop those links via finite-value guards. Passed to
\code{\link[=.extrapolate_sigma_ata]{.extrapolate_sigma_ata()}}.}

\item{recent}{Optional positive integer; recent calendar-diagonal
filter for the underlying ATA fit. Default \code{NULL}.}

\item{tail}{Logical; whether to apply a tail factor. Default \code{FALSE}.}

\item{conf_level}{Confidence level for analytical CI on the premium
projection (\code{premium_ci_lo}, \code{premium_ci_hi}). Default \code{0.95}.}

\item{bootstrap}{Bootstrap configuration. Five forms accepted:
\describe{
\item{\code{NULL} (default)}{Auto-resolved by \code{method}: bootstrap for
\code{"ed"}, analytical for \code{"cl"}. Same behavior as the legacy
\code{bootstrap = NULL} shape.}
\item{\code{TRUE} / \code{FALSE}}{Back-compat with the legacy logical arg.
\code{TRUE} triggers \code{bootstrap = "auto"}; \code{FALSE} disables.}
\item{\code{"auto"}}{Internal \code{bootstrap()} call on the premium triangle
with defaults \verb{(type = "analytical", process = "normal", target = "premium")}.}
\item{\code{BootstrapTriangle}}{Pre-built object from \code{bootstrap()}.
Must have \code{meta$target == "premium"}.}
\item{Function \verb{function(tri) -> BootstrapTriangle}}{Lazy spec
invoked on the input Triangle (leakage-safe for \code{backtest()}).}
}
Regardless of \code{method}, the bootstrap path uses CL recursion --
premium's self-anchor makes ED and CL algebraically equivalent
(\code{g_k = f_k - 1}, \verb{sigma^2_g = sigma^2_f}).}

\item{B}{Integer number of bootstrap replicates. Used only when
\code{bootstrap} resolves to \code{"auto"}. Default \code{999L}.}

\item{seed}{Optional integer seed for reproducible bootstrap. Default
\code{NULL}.}
}
\value{
An object of class \code{"PremiumFit"} (a list with the same
structure as \code{CLFit}). Components: \code{selected}, \code{full}, \code{data},
plus attribute \code{premium_method}. The \verb{$full} data.table uses
role-specific column names (\code{premium_obs}, \code{premium_proj},
\code{incr_premium_proj}, \code{premium_proc_se}, \code{premium_param_se},
\code{premium_total_se}, \code{premium_proc_cv}, \code{premium_param_cv},
\code{premium_total_cv}, \code{premium_ci_lo}, \code{premium_ci_hi}). Under
\code{bootstrap = TRUE}, \code{premium_ci_lo} / \code{premium_ci_hi} are bootstrap
quantiles and \code{premium_total_se} / \code{premium_total_cv} are derived
from the simulation SD; the analytical proc/param decomposition is
retained as diagnostic.
}
\description{
Project cumulative premium across the cohort x development grid with
a chain ladder estimator. Two variance recursions are supported:

\describe{
\item{\code{"ed"} (default)}{Additive recursion. Empirically more robust
on long-projection premium triangles -- the multiplicative
scaling of the classical CL recursion can blow up under cohort-wise
heterogeneity (regime changes in premium, channel changes,
amendments). See \code{dev/premium_projection.qmd}.}
\item{\code{"cl"}}{Mack (1993) multiplicative recursion. Point projection
identical to ED; only the SE accumulation differs.}
}

Both methods share the same point estimate -- self-weighted ED on
premium is mathematically equivalent to chain ladder on the same
column (\code{f_k = 1 + g_k}). The only operational difference is how
cumulative variance is propagated forward.
}
\examples{
\dontrun{
data(experience)
tri <- as_triangle(
  experience[coverage == "surgery"],
  groups   = "coverage",
  cohort   = "uy_m",
  calendar = "cy_m",
  loss     = "incr_loss",
  premium  = "incr_premium"
)

# ED-additive recursion (default; robust on long projections)
pf <- fit_premium(tri)
summary(pf)

# CL-multiplicative recursion (Mack)
pf_cl <- fit_premium(tri, method = "cl")
}

}
\seealso{
\code{\link[=fit_cl]{fit_cl()}}, \code{\link[=fit_ed]{fit_ed()}}, \code{\link[=fit_ratio]{fit_ratio()}}, \code{\link[=as_triangle]{as_triangle()}}.
}
