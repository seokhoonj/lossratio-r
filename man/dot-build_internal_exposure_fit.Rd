% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exposure.R
\name{.build_internal_exposure_fit}
\alias{.build_internal_exposure_fit}
\title{Build the internal exposure-side \code{ExposureFit} for a within-role composer}
\usage{
.build_internal_exposure_fit(
  x,
  alpha,
  sigma_method,
  recent = NULL,
  regime = NULL,
  groups = NULL,
  conf_level = 0.95
)
}
\arguments{
\item{x}{A \code{Triangle}.}

\item{alpha, sigma_method, recent, regime}{Forwarded to \code{\link[=fit_cl]{fit_cl()}}.}

\item{groups}{Group columns, for the incremental / CI derivation in
\code{\link[=.exposure_rename_full]{.exposure_rename_full()}}.}

\item{conf_level}{Confidence level for the analytical CI columns.}
}
\value{
A \code{CLFit} with \code{ExposureFit} prepended to its class and an
\verb{exposure_*}-schema \verb{$full}.
}
\description{
Within-role composers (\code{fit_bf()}, \code{fit_cc()}) need an ultimate
exposure projection. They obtain it by calling the worker \code{fit_cl()}
directly on the standardized \code{exposure} column -- a downward
worker-layer dispatch, avoiding the upward dependency on the
\code{fit_exposure()} dispatcher -- then translating the worker's \verb{loss_*}
schema to \verb{exposure_*} and tagging the result as an \code{ExposureFit}.
This helper packages that shared three-step block.
}
\keyword{internal}
