% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{.boot_refit_cl}
\alias{.boot_refit_cl}
\title{Refit chain-ladder per bootstrap replicate}
\usage{
.boot_refit_cl(triangle, boots, alpha = 1)
}
\arguments{
\item{triangle}{The original \code{Triangle} (carries the observed region
mask).}

\item{boots}{A \code{BootstrapTriangle} from \code{bootstrap()}.}

\item{alpha}{Mack variance exponent (currently \code{1} only).}
}
\value{
A \code{data.table} keyed by \verb{[groups..], cohort, dev, rep} with
columns \code{cell_mean}, \code{cell_proc_var}.
}
\description{
For each replicate \code{b} in \code{boots$alt_triangles}, take the alt cumulative
loss triangle, refit Mack chain-ladder factors \verb{f*_k} and \verb{sigma^2*_k},
and compute the per-cell process variance \verb{sigma^2*_k * C_\{i,k-1\}} for
projected (missing-region) cells. Observed-region cells get
\code{cell_proc_var = 0} so Stage 2 does not add noise to them.
}
\details{
\code{cell_mean} is the alt cumulative loss as already projected by
\code{bootstrap.Triangle()}. \code{cell_proc_var} carries the \emph{method-specific}
variance baked in so the downstream Stage 2 helper can stay
method-blind.
}
\keyword{internal}
