% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/experience.R
\name{as_experience}
\alias{as_experience}
\title{Coerce a dataset to an \code{Experience} object}
\usage{
as_experience(df, add_period = TRUE)
}
\arguments{
\item{df}{A data.frame containing experience data.}

\item{add_period}{Logical; if \code{TRUE}, derive additional period
variables using \code{\link[=add_experience_period]{add_experience_period()}}. Default is \code{TRUE}.}
}
\value{
A data.frame with class \code{"Experience"} prepended.
}
\description{
Coerce a data.frame to a minimal \code{Experience} object for loss ratio
analysis.

This function checks that the input contains the minimum required
columns, attempts to coerce them to the expected classes, optionally
derives standard period variables via \code{\link[=add_experience_period]{add_experience_period()}}, and
prepends class \code{"Experience"}.

The function intentionally performs only minimal coercion. Other
columns such as grouping variables or presentation variables are left
unchanged and should be cleaned by the user in advance.
}
\details{
Minimum required columns are:
\itemize{
\item \code{cym}          : Calendar year-month (\code{Date} or coercible to \code{Date})
\item \code{uym}          : Underwriting year-month (\code{Date} or coercible to \code{Date})
\item \code{loss_incr}    : Per-period loss amount (\code{numeric} or coercible)
\item \code{premium_incr} : Per-period premium (\code{numeric} or coercible); for
long-term health insurance, risk premium is commonly used
}

If \code{add_period = TRUE}, additional period variables such as \code{uy},
\code{uyh}, \code{uyq}, \code{cy}, \code{cyh}, \code{cyq}, \code{elap_y}, \code{elap_h}, and \code{elap_q} may be
added, depending on the available source columns.
}
\examples{
\dontrun{
x <- as_experience(df)
class(x)
}

}
\seealso{
\code{\link[=check_experience]{check_experience()}}, \code{\link[=add_experience_period]{add_experience_period()}}
}
