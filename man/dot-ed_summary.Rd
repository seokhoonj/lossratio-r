% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ed.R
\name{.ed_summary}
\alias{.ed_summary}
\title{Cohort-level reserve summary for an \code{EDFit}}
\usage{
.ed_summary(x)
}
\arguments{
\item{x}{An object of class \code{"EDFit"} with a populated \verb{$full} slot.}
}
\value{
The input \code{x} with \verb{$summary} filled.
}
\description{
Internal helper that derives the per-cohort \code{latest} / \code{loss_ult} /
\code{reserve} / process / parameter / total SE table from \code{x$full} and
stores it on \code{x$summary}. Mirrors \code{\link[=.cl_summary]{.cl_summary()}} for cross-paradigm
slot symmetry: both \code{CLFit$summary} and \code{EDFit$summary} carry the same
columns, so downstream consumers (\code{summary.CLFit()}, future
\code{summary.EDFit()} reserve view, \code{fit_ratio()} composition, etc.) read
from a uniform layout.
}
\keyword{internal}
