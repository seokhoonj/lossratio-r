% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bf.R
\name{fit_bf}
\alias{fit_bf}
\title{Bornhuetter-Ferguson projection}
\usage{
fit_bf(
  x,
  loss = "loss",
  premium = "premium",
  prior,
  bootstrap = NULL,
  B = 999L,
  seed = NULL,
  type = c("parametric", "nonparametric", "analytical"),
  residual = c("cell", "link"),
  process = c("gamma", "od_pois", "normal"),
  alpha = 1,
  sigma_method = c("locf", "min_last2", "loglinear", "mack", "none"),
  recent = NULL,
  regime = NULL,
  maturity = NULL,
  credibility = NULL,
  conf_level = 0.95,
  ...
)
}
\arguments{
\item{x}{A \code{Triangle} object.}

\item{loss}{A single cumulative loss variable to project. Default
\code{"loss"}.}

\item{premium}{A single cumulative premium variable used as the
denominator of the prior ELR. Default \code{"premium"}.}

\item{prior}{The a priori expected loss ratio. Accepts:
\describe{
\item{single numeric}{Applied uniformly to every cohort.}
\item{per-cohort \code{data.frame} (\code{cohort} + \code{elr})}{Per-cohort
ELR. Must cover every cohort present in \code{x} (extras are
silently dropped, missing cohorts raise an error).}
\item{per-group \code{data.frame} (grouping columns + \code{elr})}{One ELR
per group, broadcast to every cohort in that group. Useful when
a single a priori ELR is set per line of business. Must cover
every group present in \code{x}.}
}
A \code{data.frame} prior may also carry an optional \code{elr_se} column --
the standard error of the a priori ELR (a \emph{distribution prior}).
When supplied, the bootstrap path draws a per-replicate ELR from
\code{Normal(elr, elr_se)} instead of treating the prior as a fixed
point. Omit it (or leave \code{NA}) for a deterministic prior.}

\item{bootstrap}{Bootstrap configuration. Five forms accepted:
\describe{
\item{\code{NULL} / \code{FALSE} (default)}{Point estimate only -- no
bootstrap SE/CI.}
\item{\code{TRUE} / \code{"auto"}}{Internal \code{bootstrap()} calls (one for
loss, one for premium) sharing \code{seed} so replicate indices
align across the two simulations.}
\item{Named list \code{list(loss = BootstrapTriangle, premium = BootstrapTriangle)}}{Pre-built objects from \code{bootstrap()}. Must
have matching \code{meta$B} / \code{meta$seed} so per-replicate
composition is well-defined; \code{meta$target} must be \code{"loss"}
and \code{"premium"} respectively.}
\item{Function \verb{function(tri) -> list(loss = ..., premium = ...)}}{Lazy spec invoked on the input Triangle (leakage-safe
for \code{backtest()}).}
}
Latest observed cumulative loss is \emph{not} perturbed in the BF
recipe -- it is treated as the cohort anchor, mirroring the
point-estimate formula.}

\item{B}{Integer number of bootstrap replicates. Used only when
\code{bootstrap} resolves to \code{"auto"}. Default \code{999}.}

\item{seed}{Optional integer seed for reproducible bootstrap. Default
\code{NULL}.}

\item{type}{One of \code{"parametric"} (default), \code{"nonparametric"}, or
\code{"analytical"}. \code{"parametric"} / \code{"nonparametric"} select the
bootstrap residual paradigm; \code{"analytical"} skips simulation and
uses the closed-form Mack (2008) BF MSEP decomposition for the
cohort-level SE / CI. When no bootstrap is requested the analytical
path is used regardless of \code{type}.}

\item{residual}{Residual scope for \code{type = "nonparametric"}. One of
\code{"cell"} (default) or \code{"link"}. See \code{\link[=bootstrap]{bootstrap()}}.}

\item{process}{One of \code{"gamma"} (default), \code{"od_pois"}, or \code{"normal"}.
See \code{\link[=bootstrap]{bootstrap()}}.}

\item{alpha}{Numeric scalar passed through to the inner \code{\link[=fit_cl]{fit_cl()}} and
\code{\link[=fit_premium]{fit_premium()}} calls. Default \code{1}.}

\item{sigma_method}{Sigma extrapolation method forwarded to
\code{\link[=fit_cl]{fit_cl()}} / \code{\link[=fit_premium]{fit_premium()}}. Default \code{"locf"}.}

\item{recent}{Optional positive integer; calendar-diagonal filter
forwarded to the inner fits. Default \code{NULL}.}

\item{regime}{Optional regime specification forwarded to the inner
loss and premium fits. See \code{\link[=fit_cl]{fit_cl()}} for the four-type dispatch.}

\item{maturity}{Optional maturity specification forwarded to the inner
loss fit. See \code{\link[=fit_cl]{fit_cl()}} for the four-type dispatch.}

\item{credibility}{Optional credibility specification. \code{NULL}
(default) gives the classical BF blend with weight equal to the
emergence fraction \code{q}. A list \code{list(method = "bs", K = NULL)}
switches to a Buehlmann-Straub credibility blend
\code{ult = Z * CL + (1 - Z) * prior}, where \code{Z = K / (K + s^2)},
\code{s^2} is the variance of the cohort's own CL loss-ratio estimate,
and \code{K} is the variance of the hypothetical means (the genuine
between-cohort spread). \code{K} is estimated per group when \code{NULL}, or
supplied as a non-negative numeric scalar. The credibility weight
protects rare-event cohorts: a green cohort with a CL estimate
built on almost no data has a large \code{s^2}, so \code{Z} shrinks toward 0
and the cohort is pulled to the prior even when its \code{q} is high.
A credibility blend always uses the analytical SE path (the SE is
approximate -- the credibility factor is treated as a fixed
plug-in).}

\item{conf_level}{Confidence level for the bootstrap quantile CI on
\code{loss_ult}. Default \code{0.95}.}

\item{...}{Reserved for future extension (currently unused).}
}
\value{
An object of class \code{"BFFit"} containing:
\describe{
\item{\code{call}}{The matched call.}
\item{\code{data}}{The input \code{Triangle}.}
\item{\code{method}}{\code{"bf"}.}
\item{\code{groups}}{Grouping variable names.}
\item{\code{cohort}}{Raw cohort variable name.}
\item{\code{dev}}{Raw development variable name.}
\item{\code{loss}, \code{premium}}{Loss / premium variable names.}
\item{\code{full}}{\code{data.table} \verb{[group, cohort, dev, loss_obs, loss_proj, premium_obs, premium_proj, is_observed, incr_loss_proj, incr_premium_proj]}. When \code{bootstrap} is
enabled, additional columns \code{loss_total_se}, \code{loss_total_cv},
\code{loss_ci_lo}, \code{loss_ci_hi} carry per-cell bootstrap SE / CI on
projected cells (observed cells stay \code{NA}).}
\item{\code{proj}}{Same shape as \code{full}, with observed-cell projection
columns NA'd out.}
\item{\code{summary}}{Cohort-level reserve summary: \verb{[group, cohort, latest, loss_ult, reserve, elr, q]}. When \code{bootstrap} is
enabled, additional columns \code{loss_total_se}, \code{loss_total_cv},
\code{loss_ci_lo}, \code{loss_ci_hi} carry bootstrap SE / CI on
\code{loss_ult}.}
\item{\code{prior}}{Resolved \code{data.table(group..., cohort, elr)}.}
\item{\code{q}}{\code{data.table(group..., cohort, q)} of expected
emerged fractions.}
\item{\code{credibility}}{\code{NULL} for the classical blend, or a list
\code{list(method, weights)} where \code{weights} is a \code{data.table}
\verb{[group..., cohort, Z, K]} of the Buehlmann-Straub credibility
factors used in place of \code{q}.}
\item{\code{cl_fit}}{The inner \code{CLFit} used to derive \eqn{q_i}.}
\item{\code{premium_fit}}{The inner \code{PremiumFit} used to derive
\eqn{E_i^{ult}}.}
\item{\code{bootstrap}}{When \code{bootstrap} is enabled, a
\code{BFBootstrap} helper holding both Triangle-level
\code{BootstrapTriangle} objects and the per-replicate ultimate
replicates; \code{NULL} otherwise.}
\item{\code{ci_type}}{\code{"bootstrap"} when a bootstrap was run,
\code{"analytical"} when the closed-form Mack (2008) MSEP was used.
In the analytical case \verb{$summary} carries \code{loss_total_se},
\code{loss_total_cv}, \code{loss_ci_lo}, and \code{loss_ci_hi}.}
\item{\code{alpha}, \code{sigma_method}, \code{recent}, \code{regime}, \code{maturity}}{
Inputs forwarded to the inner \code{\link[=fit_cl]{fit_cl()}} / \code{\link[=fit_premium]{fit_premium()}}
calls.}
}
}
\description{
Fit a Bornhuetter-Ferguson (1972) projection from a \code{"Triangle"}
object. The BF estimator blends the \emph{observed} cumulative loss for
each cohort with an \emph{a priori} expected loss ratio (ELR) applied to
the cohort's ultimate premium, weighted by the expected unemerged
fraction \eqn{1 - q_i}:

\deqn{\hat L_{ult, i}^{BF} = L_{obs, i} +
  (1 - q_i) \cdot \mathrm{ELR}_i \cdot E_i^{ult}}

where
\itemize{
\item \eqn{L_{obs, i}}: cohort \eqn{i}'s observed cumulative loss
at its latest observed development period.
\item \eqn{q_i = L_{obs, i} / \hat L_{ult, i}^{CL}}: the \emph{expected
emerged fraction}, equivalent to the inverse of the cumulative
loss development factor (LDF) for cohort \eqn{i}.
\item \eqn{\mathrm{ELR}_i}: the user-supplied a priori expected loss
ratio for cohort \eqn{i} (\code{prior} argument).
\item \eqn{E_i^{ult}}: cohort \eqn{i}'s ultimate premium, projected
via chain ladder on the \code{premium} column.
}

This is a peer worker alongside \code{\link[=fit_cl]{fit_cl()}} / \code{\link[=fit_ed]{fit_ed()}} / \code{\link[=fit_loss]{fit_loss()}}.
Standalone for the BF recipe -- composition with \code{\link[=fit_ratio]{fit_ratio()}} is not
part of this worker. Point projection is always computed; bootstrap
SE / CI is opt-in via \code{bootstrap = TRUE} (Phase 3b). Closed-form
Mack (2008) MSEP is not yet implemented.
}
\examples{
\dontrun{
data(experience)
tri <- as_triangle(
  experience[coverage == "surgery"],
  groups   = "coverage",
  cohort   = "uy_m",
  calendar = "cy_m",
  loss     = "incr_loss",
  premium = "incr_premium"
)

# Scalar prior: 0.7 ELR for every cohort
bf1 <- fit_bf(tri, prior = 0.7)
summary(bf1)

# Per-cohort prior table
prior_tbl <- data.frame(
  cohort = unique(tri$cohort),
  elr    = c(0.6, 0.65, 0.7, 0.72, 0.75)
)
bf2 <- fit_bf(tri, prior = prior_tbl)
}

}
\references{
Bornhuetter, R. L. and Ferguson, R. E. (1972). The actuary and IBNR.
\emph{Proceedings of the Casualty Actuarial Society}, 59, 181-195.

Mack, T. (2008). The prediction error of Bornhuetter/Ferguson.
\emph{ASTIN Bulletin}, 38(1), 87-103. (MSEP -- not yet implemented.)
}
\seealso{
\code{\link[=fit_cc]{fit_cc()}} (pooled ELR variant), \code{\link[=fit_cl]{fit_cl()}},
\code{\link[=fit_premium]{fit_premium()}}
}
