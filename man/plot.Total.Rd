% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/triangle-vis.R
\name{plot.Total}
\alias{plot.Total}
\title{Plot a \code{Total} object as a per-group bar chart}
\usage{
\method{plot}{Total}(
  x,
  value_var = "lr",
  amount_divisor = 1e+08,
  theme = c("view", "save", "shiny"),
  ...
)
}
\arguments{
\item{x}{An object of class \code{Total}.}

\item{value_var}{A single metric to plot. Must be one of the columns
carried by a \code{Total}: \code{"lr"}, \code{"loss"}, \code{"premium"}, \code{"loss_share"}, or
\code{"premium_share"}. Default \code{"lr"}.}

\item{amount_divisor}{Numeric scaling factor used only for y-axis
labels of amount variables. Default \code{1e8}.}

\item{theme}{A string passed to \code{\link[=.switch_theme]{.switch_theme()}}
(\code{"view"}, \code{"save"}, \code{"shiny"}).}

\item{...}{Additional arguments passed to \code{\link[=.switch_theme]{.switch_theme()}}.}
}
\value{
A \code{ggplot} object.
}
\description{
Visualise an object of class \code{Total} as a horizontal bar chart, with
one bar per group. Because \code{Total} has no time dimension, this is a
simple group-level comparison of the chosen metric (loss ratio, total
loss, etc.) rather than a trajectory.
}
\details{
Bars are ordered by the value of \code{value_var} (descending). When more
than one grouping variable is present, an interaction is used as the
bar identifier.

Ratio and proportion metrics are plotted on the original scale and
labelled as percentages. Amount metrics are plotted on the original
scale and labelled using \code{amount_divisor}.
}
\examples{
\dontrun{
tot <- build_total(df, group_var = coverage)
plot(tot)
plot(tot, value_var = "loss")
}

}
