% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.apply_regime_filter}
\alias{.apply_regime_filter}
\title{Apply regime-break (cohort) filter to a triangle-shaped data.table}
\usage{
.apply_regime_filter(
  dt,
  regime,
  grp = character(0),
  coh,
  dev,
  dev_split = NULL
)
}
\arguments{
\item{dt}{A data.table.}

\item{regime}{The cohort cutoff. Accepts:
\itemize{
\item \code{NULL} -- no filter (return copy of \code{dt} unchanged).
\item A single Date or character (coercible to Date).
\item A Date/character vector -- uses the latest (max) date.
\item A single-group \code{Regime} object -- extracts the latest from
\verb{$breakpoints}.
\item A multi-group \code{Regime} object -- dispatches per group on the
intersection of \code{Regime$groups} and \code{grp}.
}}

\item{grp}{Character vector of group columns (may be empty).}

\item{coh}{Single column name for the cohort variable.}

\item{dev}{Single column name for the development variable.}

\item{dev_split}{Optional numeric scalar — the maturity target dev
(= \code{ata_to}, equivalently the first CL-region dev). When supplied,
the cohort filter is only applied to rows where \code{dev < dev_split}
(ED region); rows with \code{dev >= dev_split} (CL region) are kept
regardless of cohort.}
}
\value{
A filtered copy of \code{dt} (class preserved).
}
\description{
Drops rows where \code{coh < break_date}. Optionally restrict the filter
to rows with \code{dev < dev_split} (the ED region of an SA fit); rows
with \code{dev >= dev_split} (CL region) are kept regardless of cohort.

Supports both \strong{scalar} dispatch (single break date applied to every
row) and \strong{per-group} dispatch (different break date per group,
broadcast via left-join). The mode is auto-selected from
\code{regime} and \code{grp}: a multi-group \code{Regime} whose \verb{$groups}
intersect \code{grp} triggers the per-group path. Groups in \code{dt} that have
no matching break date (NA after the left-join) are kept unfiltered.
}
\keyword{internal}
