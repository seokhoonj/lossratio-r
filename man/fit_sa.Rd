% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sa.R
\name{fit_sa}
\alias{fit_sa}
\title{Fit stage-adaptive (SA) loss projection on a Triangle}
\usage{
fit_sa(
  x,
  loss = "loss",
  premium = "premium",
  alpha = 1,
  premium_fit = NULL,
  premium_method = c("ed", "cl"),
  premium_alpha = 1,
  sigma_method = c("locf", "min_last2", "loglinear", "mack", "none"),
  recent = NULL,
  regime = NULL,
  maturity = "auto",
  tail = FALSE,
  conf_level = 0.95,
  bootstrap = NULL,
  B = 999L,
  seed = NULL,
  type = c("parametric", "nonparametric", "analytical")
)
}
\arguments{
\item{x}{A \code{"Triangle"} object. The standardized \code{"loss"} and
\code{"premium"} columns are used (\code{as_triangle()} produces these).}

\item{loss}{Cumulative loss column name. Default \code{"loss"}.}

\item{premium}{Cumulative premium column name. Default \code{"premium"}.}

\item{alpha}{Variance-structure exponent for the loss fit. Default \code{1}.}

\item{premium_fit}{Optional pre-built \code{PremiumFit} supplying the
premium projection. When \code{NULL}, \code{fit_sa()} builds the premium
projection internally -- a worker-layer \code{fit_cl()} on the
\code{premium} column -- using \code{premium_method}, \code{premium_alpha},
and the resolved \code{regime}.}

\item{premium_method}{One of \code{"ed"} (default) or \code{"cl"}. Used only
when \code{premium_fit = NULL}.}

\item{premium_alpha}{Variance-structure exponent for the premium fit.
Default \code{1}.}

\item{sigma_method}{Method used to extrapolate \code{sigma} for links where it
cannot be estimated. One of \code{"locf"} (default), \code{"min_last2"},
\code{"loglinear"}, \code{"mack"}, or \code{"none"}. \code{"mack"} applies the Mack
(1993, Appendix B) tail estimator to the last unestimated link only,
falling back to LOCF for any earlier ones with a warning. \code{"none"}
performs no extrapolation; \code{sigma} stays \code{NA} and downstream variance
terms drop those links via finite-value guards. Passed to
\code{\link[=.extrapolate_sigma_ata]{.extrapolate_sigma_ata()}}.}

\item{recent}{Optional positive integer; calendar-diagonal filter.}

\item{regime}{Optional regime specification (loss-side). Accepts the
standard 4-type dispatch (\code{NULL} / \code{Regime} / \code{"auto"} / function).
In SA mode the resolved regime drives the hybrid 2-pass filter
(cohort cut for the ED phase, calendar-diagonal wedge for the CL
phase).}

\item{maturity}{Maturity specification. Default \code{"auto"}. Accepts the
standard 4-type dispatch (\code{NULL} / \code{Maturity} / \code{"auto"} / function).
SA requires a maturity -- \code{NULL} disables SA entirely (use ED or
CL directly in that case).}

\item{tail}{Logical or numeric; tail factor for the CL phase.
Forwarded to the internal premium fit when relevant.}

\item{conf_level}{Confidence level for the analytical CI on the loss
projection. Default \code{0.95}.}

\item{bootstrap}{Bootstrap configuration (NULL / TRUE / FALSE /
"auto" / \code{BootstrapTriangle} / lazy function). Default \code{NULL}
resolves to \code{"auto"} (residual bootstrap) for SA.}

\item{B}{Integer number of bootstrap replicates. Default \code{999}.}

\item{seed}{Optional integer seed.}

\item{type}{Bootstrap process type. Default \code{"parametric"}. (Only used
when \code{bootstrap = "auto"}.)}
}
\value{
An object of class \code{"SAFit"}. List with components mirroring
\code{LossFit}: \code{full}, \code{proj}, \code{maturity}, \code{loss_ata_fit},
\code{premium_ata_fit}, \code{premium_fit}, \code{ed}, \code{factor}, \code{selected}, plus
metadata (\code{method = "sa"}, \code{alpha}, \code{sigma_method}, \code{recent},
\code{regime}, \code{conf_level}, \code{ci_type}, \code{bootstrap}, \code{usage}).
}
\description{
Project cumulative loss across the cohort x development grid using the
\emph{stage-adaptive} (SA) method: ED before the maturity point, CL after.
SA composes both projection paradigms anchored on a per-group maturity
switch -- a 2-pass fit (maturity detection via \code{fit_ata()}, then the
SA projection itself).

SA is a worker -- standalone, no internal method dispatch. The
role-specific entry point \code{\link[=fit_loss]{fit_loss()}} dispatches \code{method = "sa"} to
this function; users can also call \code{fit_sa()} directly.
}
\examples{
\dontrun{
data(experience)
tri <- as_triangle(
  experience[coverage == "surgery"],
  groups   = "coverage",
  cohort   = "uy_m",
  calendar = "cy_m",
  loss     = "incr_loss",
  premium = "incr_premium"
)

sa <- fit_sa(tri)
summary(sa)
}

}
\seealso{
\code{\link[=fit_loss]{fit_loss()}}, \code{\link[=fit_cl]{fit_cl()}}, \code{\link[=fit_ed]{fit_ed()}}, \code{\link[=fit_ratio]{fit_ratio()}}.
}
