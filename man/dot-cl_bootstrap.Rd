% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{.cl_bootstrap}
\alias{.cl_bootstrap}
\title{Per-cohort CL parametric bootstrap (sim matrix only)}
\usage{
.cl_bootstrap(
  target_obs,
  target_proj,
  f_sel,
  f_sigma2,
  f_var,
  last_obs,
  B,
  alpha,
  process = c("normal")
)
}
\arguments{
\item{target_obs}{Numeric vector of observed cumulative target by dev
(NA for not-yet-observed cells).}

\item{target_proj}{Numeric vector of projected cumulative target (used
only for length; values not consumed in simulation).}

\item{f_sel, f_sigma2, f_var}{Per-dev factor / Mack-sigma^2 / Var(f_hat)
vectors, aligned with \code{target_proj} indices \verb{1..n-1} (link k -> k+1).}

\item{last_obs}{Index of the last observed dev (boundary between
observed-rows-fixed and projected-rows-simulated).}

\item{B}{Integer number of bootstrap replicates.}

\item{alpha}{Variance exponent in \code{sigma_k^2 * C_k^alpha} (Mack uses 1).}

\item{process}{Process distribution. Phase 2a: only \code{"normal"}.}
}
\value{
A \verb{[length(target_proj) x B]} numeric matrix. Observed rows
are constant across columns (the observed value); projected rows
contain \code{B} simulated cumulative values. Negative simulations are
clipped to 0.
}
\description{
Simulates \code{B} replicates of a Mack chain-ladder projection path for a
single cohort. Future cells (\code{k > last_obs}) are drawn via the
multiplicative recursion (see \link{.cl_step}); observed cells are filled
with the observed value across all replicates.
}
\keyword{internal}
