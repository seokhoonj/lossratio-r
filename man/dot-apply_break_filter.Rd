% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.apply_break_filter}
\alias{.apply_break_filter}
\title{Apply regime-break (cohort) filter to a triangle-shaped data.table}
\usage{
.apply_break_filter(
  dt,
  break_date,
  grp_var = character(0),
  coh_var,
  dev_var,
  dev_max = NULL
)
}
\arguments{
\item{dt}{A data.table.}

\item{break_date}{The cohort cutoff. Accepts:
\itemize{
\item \code{NULL} -- no filter (return copy of \code{dt} unchanged).
\item A single Date or character (coercible to Date).
\item A Date/character vector -- uses the latest (max) date.
\item A \code{CohortRegime} object -- extracts the latest from \verb{$breakpoints}.
}}

\item{grp_var}{Character vector of group columns (may be empty).}

\item{coh_var}{Single column name for the cohort variable.}

\item{dev_var}{Single column name for the development variable.}

\item{dev_max}{Optional numeric scalar. When supplied, the cohort filter
is only applied to rows where \code{dev_var <= dev_max}; rows with
\code{dev_var > dev_max} are kept regardless of cohort.}
}
\value{
A filtered copy of \code{dt} (class preserved).
}
\description{
Drops rows where \code{coh_var < break_date}. Optionally restrict the filter
to rows with \code{dev_var <= dev_max} (apply only to ED-phase cells); rows
with \code{dev_var > dev_max} are kept regardless of cohort.
}
\keyword{internal}
