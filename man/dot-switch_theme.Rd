% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-plot.R
\name{.switch_theme}
\alias{.switch_theme}
\title{Unified theme dispatcher (internal)}
\usage{
.switch_theme(
  theme = c("view", "save", "shiny"),
  family = getOption("lossratio.font", ""),
  x.size = NULL,
  y.size = NULL,
  t.size = NULL,
  s.size = NULL,
  l.size = NULL,
  x.face = "plain",
  y.face = "plain",
  t.face = "plain",
  s.face = "plain",
  l.face = "plain",
  x.angle = 0,
  y.angle = 0,
  x.hjust = 0.5,
  x.vjust = 0.5,
  y.hjust = NULL,
  y.vjust = NULL,
  show_grid_major = FALSE,
  show_grid_minor = FALSE,
  legend.key.height = NULL,
  legend.key.width = NULL,
  legend.position = "right",
  legend.justification = "center",
  plot.background.fill = "transparent"
)
}
\arguments{
\item{theme}{One of \code{"view"}, \code{"save"}, \code{"shiny"}.}

\item{family}{Font family; defaults to
\code{getOption("lossratio.font", "")} (empty string = system default).}

\item{x.size, y.size, t.size, s.size, l.size}{Font sizes for x-axis,
y-axis, title, strip, and legend text. \code{NULL} leaves the ggplot
default; \code{0} hides the element.}

\item{x.face, y.face, t.face, s.face, l.face}{Font faces
(\code{"plain"}, \code{"bold"}, \code{"italic"}, \code{"bold.italic"}).}

\item{x.angle, y.angle, x.hjust, x.vjust, y.hjust, y.vjust}{Axis text
placement.}

\item{show_grid_major, show_grid_minor}{Whether to draw grid lines.}

\item{legend.key.height, legend.key.width, legend.position, legend.justification}{Legend geometry.}

\item{plot.background.fill}{Fill for the plot panel background; used
only when \code{theme = "shiny"}.}
}
\value{
A \code{\link[ggplot2:theme]{ggplot2::theme()}} object.
}
\description{
Produces a \code{\link[ggplot2:theme]{ggplot2::theme()}} configured for one of three usage
contexts:
\itemize{
\item \code{"view"}: RStudio screen exploration; sizes inherit from ggplot2
defaults.
\item \code{"save"}: embedding in spreadsheet tools such as Excel; fixed axis /
title sizes, plot background untouched.
\item \code{"shiny"}: embedding in Shiny apps; same as \code{"save"} plus a
configurable transparent plot background.
}

Setting any \verb{*.size} argument to \code{0} replaces that element with
\code{\link[ggplot2:element]{ggplot2::element_blank()}}.
}
\keyword{internal}
