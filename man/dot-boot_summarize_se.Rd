% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{.boot_summarize_se}
\alias{.boot_summarize_se}
\title{Summarize per-cell bootstrap SE decomposition (method-independent)}
\usage{
.boot_summarize_se(refit_dt, grp = character(0))
}
\arguments{
\item{refit_dt}{A \code{data.table} with \code{cell_mean} and \code{cell_real} columns
(the latter produced by \code{.boot_add_process_noise()}).}

\item{grp}{Character vector of group column names (may be \code{character(0)}).}
}
\value{
A \code{data.table} keyed by \verb{[grp..], cohort, dev} with columns
\code{target_proj} (point estimate), \code{target_proc_se}, \code{target_param_se},
\code{target_total_se}, \code{target_total_cv}, plus quantile-based 95\% CI
columns \code{target_ci_lo} / \code{target_ci_hi}.
}
\description{
Aggregates over the \code{rep} dimension to produce per-cell SE columns
matching the existing \code{target_proc_se} / \code{target_param_se} /
\code{target_total_se} / \code{target_total_cv} convention used by \code{fit_cl()} /
\code{fit_loss()} / \code{fit_premium()} / \code{fit_lr()}.
}
\details{
Decomposition:
\itemize{
\item \code{param_se} = \code{sd(cell_mean)} across replicates (Stage 1 spread)
\item \code{total_se} = \code{sd(cell_real)} across replicates (Stage 1 + 2)
\item \code{proc_se}  = \code{sqrt(max(total_se^2 - param_se^2, 0))}
}
}
\keyword{internal}
