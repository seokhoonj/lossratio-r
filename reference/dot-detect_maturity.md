# Internal: locate the first mature ata link from an `ATASummary`

Internal: locate the first mature ata link from an `ATASummary`

## Usage

``` r
.detect_maturity(
  x,
  max_cv = 0.15,
  max_rse = 0.05,
  min_valid_ratio = 0.5,
  min_n_valid = 3L,
  min_run = 2L
)
```
