# Summarise an `RatioFit` object by cohort

Summarise an `RatioFit` object by cohort

## Usage

``` r
.ratio_summary(x)
```

## Arguments

- x:

  An object of class `"RatioFit"`.

## Value

The input object with `$summary` set.
