# Expand a `Triangle` object to a full projection grid

Expand a `Triangle` object to a full projection grid

## Usage

``` r
.expand_grid(triangle, ed_fit, premium_ata_fit, loss_var, premium_var)
```
